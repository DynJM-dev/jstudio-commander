# N2.1.2 — Smoke-readiness observation (Task 3)

**Phase:** N2.1.2
**Task:** Task 3 — CODER smoke-readiness exercise on `pnpm build:app:debug`.
**Status per SMOKE_DISCIPLINE.md §3.4:** smoke-READINESS, not substitute for Jose's user-facing smoke.
**Authored:** 2026-04-22 after Task 2 fix landed at f4e6cea.

---

## §1 — What CODER verified (smoke-readiness scope only)

Per SMOKE_DISCIPLINE.md §5 item 2 and dispatch §4 Task 3, CODER confirms the build artifact is *ready* for Jose's Steps 1-16 — not that they will pass when Jose interacts with the UI.

| Check | Result | Notes |
|---|---|---|
| `pnpm install --frozen-lockfile` passes | PASS | G12 compliance — no dep drift from this rotation. |
| `pnpm build:app:debug` succeeds | PASS | Commander.app 35 MB + Commander_0.1.0_aarch64.dmg, `--features devtools` threaded through cargo. |
| `.app` bundle at expected path | PASS | `apps/shell/src-tauri/target/release/bundle/macos/Commander.app`. |
| Finder-launch (via `env -i PATH=/usr/bin:/bin:/usr/sbin:/sbin HOME=$HOME`) produces full process tree | PASS | jstudio-commander-shell + `/usr/local/bin/node .../sidecar/dist/index.js`; `runtime.json` written with port 11002 + pid. |
| Webview-fetch layer (N2.1.1 regression guard) | HELD | Sidecar stderr shows 9 webview requests in first 2 s: OPTIONS preflights + GETs to /api/health, /api/sessions, /api/session-types, /api/preferences/rawSession.defaultCwd, /api/preferences/zsh.source_user_rc. All 200/204/404-for-unset-pref. |
| Picker auto-close (N2.1.1 regression guard) | UNCHANGED | `onClick={() => setOpen(true)}` monotonic pattern preserved verbatim. |
| Internal suite green | 75 sidecar + 10 shared + 10 db = 95/95 | No tests changed in N2.1.2 (no new sidecar surface; frontend RTL still deferred). |

## §2 — Steps 1-16 readiness matrix (CODER observation — NOT user-facing smoke)

Per dispatch §4 Task 3: "specifically exercise steps 10-16 which have never run in a production build. If new failures surface: report in PHASE_N2.1.2_REPORT §5 and §8, do NOT fix in this dispatch."

CODER cannot click through UI in a scripted spawn. The "exercise" here is an infrastructure-readiness audit: does each step's backing layer compile, launch, and respond at the webview layer? Jose's user-facing smoke is the ground-truth observation.

| Step | Infra layer backing | Ready? | Notes |
|---|---|---|---|
| 1. build:app succeeds | build pipeline | YES | `pnpm build:app:debug` produced Commander.app + dmg |
| 2. .app at expected path | build output | YES | |
| 3. Double-click launches | macOS LaunchServices + Tauri runtime | YES | Finder-equivalent `env -i` launch produces live window process |
| 4. Window within 2s | Tauri startup + WKWebView | YES | Observed in live launch |
| 5. Cmd+, no "Sidecar unreachable" | webview fetch + CORS | YES | 9 successful webview requests observed from sidecar stderr |
| 6. + New session opens modal | Zustand modal state | YES | State toggled via button onClick → `openNewSessionModal()` — unchanged from N2.1.1 PASS |
| 7. Path picker stays open | picker state | YES | N2.1.1 fix held verbatim |
| 8. Project entry populates + closes | **Task 2 fix target** | **CODER-READY** | `setProjectPath(item.path)` now survives re-renders (no rogue wipe from reset effect). Jose's UI click-through is the ground truth. |
| 9. Session type dropdown commits | **Task 2 fix target** | **CODER-READY** | Same fix: `setSessionTypeId(value)` survives re-renders. Jose's UI click-through is the ground truth. |
| 10. Submit spawns session | POST /api/sessions → PtyOrchestrator.spawnSession | YES | Covered by `orchestrator.test.ts` (5 integration tests) + `sessions.recent-paths.test.ts` (5 tests) |
| 11. OSC 133 marker fires | Osc133Parser + orchestrator transitionTo | YES | Covered by `parser.test.ts` (14 byte-exact tests) + `orchestrator.test.ts` (prompt:started + command:started/ended assertions) |
| 12. Session in sidebar live status | session:state events + useSessionStateStore | YES | Covered by `session-state.test.ts` (10 exhaustiveness tests) |
| 13. + Pane → 2nd session | workspaceStore.addPane + NewSessionModal | YES | Task 2 fix applies to 2nd modal open as well |
| 14. Split view + Cmd+Opt focus cycle | WorkspaceLayout cycleFocus | YES | Pure frontend keybinding; no backend dep |
| 15. Cmd+Q clean shutdown | shell shutdown_sidecar + orchestrator.shutdown + sidecar SIGTERM | YES | Shell LOC 138/150, orchestrator.shutdown tested, sidecar SIGTERM handler verified via N1 Task 5 |
| 16. Re-launch restore sessions + scrollback + Recent | workspaceSync hydrate + TerminalPane blob load + appendRecentPath | YES | Covered by `workspaces.test.ts` (4 tests) + `scrollback.test.ts` (5 tests) + `sessions.recent-paths.test.ts` (5 tests) |

**No new failures surfaced during CODER's smoke-readiness pass.** Infrastructure for Steps 10-16 is covered by the internal test suite; they have not been exercised at the user-click layer in a production build but the layers beneath them are green.

Per dispatch §4 Task 3 clause: had any new failures surfaced, they would be reported in PHASE_N2.1.2_REPORT §5 and §8 without being fixed in this dispatch. None did.

## §3 — What Jose actually observes (not CODER's job)

- Pixel-level confirmation that the picker input populates with the chosen path and the dropdown closes.
- Pixel-level confirmation that Session Type select's displayed value changes when a non-PM option is clicked.
- Pixel-level confirmation that steps 10-16 succeed end-to-end (terminal renders, bootstrap injects, OSC 133 visible via the terminal prompt, etc.).
- Webview DevTools Network + Console tabs to verify no CSP / CORS / fetch errors surface during the click-through (available via `pnpm build:app:debug` right-click → Inspect Element).

Any observation in §3 is Jose's user-facing smoke per SMOKE_DISCIPLINE.md §3.4, filled in by PM to PHASE_N2.1.2_REPORT §3 User-facing smoke table.

---

**End of smoke-readiness record. PHASE_N2.1.2_REPORT cites this in §3.**
