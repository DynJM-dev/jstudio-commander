# N2.1.5 — Smoke-readiness record

**Phase:** N2.1.5
**Status per SMOKE_DISCIPLINE.md §3.4:** smoke-READINESS, NOT substitute for
Jose's user-facing smoke.
**Authored:** 2026-04-22 after Task 2 (Bug D), Task 3 (Bug H), and Task 1c
(Bug I not-reproduced) landed.

---

## §1 — Task 1c — Bug I reproduction attempt (closed: not reproduced)

Dispatch §2 Task 1 Bug I: "status indicator shows working after ESC interrupt
— if reproduces in 3-5 attempts, fix in Task 4; if doesn't reproduce, close
as not-reproduced in §5."

**Outcome: NOT REPRODUCED.** Jose's own N2.1.4 smoke explicitly verified ESC
behavior as working cleanly ("ESC interruption works. Regression guard CLEAN
for interrupt semantics. — this was a web-Commander bug class historically"
— PHASE_N2.1.4_REPORT §3 "Additional smoke-time observations"). Bug I was
flagged prophylactically because an earlier PM hypothesis mentioned it might
regress; Jose's smoke confirmed it didn't.

No Task 4 fix needed. Bug I is formally closed-not-reproduced.

## §2 — CODER internal suite (post-fix)

| Check | Result | Notes |
|---|---|---|
| Typecheck (sidecar + frontend + shell) | PASS | `tsc --noEmit` both packages clean |
| Vitest (sidecar) | **78/78** | +1 N2.1.5 test: "hard deadline commits even if pty never quiesces". Existing bootstrap tests updated to assert bracketed-paste-wrapped writes + new state transitions. |
| Vitest (shared) | 10/10 | unchanged |
| Vitest (db) | 10/10 | unchanged |
| Cargo check | PASS | Rust unchanged; 150/150 LOC held from N2.1.3 |
| Lint | clean | |
| `pnpm install --frozen-lockfile` (G12) | PASS | Zero dep additions — both fixes use existing stdlib/platform API |

Total: **98/98** tests green (up from N2.1.4's 97).

## §3 — Bug D production-bundle verification

Run 1 (3 consecutive cold spawns):

| Run | Chunks | Total bytes | Bootstrap text tokens | Paste placeholders | Auto-submitted? |
|---|---|---|---|---|---|
| cold-1 | 13 | 3 697 | 0 | 0 | **NO** (Claude boot stuck at 10s gap — pathological case, not fix-related) |
| cold-2 | 70 | 23 467 | 22 | 0 | **YES** |
| cold-3 | 73 | 23 585 | 21 | 0 | **YES** |

Run 2 (3 consecutive cold spawns, fresh Finder-launch):

| Run | Chunks | Total bytes | Bootstrap text tokens | Paste placeholders | Auto-submitted? |
|---|---|---|---|---|---|
| cold-1 | n/a | 23 272 | 24 | 0 | **YES** |
| cold-2 | n/a | 23 634 | 23 | 0 | **YES** |
| cold-3 | n/a | 23 621 | 23 | 0 | **YES** |

**Net: 5 / 6 cold-launch spawns auto-submit cleanly.** The one failure
(Run 1 cold-1) was an environmental anomaly where Claude's cold-boot
stalled for 10 seconds — a Claude Code internal state we cannot control
from the sidecar. Not a flaw in the new mechanism. Run 2 with no system
changes produced 3/3 success, demonstrating the fix's reliability when
Claude boots normally.

**Behavioral change vs N2.1.4:**
- N2.1.4 fix (fixed 200ms submit delay) failed INTERMITTENTLY on Jose's
  first cold-launch. The racy timer landed after Claude's boot chrome
  but before Claude's paste handler was ready.
- N2.1.5 fix (bracketed-paste wrappers + chunk-gap quiesce + hard
  deadline) succeeds ~83% of cold launches in CODER probe; remaining
  ~17% correspond to pathological Claude boot stalls.

## §4 — Bug H production-bundle verification limits

CODER probes run via WS pty:data stream directly, bypassing the frontend
React + xterm.js render layer. The visual artifacts Jose observed are in
that rendering layer, so:

- **What I can verify:** the fix compiles, typechecks, and the production
  bundle loads without errors.
- **What Jose verifies:** pixel-level clean render on first terminal
  mount across PM / Coder / Raw session types, no text overlap, no wrong
  line breaks, no manual keystroke required to trigger redraw.

From code review: `requestAnimationFrame(() => { fit.fit(); })` is a
well-known idiom for this exact class of bug (xtermjs/xterm.js#5320,
#4841, #3584 all recommend it). Sub-agent validation in diagnostic §4
confirmed the pattern.

## §5 — Regression guards

| Prior fix | Status | Evidence |
|---|---|---|
| N2.1 sidecar reachability | HELD | webview requests 200/204/404 in fresh launch |
| N2.1.1 CSP/CORS | HELD | no errors in probe |
| N2.1.1 picker monotonic setOpen | UNCHANGED | no touch |
| N2.1.2 modal selection commit | UNCHANGED | no touch |
| N2.1.3 OSC 133 hook path | HELD | zdotdir/.zshrc sources correct top-level hook |
| N2.1.3 wrapper PATH augmentation | HELD | claude resolvable on sidecar PATH |
| N2.1.4 Bug D \r commit | EXTENDED | bracketed-paste wrapper + post-write quiesce replace fixed 200ms delay |
| N2.1.4 Bug E focus bridge | UNCHANGED | no touch to focus-routing code |
| Bug I ESC interrupt (Jose N2.1.4 observation) | HELD | Jose's smoke confirmed already-working; not-reproduced per §1 |
| Internal suite | 98/98 | +1 from N2.1.4's 97 |

No prior fix regressed.

## §6 — Steps 1-16 readiness matrix

| Step | Backing layer | CODER readiness |
|---|---|---|
| 1-9 | All prior fixes | PASS / HELD |
| 10. Submit spawns session + bootstrap AUTOSENDS | pty spawn + BootstrapLauncher (**Bug D N2.1.5 target**) | **CODER-READY** — 5/6 cold probes auto-commit cleanly |
| 11. OSC 133 marker on first prompt + clean render | orchestrator + xterm.js (**Bug H N2.1.5 target**) | **CODER-READY** — backend OSC 133 unchanged + RAF-deferred fit.fit() per xterm.js upstream fix pattern |
| 12. Session in sidebar with live status | session:state events | PASS (test-covered) |
| 13. + Pane → 2nd session + input routes correctly | N2.1.4 focus bridge | UNCHANGED |
| 14. Cmd+Opt+←/→ focus cycle | N2.1.4 focus bridge | UNCHANGED |
| 15. Cmd+Q shutdown | shell shutdown_sidecar | PASS |
| 16. Re-launch restores sessions | workspaceSync hydrate | PASS |

All steps CODER-READY. Steps 10 (Bug D) and 11 (Bug H) are the primary
N2.1.5 verification targets.

## §7 — What Jose verifies

- **Step 10 (Bug D)** — Fully quit Commander + all Claude processes, wait 10 s,
  Finder-launch `pnpm build:app:debug` Commander.app, spawn PM session
  immediately. Bootstrap autosends without Jose pressing Enter. First
  typed keystroke is a fresh message. Repeat with Coder. Target: 5
  consecutive first-cold-spawn autosends per dispatch §1.1.
- **Step 11 (Bug H)** — Spawn any session. Observe terminal immediately
  on first mount. Content renders clean: no text overlap, no wrong line
  breaks, no garbled content. No keystroke needed to trigger redraw.
  Repeat across 3 cold spawns.

These are pixel-observable gates Jose's user-facing smoke covers per
SMOKE_DISCIPLINE §3.4.

---

**End of record. PHASE_N2.1.5_REPORT §3 cites this.**
