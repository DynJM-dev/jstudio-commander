# JStudio Command Center — State

## Current State
- **Issue 15.3 CLOSED 2026-04-20 (Jose-ratified ship with tracked residuals)**. P0 FULLY CLOSED: Case 2 (Read-STATE, the original 20s stuck-Idle during active Read work) now renders continuously with no pre-tool gap. Final ship stack: `dab9896` (Fix 1 isSessionWorking OR typed-Working + freshness + Fix 2 resolveActionLabel typed:Idle fall-through), `f9ab17d` (Option 4 hard-off on fresh typed-Idle), `7680da0` (Option 2 turn-bounded freshness lock on unmatchedToolUse true→false), `4d85d02` (Activity-gap: hold userJustSent through pre-tool thinking window). Total 15.3 arc commits: Phase 1 `fee7f35` → Phase 1.1 `9abe598` → §6.1 `ec3528a` → §6.2 `94e5c09` → §6.3 `0aefcf1` → §6.1.1 `d60d0f6` → §6.4 Δ1 `cf28597` → §6.4 Δ2 `5d7f57c` + test `2ecfa2b` → Tier A Item 1 `41c0f2c` (REVERTED `93b25dc` on live smoke fail) → Tier A Item 3 `00f1c30` (preserved) → §12 instrumentation `e41a3ee` → Fix 1+2 `dab9896` → Option 4 `f9ab17d` → Option 2 `7680da0` → Activity-gap `4d85d02`. Test suite 310/310 post-ship, typecheck clean. 3/5 live-smoke cases consistently clean (1, 2, 4); Cases 3 and 5 show intermittent server-side-timing residuals logged as Candidates 32+33. **Root cause of residuals**: `session.status` pane-classifier on the server lags 2–60s unpredictably based on post-completion pane text. Client-side patches (Fix 1/Fix 2/Option 2/Option 4/Activity-gap) aggressively gated typed-state signals but `session.status` remains unguarded — fixing it requires Codeman-model refactor (derive working/idle from ChatMessage[] alone, ignore server pane classification) which is out of 15.3 scope. **§20.LL-L10 discipline breakthrough**: three speculative-fix rotations (Phase 1, §6.1-6.3, Tier A) shipped unit-green and failed live smoke before §12 instrumentation rotation broke the pattern. Instrumentation-first is now durable project discipline (see `feedback_understand_before_patching`).
- **Candidate 33 logged 2026-04-20 (P2, 15.3 residual)**: **Case 5 / multi-step tool sequence — intermittent 60s stuck "Running command..."**. Observed once in final 15.3 smoke, absent in prior smoke on same code. Same root class as Candidate 32. When `session.status` pane classifier holds 'working' post-completion and typed-state also holds Working:ToolExec, all aggressive client-side gates are defeated by a non-gated `session.status` OR-branch. Fix direction: Codeman-model client-side derivation from ChatMessage[] alone, ignoring `session.status`. Cross-ref Candidate 23 (runtime contextLimit investigation) — same architectural family, both push toward "trust client-side structured signals over server pane classification." Queue together for future architectural rotation.
- **Candidate 32 logged 2026-04-20 (P2, 15.3 residual)**: **Case 3 / Edit post-completion — intermittent activity-missing during multi-step tool sequences**. In the final 15.3 smoke, first 2 of 3 bash-style dispatches inside an Edit turn showed no activity indicator; third showed activity correctly. Intermittent across runs; same code produces different behavior. Root cause: server-side timing variability of `session.status` pane classifier flipping to 'working' for tool exec windows. Fix direction: same as Candidate 33 — Codeman-model client-side derivation. Not fixable by additional OR-chain patches without a structural refactor.
- **Candidate 22 SHIPPED + VISUAL VERIFIED 2026-04-20 (`c78e238`, fresh CODER, Jose-ratified)**. `client/src/utils/text-renderer.tsx` −97/+25: removed `NUMBERED_LIST_RE`, `'plan'` variant of `TextSegment`, plan-extraction loop in `splitSegments()`, inline `AgentPlan` branch in `renderTextContent()`, plus `parsePlanStatus` / `itemsToTasks` helpers and `AgentPlan`/`PlanTask` imports (no remaining consumers in this file). Kept: code-fence segmentation, inline formatting, line-break handling, `CodeBlock` mount. Correct Plan path (`plans.ts::buildPlanFromMessages()` → `TaskCreate` events → `StickyPlanWidget` via `ChatPage.tsx:590`) untouched and still works. New test file `client/src/utils/__tests__/text-renderer-candidate-22.test.ts` (3 cases, all pass). Full suite 284/284 post-ship. **Visual verification GREEN**: Jose confirmed a 4-item numbered-list probe in PM reply rendered as prose with numeric prefixes, zero fake Plan chip. Candidate 22 CLOSED. PM reverts to structured markdown replies (numbered lists, tables allowed). Fresh CODER session that shipped this work can stand down. Dispatch: `docs/dispatches/CANDIDATE_22_DISPATCH.md`.
- **Tier A Item 1 REVERTED 2026-04-20 (`93b25dc`, PM-executed, Jose-authorized)**. `git revert 41c0f2c` clean. Test suite 284/284 post-revert + orphan-file rename. Item 3 (`00f1c30`) PRESERVED — `usePromptDetection` changes stand, cadence tests pass. **Item 1 ship rejected on browser smoke** — the reverse-scan fix did not reach the runtime failure class. Orphan hygiene: §6.1.1 WIP test file `ContextBar-6.1.1-integration.test.ts` renamed to `.disabled` (imported `getActionInfo` re-export that only existed under Item 1); logged as Candidate 31. Instrumentation rotation dispatched per Jose amendment — 5-case capture protocol with intermittency-control (see `docs/dispatches/15.3_INSTRUMENTATION_DISPATCH.md`).
- **Candidate 31 logged 2026-04-20 (P3 hygiene, NOT dispatched, do NOT patch)**: **§6.1.1 integration test coverage orphaned during 15.3 Tier A revert**. File `client/src/utils/__tests__/ContextBar-6.1.1-integration.test.ts.disabled` created as WIP during §6.1.1 rotation, never committed. Imported `getActionInfo` re-export which Item 1 `41c0f2c` added and the revert removed. Disabled to unblock test suite post-revert. Restore when §6.1.1 integration coverage is formally re-authored (or migrated into a new integration test harness). Not a 15.3 concern.
- **Live activity-monitor evidence 2026-04-20 ~02:51 AM (Jose-observed, 2 concurrent CODER sessions)**: activity monitor IS rendering rich labels correctly RIGHT NOW — "Running command... 3s" for Bash, "Thinking deeply... 27s", "Mulling 28s", "Choose an option 8s" all visible in live sessions. Yet Tier A smoke 15 minutes earlier showed Read STATE.md rendering as "Idle" in the same Commander build. **This confirms Item 1 is intermittency-class, not absolute absence.** v5 §11.1's tail-block hypothesis may still be part of the story but is not the full picture; the bug manifests on some tool-use shapes / turn structures / timing windows but not others. Instrumentation rotation MUST capture both working and failing cases in the same session for valid diff. Protocol amended to 5 cases (BASH-10, READ-STATE, EDIT-DIAG, SPAWN-AGENT, BASH-2) per Jose 2026-04-20.
- **Tier A BROWSER SMOKE FAILED 2026-04-20 (P0, Jose-observed)**. Two commits `41c0f2c` (Item 1) + `00f1c30` (Item 3) shipped and landed, but live browser smoke shows minimal user-facing improvement over pre-Tier-A state. **Case 1 PASS** (Bash sleep 10 renders "Running command..." — non-regression holds). **Case 5 PASS** (genuine idle reads correctly). **Case 2 FAIL** (approval works but NO activity indicator during tool-exec, results appear without intermediate status). **Case 3 FAIL** (Read command — no activity, red doc tool card + result right away, status says idle throughout; subsequent `read parts of X` still no activity monitor). **Case 4 FAIL** (Edit — approval fast, then momentary thinking indicator vanished instantly, status stuck "waiting for input" until done, no "Editing X..." label). **Case 6 PARTIAL** (`/compact` — 30–40s delay before "Compacting context..." indicator appears, then works correctly; should be instant since compact is a Claude Code primitive). **Pattern identical to pre-Tier-A class** — v5 §11.1's tail-block diagnosis does not fully explain Cases 2–4–6; Item 3's gate-removal does not fully explain Case 2's missing activity. **Investigation-first required before further code**. Next action: diagnose WHY Tier A didn't reach — do the new commits not build on the running dev server, is §6.1.1's `isWorkingOverride` composite not feeding through at runtime, is the reverse-scan hitting an unexpected tail shape in real JSONL vs the test fixtures, or is the whole `isWorking` pipeline upstream of where we patched. Jose forwarded the smoke results to CTO for external research. **Jose-authorized pivot**: Candidate 22 (Plan widget false-match) fix before Tier A re-diagnosis per `feedback_dev_frustration_prefers_understanding_over_patches`.
- **Issue 15.3 v5 Investigation RATIFIED by Jose 2026-04-20** (PM-reviewed + Jose-authorized). v5 addendum `docs/15.3-diagnostic.md` §11 holds. Fix shapes locked: **Item 1 = Shape A** (reverse-scan tail blocks in last-assistant-message, return first `tool_use` match; Shape B / `hasUnmatchedToolUse`-reuse logged as follow-up refinement candidate post-Tier-A); **Item 3 = primary** (remove `isActive` gate at `usePromptDetection.ts:68`, scale idle polling cadence to 5–10s; server-push via `session:prompt` WS event logged as separate follow-up dispatch). **Class A unification confirmed**: Items 2 + 3 share upstream at `usePromptDetection.ts:68`; Item 3 is the permanent tail, Item 2 the eventual tail. **Tier breakdown locked**: Tier A (Items 1 + 3 — class-rooted, code-ready) fires as SERIAL rotation now; Tier B+C (Items 2, 4, 5 — live-capture-required) fires AFTER Tier A ships green on Jose browser smoke, same CODER pipe. **Item 5 origin correction**: `git show --stat ec3528a 94e5c09 0aefcf1 d60d0f6 cf28597 5d7f57c 2ecfa2b` shows ZERO touch of `ToolCallBlock` / `AssistantMessage` / `ChatThread` tool-render code. Item 5 (Write overlay) is NOT a 15.3-thread regression — likely older origin. Candidate window: `abbbbb3` forward through activity-chip refactors (`509be5a`, `3bc6c0e`, `d4eb1eb`). Dispatch framing corrected; Item 5 reclassified as pre-15.3 legacy surface. **Foundation posture confirmed**: §6.1.1 preservation supported by evidence (Item 4 hypotheses A+B refuted at source + JSONL level; two-file wire-through does not touch `SessionSkillsChip` / `allMessages` construction). §6.4 Δ1/Δ2 preservation supported (Item 2 fix shape extends the exemption model, does not revert it). **Tier A dispatch**: `docs/dispatches/15.3_TIER_A_CODE_DISPATCH.md`.
- **Candidate 30 logged 2026-04-20 (P2 UX / visual polish, Jose-observed, NOT dispatched, do NOT patch)**: **Markdown renderer visual quality parity with VSCode Claude**. Commander's assistant-message markdown renderer produces visibly lower-quality output than VSCode Claude for identical source markdown. Reference bar: VSCode Claude's sidebar render — clean hierarchical sections, proper list typography, inline code span contrast, aligned inline icons/flags, spacing between levels. **Audit scope** (all in ChatMessage / ToolCallBlock / AssistantMessage render tree): (1) section hierarchy `##` / `###` spacing + visual weight contrast; (2) bullet + numbered list typography (leading, left gutter, indent); (3) nested list rendering; (4) inline code span styling (background, padding, font); (5) code block syntax highlighting + language header; (6) table rendering (presence + styling); (7) blockquote, horizontal rule, callout rendering; (8) inline emoji/icon treatment (🚩 ✅ ⚠) — consistent baseline, proper size; (9) paragraph spacing between blocks; (10) link styling (hover, visited); (11) bold/italic contrast. **Likely fix axes**: `@tailwindcss/typography` plugin config (prose classes, dark-theme sizing/color); remark/rehype stack (rehype-highlight for code, remark-gfm for tables/task lists); custom `ReactMarkdown` component overrides where typography alone insufficient. **Sequencing when dispatched**: (1) investigation rotation — audit current renderer stack + screenshot Commander vs VSCode Claude side-by-side for reference markdown, enumerate specific gaps; (2) fix rotation — close gaps in visual-impact order (headings + lists + code spans likely 80% of perceived quality gap); (3) self-dogfood — all PM/CODER/CTO responses rendered in Commander must match VSCode bar post-fix. **Priority**: P2 — not a bug blocker, but "rival VSCode Claude / Claude Code" UX bar in CTO acceptance. NOT 15.3 scope. Queue post-15.3 close + after M7/M8. **Cross-refs**: §23 Command Center State-of-World renderer SSOT; Candidate 22 (Plan widget over-matches plain markdown lists) — related surface, different root cause, both touch chat render tree. **Numbering note**: this is Candidate 30 (Jose originally called it "Candidate 29" but 29 was already taken earlier today by the `task_reminder` renderer-registry audit — PM renumbered to preserve coherence of STATE.md trail).
- **Candidate 29 logged 2026-04-20 (P2, Jose-observed, NOT dispatched, do NOT patch)**: **Unmapped attachment chip for `task_reminder` event type + full renderer-registry audit for other unregistered types**. Renderer-registry gap: Commander's typed-renderer set has 8 known attachment types (post-Issue 7.1 `abbbbb3`). `task_reminder` observed in transcript but no registered renderer; likely falls through to a generic attachment chip with raw type string exposed or silently drops. Candidate scope: (1) add `task_reminder` renderer; (2) audit full Claude Code event-type catalog against Commander's registered set; (3) catalog unregistered types + decide per-type whether to register a specific chip, route through generic fallback, or intentionally suppress. Related to OS §23 "Active-rendering typed-renderer list" maintenance. **Numbering note**: this is Candidate 29 (Candidate 28 = empty `commander.db` repo-root file, logged earlier today).
- **Candidate 28 logged 2026-04-20 (P3 housekeeping, Jose-observed, NOT dispatched, do NOT patch)**: **Empty `commander.db` created at repo root**. File `~/Desktop/Projects/jstudio-commander/commander.db` exists at 0 bytes; real DB lives at `~/.jstudio-commander/commander.db`. Something creates the empty file at repo root — candidates: (a) test runner with default path, (b) path-resolution bug on Commander spawn (missing tilde expansion, or relative-path fallback), (c) a dev script that opens a connection with a bare filename. Investigation should trace origin (grep `commander.db` literal in codebase, check spawn-path resolution, check vitest/jest config). Fix options: fix the path resolution OR add `commander.db` to `.gitignore` at repo root if empty-file is benign side-effect. Deferred to polish rotation.
- **M7 kickoff pre-check 2026-04-20 — JLP symlink resolution VERIFIED ✅** (Jose, commander.db query). Existing JLP CODER session `d771d04a-29c3-402b-9762-50a1750a6844` stores `project_path = /Users/josemiguelbonilla/codeman-cases/JLFamily` (realpath-resolved, not symlink surface). Issue 15.2 `fs.realpathSync` inside `resolveSessionCwd` covers JLP case. M7 project-detection works across all 9 canonical projects with no special-casing. No blocker for M7 kickoff.

## Standing references
- **JSTUDIO OS §23 — Command Center State-of-World** (`~/Desktop/Projects/JSTUDIO/OPERATING_SYSTEM.md`, dated 2026-04-18, **v2 landed 2026-04-19**). Canonical snapshot of current Command Center architecture: pane model (max 2, visual-only), split-view UX (header button, X to close), renderer SSOT (`event-policy.ts` denylist), session binding invariants (`project_path` load-bearing, heuristic matches excluded from auto-learn), **§23.3 bootstrap-injection invariants + `resolveSessionCwd` SSOT rule (Issue 10, `aaca34d`)**: Coder/PM sessions inject `/effort` + bootstrap regardless of cwd; Raw inject `/effort` only, no bootstrap; cwd spec affects tmux pane cwd only, never post-boot injection pipeline; `resolveSessionCwd` is single source of truth for "canonical session cwd" — all call sites (tmux spawn, spawn-bind watcher, row `project_path`, hook-event `cwd-exclusive` matcher) must agree on canonical form or downstream binding breaks silently; `execFile` does not expand `~`, tmux silently falls back to `$HOME` otherwise. Active-rendering typed-renderer list 4→8 (adds FileEditNote, SessionSkillsChip, InvokedSkillChip, QueuedCommandChip, Issue 7.1 `abbbbb3`). 7.2 candidates in known-rough-edges: QueuedCommandChip `Dispatched` transition (deferred — fragile text-correlation per §24), syntax highlighting for FileEditNote snippet + ToolResult file bodies (pending bundle-cost decision). Standing assets: Playwright harness, self-dogfood pattern. Changelog v2 entry: "2026-04-18 v2 — Renderer set extended from 4 to 8 (Issue 7.1); §23.3 bootstrap injection + resolveSessionCwd SSOT rule added (Issue 10)."
- **JSTUDIO OS §24 — Pattern-Matching Discipline** (new, 2026-04-18). Formalizes the rule applied across Issues 8 P0, 8.1, 9 P2 (three manifestations of the same class-of-bug). Acceptable constraint patterns: line-start anchor, verb morphology (`-ing`/`-ed`/explicit allowlist), explicit option tokens (numbered list + Yes/No/Custom for approval classifiers), activity-event verification, word boundaries for token matching. Required: inline documentation at every pattern-match site per §24.3. Deferred architectural note on canonical pane-classifier as future work. Any phase adding a new pattern-match site MUST cite §24 in its PHASE_REPORT and include the inline comment block.
- **OS §20 LESSONS_LEARNED** extended 2026-04-18 with 8 entries from this session: silent early-returns as architectural hazards (Issue 6), deterministic invariants beat heuristic fallbacks (Issue 6), symptom-side fixes can be valuable on their own merits (Issues 5 & 6 interplay), addendum discipline (Issues 4, 7, 9 Part 1), browser-verification required for interactive phases (Phase W P0, W.2), self-dogfood pattern for lifecycle features (W.2 smoke #13), pattern-matching brittleness (Issues 8 P0, 8.1, 9 P2), spec verification before dispatch (Issue 9 Part 1 grep gap).
- **OS §22.5 amendment**: CTO sessions must read §23 before proposing changes to Command Center UI / rendering / session model / binding. Stale §23 (>30 days) triggers a PM state-brief request. (Unchanged.)
- **PM ownership**: changelog + rough-edges maintenance in §23 AND §24 falls under PM purview whenever Command Center ships a structural change or a new pattern-match site lands. Update §23/§24 changelog + append to §20 LESSONS in the same pass as STATE.md after any phase that alters pane model, renderer SSOT, session binding, persistence schema, or introduces a new tool-output pattern match.

- Phase: **Track B COMPLETE (Coder-16, 2026-04-19)**. Three discrete commits, all acceptance green, candidates 19/20/21 untouched. **15.2 realpath** HEAD `faa7169`: `fs.realpathSync` folded into `resolveSessionCwd` after slash-trim + tilde-expand; non-existent paths fall back to canonical (no crash); `/tmp ↔ /private/tmp` agree post-canonicalize; 3 new tests (realpath branch + missing-path fallback + slash+realpath chain); Issue 6 invariants preserved. **§23.3 CHANGELOG text drafted for OS inclusion next session**: *"2026-04-19 — `resolveSessionCwd` canonicalizes via path normalization + realpath resolution. Covers macOS `/tmp → /private/tmp` symlink and any future symlink-based cwd at the single SSOT helper — the spawn-bind watcher + `resolveOwner.cwd-exclusive` strategy now agree on encoded path regardless of symlink depth (Issue 15.2)."* **16.1 MODEL_CONTEXT_LIMITS base-IDs → 200K** HEAD `f2821fd`: `claude-opus-4-7`, `claude-opus-4-6`, `claude-sonnet-4-6`, `claude-opus-4-5-20251101` all return 200K; `[1m]` suffix branch in `getContextLimit` still returns 1M (regression-pinned by 3 new tests for opus-4-7[1m]/sonnet-4-6[1m]/opus-4-6[1m]); `models.test.ts:11-13` inverted as directed; migration posture **left + trust** per dispatch guidance (pre-Issue-16 rows with base IDs see ctx%-correction on client reload, no DB backfill; Commander-spawned sessions since `abc1e33` default to `[1m]` bounding affected population); verified call sites `ContextBar.contextPercent` now accurate for base-ID rows + `CreateSessionModal.priceDetail` correctly shows "200K ctx" for base entries. **15.1-G compact summary → SystemNote** HEAD `227e0c6`: discriminator `isCompactSummary: true` (structured §24.2, NOT text prose); shared gets `compact_summary` ContentBlock variant; server `parseUserRecord` routes `isCompactSummary===true` → system-role + `compact_summary` block BEFORE regular user-content path (flattens array-of-text-blocks AND bare-string content defensively); client `CompactSummaryNote.tsx` chip (FileText icon, compact accent, click-to-expand); ChatThread SystemNote routes the block; 5 new parser tests pin routing matrix. **Screenshot caveat**: coder-16 had no Playwright headless access this rotation — Jose reloads tab + verifies next `/compact` synthetic summary renders as "Post-compact summary" chip (compact accent) instead of JB-crown user message. **Totals**: Server 356/356 (+8), Shared 34/34 (+3), Client 234/234 (15.1-G is pure component no added test file), typecheck clean all packages. **Candidates 19/20/21 confirmed NOT touched**.
- **Track B DISPATCH HISTORY 2026-04-19 (coder-16 active, 3 parallel commits)**: Jose live smoke on 15.1-H + 15.1-D + 15.1-C + Issue 15 originals passed 4/4. Track B fires: 15.2 realpath + 16.1 MODEL_CONTEXT_LIMITS base-ID fix + 15.1-G post-compact synthetic summary as SystemNote. Three discrete commits, one PHASE_REPORT with per-fix smoke + 15.1-G before/after screenshots.
- Phase: **Issue 15.3 §6.1.1 + §6.4 CODE COMPLETE (Coder-16, 2026-04-20)**. Four commits (one extra test-split commit, minor hygiene deviation, acceptable): `d60d0f6` §6.1.1 wire-through / `cf28597` §6.4 Delta 1 POLL_INTERVAL / `5d7f57c` §6.4 Delta 2 cooldown exemption / `2ecfa2b` Delta 2 test coverage. **§6.1.1**: single-prop `isWorkingOverride={isSessionWorking}` threaded at `ChatPage.tsx:571`; ContextBar at :323 collapses to `isWorking = isWorkingOverride ?? (sessionStatus === 'working' || userJustSent)` (nullish-coalescing preserves legacy callers); `effectiveStatus` upgraded from `userJustSent` to composite `isWorking`; upgrade-only semantic (false override does NOT downgrade server 'working', mirrors userJustSent contract). **§6.4 Delta 1**: POLL_INTERVAL 5_000 → 1_500 at status-poller.service.ts:11. **§6.4 Delta 2**: cooldown gate at status-poller.service.ts:275-286 consults `hasPendingToolUseInTranscript` when `msSinceForceIdle < FORCE_IDLE_COOLDOWN_MS`; fall through no-cooldown-restamp if pending; cooldown still fires for empty transcripts / no pending tool. **Tests (user-observable DOM framing per §20.LL-L10)**: NEW `ContextBar-6.1.1-integration.test.ts` 8 tests composing full 5-step derivation chain (isWorking → jsonlLabel → actionLabel → effectiveStatus → statusLabel); pinned DOM strings "Running command..." (Case 1 sleep 10), "Reading STATE.md...", "Editing foo.ts..." (Case 4), "Idle — Waiting for instructions" regression baseline without override, "Working..." upgrade-only proof, "Waiting for approval" non-regression. EXTENDED `phase-u1-cooldown.test.ts` with `decide()` helper `pendingTool` param + 4 new cases (exemption fires, baseline preserved, hook-yield still wins when recent, stale working + exemption still force-idles); 8 pre-existing cooldown tests unchanged. **Non-regression green**: Client 279/279, Server 367/367, typecheck clean. **Guardrails respected**: G1 (§6.4 minimum — only POLL_INTERVAL + cooldown exemption, HOOK_YIELD_MS/STALE_ACTIVITY_MS/IDLE_GRACE_MS/pane classifier untouched); G3 (no new signals, single-prop approach held, resolveActionLabel/getActionInfo/getStatusInfo logic untouched); no ephemeral console.log landed. **Self-dogfood caveat flagged per G2**: ContextBar render NOT self-verified — coder-16's own session runs in Commander, can observe own chat bubble LiveActivityRow from §6.2/§6.3, but ContextBar status-bar label renders in outer host window coder-16 cannot inspect. Explicit "awaiting Jose browser smoke" declaration. **Expected DOM post-smoke**: "Running command..." / "Reading X..." / "Editing Y..." within ≤1.5s of tool_use emission. **Regression indicator**: stuck on "Idle — Waiting for instructions" during active tool execution.
- Phase: **Issue 15.3 §6.1-6.3 CODE COMPLETE (Coder-16 fresh context, 2026-04-19)**. Three discrete commits per mandate: (a) `ec3528a` §6.1 `resolveActionLabel` Working-path inversion — jsonlLabel wins first for Working coarse status; (b) `94e5c09` §6.2 client-side `hasUnmatchedToolUse` OR-gate — ChatPage `isSessionWorking` triggers from ChatMessage tail scan independent of heartbeats; (c) `0aefcf1` §6.3 `heartbeatStale` tool-exec exemption — ChatThread receives `heartbeatStale && !unmatchedToolUse`, raw signal untouched for SessionCard/HealthBanner/HeartbeatDot. **D1 residual RULED OUT via pre-code live capture**: self-capture `sleep 10` showed tool_use `toolu_01Aejmz22morAaU6VRnFqgYN` flushed at 23:47:14.767Z, tool_result at 23:47:24.857Z — Δ=10.090s matching exact sleep duration, file mtime advanced during window. tool_use JSONL-flushed at dispatch not buffered. Documented in `docs/15.3-diagnostic.md` v3 changelog. **Tests (user-observable framing per §20 LESSONS, NOT implementation)**: `contextBarAction.test.ts` +1 new suite `hasUnmatchedToolUse` (6 cases: empty/matched/unmatched/multi-mixed/text-only/bounded-window); rewrote 2 existing ToolExec cases — "Working:ToolExec with jsonlLabel → jsonlLabel wins" pins Jose's criterion #2; added Thinking+jsonlLabel inversion case. `heartbeat.test.ts` +1 new suite `§6.3 heartbeat-stale tool-exec exemption` (3 cases: stale+no-tool=hide, stale+tool=visible, fresh+*=fresh). Client 271/271 green. `tsc --noEmit` clean. **Non-regression preserved by construction**: 15.1-A/B/C/D/G/H (Compacting typed path wins, Error/Stopped/Waiting typed authority retained); 16.1.1 (typed Idle returns null, LiveActivityRow unmounts, no backward flash); 8 P0 (`shouldSuppressComposingLabel` path untouched); 9 P2 (tmux pane regex demoted to terminalHint fallback LOWEST priority behind jsonlLabel under Working); Issue 15 sub-agent labels (Criterion 6) untouched; heartbeat raw signal unchanged for SessionCard/HealthBanner/HeartbeatDot readers. **`Working:ToolExec` deprecation**: NOT deprecated this rotation — typed subtype still serves as fallback when jsonlLabel is null (new user sessions pre-first-tool, thinking-only turns). When server producer enriched to populate `toolName` (future rotation), fallback becomes more specific. **Live smoke NOT YET RUN** — coder-16 cannot browse; 6-case matrix awaits Jose execution. Per `feedback_vite_stale_code` + `feedback_dist_shadows_vite` memories, Jose must: (a) kill+restart Commander (tsx watch unreliable); (b) hard-reload browser; (c) verify `client/dist/` mtime isn't shadowing Vite HMR before testing. **Scope discipline**: §6.4 poller re-tune deferred per authorization; Criterion 6 sub-agent labels deferred; Candidates 19/22/23/24 untouched.
- **§20.LL-L10 INSTANCE 2 noted 2026-04-19 (for OS maintenance pass)**: Same pattern as 16.1.1 — coder-16 passes test suite, ships, live smoke fails. Tests reason-from-implementation rather than pinning user-observable contract. 16.1.1: shared-layer `getContextLimit([1m]) === 1M` test passed in isolation while modal integration was broken. 15.3 Phase 1.1: decision-tree unit tests passed while typed state never emits during real bash exec. **Rule for Phase 2+ dispatches**: integration-surface tests MUST be written to user-observable behavior (e.g. "given fresh session, prompt `sleep 10`, ContextBar label must read 'Running Bash…' for 10s"), NOT to internal function contracts. Fold into OS §20 next maintenance pass.
- **Coder-16 SHUTDOWN REQUESTED 2026-04-19 (Jose-authorized, P0 context-reset)**: shutdown_request fired with reason linking §20.LL-L10 instance 2 + ~18-fix context accumulation. Next rotation is investigation-only, NO CODE. Awaiting coder-16 respawn for dispatch.
- **Issue 15.3 Phase 1.2 INVESTIGATION ROTATION DISPATCHED 2026-04-19 (fresh coder-16 active, NO CODE this rotation)**: Dispatch fired post-coder-16 respawn. Five-step protocol + 10 acceptance criteria + 4 rejection triggers + reference implementations (Claude Code JSONL, Codeman, VSCode Claude extension) + diagnostic-doc-first gate. **Standard**: the experience, not the ticket. "Does Commander feel like VSCode Claude / Claude Code terminal / Codeman?" is the test; unit-test green is NOT acceptance. **10 acceptance criteria** (carried from Jose): (1) any tool → continuous live indicator in ContextBar AND chat bubble no silent periods; (2) specific labels not generic ("Running Bash" not "Working"); (3) elapsed timer climbs on any non-instant op; (4) Idle = "genuinely waiting for user input" never "I don't know"; (5) state never flashes backward; (6) sub-agents/background/compaction distinct labels; (7) Plan widget ONLY on TodoWrite/TaskCreate not text-list shape; (8) system/synthetic render distinct from user messages; (9) Stop button correct pane in split view; (10) no stale input buffer reappearing after state transitions. **Rejection triggers for diagnostic**: (a) identifies symptom not class root cause; (b) fix narrow leaving other criteria broken; (c) speculative without live-signal capture backing; (d) "tests pass ship it" framing. **Deliverable**: single diagnostic doc `docs/15.3-diagnostic.md` with 8 sections: observable fault + CC event inventory + Codeman/Commander pipeline diff + VSCode Claude reference pattern + pipeline drop-point localization + class root cause single-coherent-explanation + high-level fix shape (NOT code) + live-smoke-against-10-criteria acceptance approach. **Post-diagnostic**: PM review → Jose review → Jose authorizes code path → fresh dispatch → code → PHASE_REPORT → Jose live smoke against 10 criteria → ship only when ALL 10 hold. Foundation (`fee7f35` + `9abe598`) preserved pending diagnostic recommendation (fix-forward vs revert).
- **Issue 15.3 Phase 1.2 INVESTIGATION ROTATION HISTORY (queued for fresh coder-16, Jose-mandated)**: No code this rotation. Five-step protocol: (1) capture Claude Code JSONL stream during real bash `sleep 10` execution — document every event Claude Code emits (type, timestamp, payload); this is source-of-truth signal; (2) diff Codeman's tool-execution detection logic vs Commander's — both tools consume same Claude Code events, Codeman works + Commander doesn't, the diff localizes the bug faster than any speculation; (3) reference VSCode Claude extension state management (public repo) — its UI stays live during all tool execution, its reader logic is reference implementation; (4) write diagnostic document BEFORE any code — mapping: which Claude Code events fire during sleep 10, which Commander receives, which it processes correctly, where the pipeline drops the signal; (5) code only after diagnostic doc is clear. **Rationale**: 3 speculative fixes stacked on accumulated context have now failed (Phase 1, Phase 1.1 state-cadence, Phase 1.1 override-broadening). Pattern = hypothesis-first reasoning without ground-truth signal. Memory `feedback_debug_with_real_data` explicitly prescribes this discipline. Dispatch fires after coder-16 respawn confirms. Foundation (15.3 Phase 1 at `fee7f35` + Phase 1.1 at `9abe598`) NOT reverted yet — preserved pending diagnostic findings; if diagnostic reveals architectural flaw, revert authorized.
- **Issue 15.3 Phase 1.1 SMOKE FAIL 2026-04-19 (P0 escalation)**: Jose smoke found BOTH repros still broken. Repro 1 (sleep 10): ContextBar label stays Idle full 10s AND chat bubble also silent — no Thinking verb, no live activity indicator, only Bash tool card's own spinner. Repro 2 (ls post-approval): 15s silence between prompt send + approval modal; click Yes → bash executes → result renders → NEVER any Working/ToolExec state visible. /compact still passes; 15.1-D / 15.1-C / 15.1-G / post-completion return-to-idle all regression-clean. **Diagnosis shift**: chat bubble silence alongside ContextBar silence means typed state is NOT EMITTING for bash execution AT ALL — not a chip-migration issue as coder-16 flagged contingency-wise. **Candidate root causes (must be instrumented, NOT speculated)**: (1) pendingToolUse probe not returning true for bash transcript entries (scope gap / transcript-tail scan boundary miss / timing race before JSONL flush); (2) computeSessionState branch 3 (Working:ToolExec) condition failing for bash; (3) `lastKnownStateKey` dedup too aggressive — first Working emit suppressed because key matches earlier frozen Idle; (4) emit path not actually wired for subtype-only deltas (Phase 1.1 fix didn't land mechanically). **Pattern flag — 3 speculative fixes failed to land** (Phase 1 + Phase 1.1 state-cadence + Phase 1.1 override-broadening). Root-cause reasoning has been hypothesis-first, not evidence-first. Per `feedback_debug_with_real_data` memory: curl/DB/tmux/logs before code review; "looks correct" is not evidence. **Context-accumulation concern**: coder-16 at ~18 fixes today, well past 2-3 phase compaction safe zone per `feedback_coder_compaction`. **PM recommendation pending Jose auth**: Path A restart coder-16 + instrumentation-first dispatch (preserve foundation) vs Path B revert 15.3 Phase 1 + 1.1 and rethink (nuke foundation). Jose to choose.
- Phase: **Issue 15.3 Phase 1.1 COMPLETE (Coder-16, 2026-04-19). HEAD `9abe598`**. Two root causes fixed, instrumentation not needed — code-level trace surfaced both gaps unambiguously. **Repro 1 root cause** (sleep 10 → Idle): Phase 1 computed state INSIDE `if (newStatus !== cachedStatus)` transition block, emitted only on coarse flips. During 10s sleep, idle→working flip fired once at tick 1 then subtype-only changes (ToolExec → Thinking post-tool) silently didn't broadcast; pre-first-flip window (0-5s) had NO state emitted → client `useSessionState` stayed null → ContextBar fell back to legacy reading REST `session.status='idle'`. **Fix**: compute state on EVERY tick, track `lastKnownStateKey` per session, emit on kind+subtype change even when coarse status unchanged. Emission cadence now: (a) coarse status flip → emit (Phase 1 behavior); (b) same coarse + different subtype → emit (NEW Phase 1.1); (c) same coarse + same subtype → no emit. Evidence field on subtype-only emits reads "state-subtype change: Idle:Generic → Idle:MonitoringSubagents" (log-greppable). **Repro 2 root cause** (ls post-approval → Waiting): 15.1-H override scoped `if (hinted.status === 'idle')`. Post-approval bash exec kept `hinted='waiting'` (residual pane text) → override never fired → status stuck at waiting. **Fix**: broaden override to fire from idle OR waiting. Safe because active approval prompts produce `pendingToolUse=false` (tool not yet emitted); broadening only catches post-approval execution window. **§24 discipline**: both fixes extend structured-signal-over-pane principle; no new pane pattern-matching. **Tests**: 15.1-H decision suite inverted `waiting+pending → pending-tool-upgrade` case with rationale; computeSessionState +3 new same-coarse-status subtype transitions (ToolExec→Thinking, Idle-Generic→MonitoringSubagents, Waiting→ToolExec via override). Server 385/385 (+3), typecheck clean, no Phase 2 work. **Self-dogfood deferred to Jose smoke**. **Status-chip contingency flagged by coder-16**: if sleep-10 still shows "Idle" post-restart, probable next suspect is status chip (legacy `sessionStatus` path in ContextBar's `getStatusInfo`) NOT migrated in Phase 1 (originally kept as Phase 4 "status bar label migration" scope). If user-facing regression persists there, decide: accelerate Phase 4 → Phase 1.2 OR migrate ContextBar's status chip as Phase 1 completeness-patch.
- **Issue 15.3 Phase 1.1 DISPATCH HISTORY 2026-04-19 (diagnostic+fix, original dispatch before ship)**: Jose smoke on Phase 1 found foundation holds but ContextBar subtype label mapping broken for Working:ToolExec. **Passed**: 15.1-D, 15.1-C, 15.1-G, Issue 15 loop bash, approval transition, Compacting label, MonitoringSubagents emission. **Broken**: (Repro 1) `sleep 10 && echo done` → ContextBar "Idle" throughout 10s, should be Working:ToolExec; (Repro 2) `ls ~/Desktop` post-approval → ContextBar "Waiting for input" during bash exec, approval-stick bug. **H1 server emission**: pendingToolName probe Generic-fallback + blank-pane sleep → classifier may fall through to Idle; OR priority ordering puts WaitingForInput too high without active-prompt recheck; OR 15.1-H `hasPendingToolUseInTranscript` gate not wired into typed classifier same way. **H2 client consumption**: `resolveActionLabel` may return null when `subtype===ToolExec` AND `toolName` absent → ContextBar renders Idle. Investigation = instrument both sides on sleep-10 + ls-post-approval, three possible findings (a) server emits correctly + client drops, (b) server emits Idle wrongly, (c) server correct + other render-path shadow. **Fix bias**: elevate pending-tool_use probe as authoritative Working:ToolExec trigger regardless of pane (15.1-H principle one layer up); explicit resolveActionLabel branches for ToolExec-without-toolName ("Running tool…"); audit WaitingForInput state clearing logic (approval-token-gone vs prompt-active-recheck). §24 structured > pane. Expected <80 LOC. Broader UX context: Commander's visibility parity promise (VSCode/Claude Code terminal/Codeman show continuous activity; Commander intermittent) requires completeness on subtype coverage + consumer wiring. **Sequencing**: Phase 1.1 ship → Jose re-smoke → if green THEN coder-16 restart → Phase 2 LiveActivityRow. Phase 2 dispatched before Phase 1.1 would inherit same gap class.
- **Issue 13.1 EVIDENCE CONFIRMED 2026-04-20 (Jose-observed during cleanup, NOT dispatched — strengthens queued item)**: Earlier manual stopped-session purge left **6,799 orphan rows across 4 tables**. Schema cascade FK is verifiably incomplete. When 13.1 dispatches (queued per post-15.3 sequence), this is the proof dataset for the schema migration justification. Per Issue 13 audit: `session_ticks` (no FK), `cost_entries` (SET NULL), `skill_usage` (SET NULL), `notifications` (SET NULL) are the 4 expected-gap tables. Cross-reference confirmed.
- **Candidate 26 logged 2026-04-20 (Jose-observed, NOT dispatched, do NOT patch)**: **`token_usage` growth rate** — 9,761 rows across 5 active sessions = ~1,950 rows/session avg. Heartbeat pattern (not session-lifecycle). Separate concern from Issue 13 retention. Follow-up candidate to audit heartbeat frequency + retention policy for `token_usage` table specifically. Not urgent. Deferred to polish rotation.
- **Candidate 27 logged 2026-04-20 (Jose-observed, NOT dispatched, do NOT patch)**: **`recovered-jsc-*` placeholder sessions** created and left behind (4 of them in the purge batch). Spawn-path produces placeholder sessions that never promote to real sessions. Issue 6-ish territory — bind-watcher / recovery-path boundary bug. Worth understanding mechanism before dispatch; likely a spawn-recovery branch that writes the row but never reconciles to a real claude_session_id.
- **Cleanup data-point for 13.1 diagnosis 2026-04-20**: `file_watch_state` table is schema-cascading correctly (zero orphans). Good narrowing signal — confirms cascade works on tables that HAVE the FK; the 4 gap tables are specifically the ones schema-gap'd in Issue 13 audit, not a general cascade failure.
- **Candidate 24 logged 2026-04-19 (Jose-observed during 15.3 Phase 1 smoke, NOT dispatched, do NOT patch)**: After `/compact` completes and user interacts further, the `/compact` prompt text reappears in the chat input box as if last-sent. Visual glitch, not functional. Scope: chat input state management post-compact; likely a prior-send-buffer not cleared on compact_boundary. File for future dispatch.
- Phase: **Issue 15.3 Phase 1 COMPLETE (Coder-16, 2026-04-19). HEAD `fee7f35`**. Canonical `SessionState` typed classifier landed + ContextBar migration. Dual-emit active. **Deliverables**: (1) `packages/shared/src/types/session-state.ts` discriminated union; (2) `server/src/services/session-state.service.ts` pure `computeSessionState(inputs)` with priority ordering test-locked; (3) poller plumbing — teammate count + preCompactService + pending-tool_use probe feed the classifier; (4) WS path via `event-bus.ts` StatusEmitExtras extended with optional `state`, `ws/index.ts` spreads extras; (5) client `useSessionState(sessionId)` hook; (6) ContextBar `resolveActionLabel` grew `sessionState` param reads typed subtype directly, legacy path preserved for fallback. **Final `SessionState` shape** (refined from PM sketch): `Idle{subtype:AwaitingFirstPrompt|JustFinished|PostCompact|MonitoringSubagents|Generic,since?}` / `Working{subtype:ToolExec|Thinking|Composing|Generic,hintLabel?,toolName?}` / `WaitingForInput{subtype:Approval|TrustFolder|NumberedChoice|YesNo|Generic,context?}` / `Stopped{reason}` / `Compacting` / `Error{message}`. **Dual-emit mechanism**: extended existing `session:status` payload with `state?: SessionState` field — NO new channel, zero additional fanout, existing consumers already re-render; clients missing type definition ignore the field, clients that have it prefer it. **Candidate resolutions**: 20 ✓ RESOLVED (poller queries teammate count per session; classifier emits `Idle:MonitoringSubagents`); 21 ✓ RESOLVED at emission layer (classifier requires explicit `waitingPromptKind` OR structurally-gated `paneStatus='waiting'` narrowed post-#236; richer subtyping ready when detectPrompts plumbed per-tick); 22 ✗ NOT TOUCHED (Plan widget migration queued Phase 3 per Option 1 scope); 15.4 PARTIALLY RESOLVED (subtypes emitted, ContextBar returns null for all idle states per spec — rich labels land in status-bar migration Phase 4). **Preserved invariants (all tested)**: 8 P0 verb-sanity ✓, 9 P2 approval-token ✓, 15 Stop-gate ✓ (drives Working:ToolExec), 15.1-D allowlist ✓, 15.1-H pending-tool override ✓ (elevated to typed subtype), Phase U.1 cooldown + 90s force-idle ✓. **Signals NOT plumbed this rotation (Generic fallback, matches pre-15.3 behavior)**: `waitingPromptKind` (resolves when detectPrompts per-tick plumbed), `lastStopAt` (new tracking column), `lastCompactBoundaryAt` (new column or JSONL-tail scan), `pendingToolName` (JSONL-tail name extraction), `stoppedReason` (SessionEnd-hook reason column). Each Generic fallback matches pre-15.3 behavior for unmigrated surfaces — no regression. **Deprecation posture**: ContextBar legacy path NOT yet deprecated — it's the fallback during dual-emit; deprecation planned after all UI surfaces migrated. Other independent classifiers (pane, applyActivityHints, prompt-detector, preCompactService) remain as INPUT sources — composed into canonical not phased out. **Test results**: Server 382/382 (+26 session-state tests pinning 11-branch priority tree), Client 260/260 (+13 resolveActionLabel typed-state tests), Shared 34/34, typecheck clean all three. **State transitions ContextBar now sees** (unit-test evidence): tool exec → Working:ToolExec label "Running Bash…" or "Running tool…"; spinner+verb → Working:Thinking hintLabel "Ruminating…"; generic work → Working:Generic legacy fallback; waiting prompt → WaitingForInput:Approval "Waiting for approval"; numbered choice → WaitingForInput:NumberedChoice "Choose an option"; Compacting → "Compacting context..." (beats jsonl + terminalHint); Idle:MonitoringSubagents → null (status bar renders teammate count); Idle:* → null (LiveActivityRow unmounts as before). **Self-dogfood deferred**: coder-16 couldn't verify inside current session (server hadn't restarted to pick up changes); Jose smoke is authoritative. **Phase 1 deliverable summary**: foundation layer + one proof surface (ContextBar). Phases 2-4 (LiveActivityRow, Plan widget incl. Candidate 22, status bar label incl. 15.4 polish) queued as discrete follow-up rotations post-coder-16 restart per Jose's locked sequence.
- **Issue 15.3 SCOPE LOCKED — Option 1 (Jose-approved 2026-04-19)**: ship canonical server-side classifier + ContextBar migration ONLY this rotation. LiveActivityRow / Plan widget / status bar label migrations queued as discrete follow-up rotations (each small + mechanical, clear acceptance, post-coder-16-restart). **Rationale**: typed subtype IS the architectural fix (unmigrated surfaces continue working via dual-emit fallback); context-accumulation threshold honored (~15 fixes today past 2-3 phase compaction safe zone per coder-lifecycle memory); revertability stays surgical per surface. Candidates 20/21/22 still resolve because subtype emission is the fix — UI surfaces that don't consume typed state yet keep current (working) behavior. **Ship this rotation**: `server/src/services/session-state.service.ts` (pure classifier + tests); poller dual-emit `session:status` with new `state` field; ContextBar `actionLabel` reads typed state with fallback. **Post-15.3 Option 1 sequence locked**: (1) Jose smokes 15.3 Phase 1 (ContextBar subtype across transitions); (2) coder-16 restart before surface migrations 2-4; (3) LiveActivityRow migration → (4) Plan widget migration → (5) status bar label migration (each discrete rotation); (6) Candidate 19 (P1 UI routing); (7) Candidate 23 investigation; (8) Issue 17 polish batch; (9) 13.1 schema cascade; (10) M5 migration resume.
- **Issue 15.3 DISPATCH HISTORY 2026-04-19 (P1, coder-16 active, 2-4hr expected, original dispatch before scope clarification)**: Canonical classifier refactor. **Core thesis**: 6+ pattern-match class-of-bugs (8 P0, 8.1, 9 P2, 15 M2, 15 M1 original, 15.1-D) plus Candidates 20/21/22 all trace to Commander deriving session state from scattered sources (pane-text classifier, hook-event Stop-gate, `applyActivityHints`, ContextBar jsonl derivation, Plan widget markdown match, approval prompt detector). Refactor: canonical classifier service consumes all signals, emits single typed `SessionState` event stream, UI surfaces subscribe. **Migration path**: (1) new service alongside existing; (2) dual-emit typed `SessionState` never-break-existing-surfaces; (3) migrate UI surfaces one at a time; (4) remove deprecated classifiers only after all surfaces migrated; (5) Phase U.1 defenses preserved as INPUT not replaced; (6) Issue 15 Stop-gate preserved as `hasPendingToolUseInTranscript` INPUT. **Must preserve as inputs NOT deleted**: 8 P0 verb-sanity filter, 9 P2 approval-token matcher, 15 Stop-gate, 15.1-D cooldown, 15.1-H tool_use override, Phase U.1 90s hook-absence guard. **Expected Candidate resolutions (verify in PHASE_REPORT)**: Candidate 20 → `MonitoringSubagents` state; Candidate 21 → `WaitingForInput` only on explicit approval-token marker; Candidate 22 → Plan renders ONLY from `tool_use.name === 'TodoWrite'` + plan metadata (not markdown shape); 15.4 → Idle subtypes (`JustFinished`/`AwaitingFirstPrompt`/`PostCompact`/`Generic`). **Does NOT resolve (out of scope)**: Candidate 19 (Stop button cross-pane, UI routing separate dispatch), 15.1-F (pre-restart subscription reinit, lifecycle), 23 (Claude Code runtime contextLimit, different scope). **Constraints**: §24 discipline throughout; structured signal > pane signal; dual emit mid-refactor; PHASE_REPORT documents Candidate resolution status per candidate + per UI surface state transitions; regression suite green on 8 P0 + 9 P2 + 15 originals + 15.1-D + 15.1-H + 15.2 + 16.1 + 16.1.1. **Suggested `SessionState` shape** (refinable): `{kind:'Idle',subtype:JustFinished|AwaitingFirstPrompt|PostCompact|Generic,since}|{kind:'Working',subtype:Composing|Thinking|ToolExec|MonitoringSubagents,hintLabel?}|{kind:'WaitingForInput',subtype:Approval|TrustFolder|NumberedChoice|YesNo,context}|{kind:'Stopped',reason:UserInitiated|ProcessExit|Crashed}|{kind:'Compacting'}|{kind:'Error',message}`. **Self-dogfood discipline invoked** per memory. **Sequencing after 15.3**: Candidate 19 (P1 UI routing) → Candidate 23 investigation → Issue 17 polish batch → 13.1 schema cascade → M5 resume.
- Phase: **Issue 16.1.1 COMPLETE (Coder-16, 2026-04-19). HEAD `13125b9`**. Modal label integration fix, single surface, <30 LOC. **Root cause — causes #1 + #2 combined** (PM dispatch hypotheses): (1) `priceDetail` did raw `MODEL_CONTEXT_LIMITS[modelId]` lookup, bypassing `getContextLimit`'s `[1m]` suffix branch entirely; (2) every `[1m]` MODEL_OPTIONS row passed the BASE id to `priceDetail` (e.g. `priceDetail('claude-opus-4-7')` for the `claude-opus-4-7[1m]` option). Pre-16.1 combo "worked" because `MODEL_CONTEXT_LIMITS['claude-opus-4-7']` was 1_000_000 — two drifts cancelled each other. 16.1's base-ID → 200K flip surfaced both simultaneously. Not causes #3 (short alias) or #4 (different constants map) — both specific to modal's own helpers. **Fix**: `priceDetail` uses `getContextLimit(modelId)` + `MODEL_PRICING[normalizeModelId(modelId)]` as SSOT; MODEL_OPTIONS entries pass `value` (full id incl. `[1m]`) to priceDetail; stale `MODEL_CONTEXT_LIMITS` import removed. **Integration coverage (the gap from 16.1)**: new `CreateSessionModal-labels.test.ts` — 13 cases, 8 `priceDetail` per-variant assertions (base + `[1m]` for opus 4.7/4.6/sonnet 4.6 + haiku + unknown fallback) + 5 MODEL_OPTIONS integration assertions (every `[1m]` starts "1M ctx", every non-suffix "200K ctx", three pair-wise base/[1m] checks). Contract pinned at modal level. **Jose-verifiable repro**: reload Create Session modal → Opus 4.7 "200K ctx · $15/$75", Opus 4.7 [1M] **"1M ctx · $15/$75"**, Sonnet 4.6 [1M] **"1M ctx · $3/$15"**, Opus 4.6 [1M] (legacy) **"1M ctx · $15/$75"**, Haiku unchanged "200K ctx · $0.8/$4". Client 247/247 (+13 new), typecheck clean. **Out-of-scope honored**: Candidates 19/20/21/22/23 untouched; 15.1-G / 15.2 / 15.1-H untouched; 15.3 not touched. **Meta-lesson**: isolation test at shared `getContextLimit` passed, but modal integration drift cancelled out pre-16.1 — integration test at the modal-label contract layer was the missing coverage. Add to OS §20 LESSONS next maintenance pass: "unit-test-only coverage on a lookup function is insufficient when UI call sites hand-roll parallel paths; pin the contract at the integration surface too."
- **Post-16.1.1 priority order locked by Jose 2026-04-19** (executes AFTER 16.1.1 PHASE_REPORT lands): (1) **15.3 canonical classifier refactor** — architectural, absorbs Candidates 20/21/22; (2) **Candidate 19 Stop button cross-pane** — P1 destructive, separate class (UI routing), standalone dispatch; (3) **Candidate 23 investigation** — verify Claude Code exposes `contextLimit` at runtime (JSONL / API / hooks); (4) **Issue 17 polish batch** — scheduled_task_fire + task_reminder + Archived Sessions + retention 30→20; (5) **Issue 13.1 schema cascade migration** — flip `session_ticks`/`cost_entries`/`skill_usage`/`notifications` SET NULL → CASCADE; (6) **Return to M5 migration work**. 15.1-F (pre-restart subscription reinit) and 15.4 (idle-label semantics) remain queued out-of-sequence, dispatch if quick or absorb into 15.3 if scope-adjacent.
- **Candidate 23 logged 2026-04-19 (architectural idea, P2, NOT dispatched)**: **Replace hardcoded `MODEL_CONTEXT_LIMITS` with Claude Code runtime values**. Commander currently derives ctx% from `tokens / MODEL_CONTEXT_LIMITS[modelId]` lookup. Table is hardcoded per model ID, requires `[1m]`-suffix discrimination, drifts from Claude Code actual behavior. **Better approach**: Claude Code exposes `contextLimit` + `contextUsed` in live session state (verify in JSONL / runtime API / hook payload). Commander reads directly, skips math entirely. **Benefits**: no hardcoded table drift; no model-ID parsing bugs (eliminates 16.1 class); no `[1m]` vs base confusion (eliminates 16.1.1 regression class); per-session authoritative state; self-healing when Claude Code adds new models. **Investigation needed BEFORE dispatch**: (1) confirm Claude Code exposes `contextLimit` in session state (verify JSONL records, runtime API, or hook events); (2) if yes → replace `MODEL_CONTEXT_LIMITS` lookup at call sites with session-state read; (3) if partial → keep table as fallback when runtime value unavailable. **Relationship to 16.1 / 16.1.1**: Candidate 23 possibly resolves the entire MODEL_CONTEXT_LIMITS surface. If 23 dispatches and ships, both 16.1 (`f2821fd`) and 16.1.1 (pending modal label fix) can be reverted as redundant. **PM recommendation**: still ship 16.1.1 as short-term fix — 16.1.1 is <20 LOC surface-level bug, affects every modal open, Candidate 23 needs investigation phase before dispatch. Reverts later are cheap. Candidate 23 queued at Jose's priority.
- **Candidate 22 logged 2026-04-19 (Jose-observed post-Track B, NOT dispatched, do NOT patch)**: **Plan widget over-matches plain markdown lists** — Commander renders any list-shaped assistant content as Plan widget, including completion summaries, Q&A responses, comparison lists. Plan should ONLY render from `TodoWrite`/`TaskCreate` tool_use events with plan metadata, not from text-block bullet shape. **Same class as 9 P2 / 8 P0 / 15.1-D** — shape vs source conflation; pattern-matching on presentation rather than authoritative structured-signal source. **Fix direction when dispatched**: route Plan widget off `tool_use.name === 'TodoWrite'` + plan metadata field, not off markdown-bullet regex in assistant text blocks. §24.2 structured-signal discipline by the book. **Likely absorbed by 15.3** canonical classifier refactor if the refactor scopes to "all UI state derivation from authoritative upstream events."
- **Candidates 19/20/21/22 dispatch-priority note 2026-04-19**: all four likely resolve via 15.3 architectural refactor (classifier + UI-derivation consolidation). **Candidate 19 is SEPARATE CLASS (UI routing, not classifier)** — may need standalone dispatch. 20/21/22 all pattern-match/shape-vs-source class; 15.3 scope decision determines whether they land in-refactor or need separate dispatches.
- **New candidates 19/20/21 logged 2026-04-19 (Jose-observed during Track A smoke, NOT dispatched, do NOT patch)**: (19) **Stop button targets wrong session in split view** — P1 when addressed; split view with two sessions, clicking Stop in one pane can interrupt session in the other; likely wiring bug between pane focus and action target. (20) **False-idle during autonomous sub-agent work** — session that dispatched TaskCreate sub-agents (Explore agents, Plan items marked "Working") shows Idle in status bar; main session has no pending tool_use so classifier technically correct, but UX gap; probably absorbed by Issue 15.3 canonical classifier. (21) **"Waiting for input" shown on sessions not actually waiting** — multiple sessions showing "Waiting for input" when not actively requesting input; should be Idle or different label; same false-state class as earlier classifier bugs; likely 15.3 territory.
- **Track B queue superseded by dispatch above; original queue text retained for history**: (3 parallel independent fixes, ONE dispatch after 15.1-H PHASE_REPORT holds) (1) **15.2** macOS `/tmp → /private/tmp` realpath — add `fs.realpathSync(canonicalCwd)` inside `resolveSessionCwd` helper; affects only `/tmp`-based test scenarios (macOS symlinks to `/private/tmp`); real project paths unaffected; extends §23.3 SSOT invariant with realpath resolution. (2) **16.1** MODEL_CONTEXT_LIMITS base-ID fix — `packages/shared/src/constants/models.ts:27-33` returns `1_000_000` for base IDs (`claude-opus-4-7`, `claude-opus-4-6`, `claude-sonnet-4-6`, `claude-opus-4-5-20251101`); `getContextLimit('claude-opus-4-7')` returns 1M incorrectly via fallback after `[1m]` regex misses; contaminates ctx-% UI for rows still holding base IDs; existing test `models.test.ts:11-13` enshrines wrong behavior and must be updated. Coder to verify against current table before patching; decide migration story for rows still holding base IDs. (3) **15.1-G** post-compact synthetic summary renders as user message — Claude Code's "This session is being continued from a previous conversation..." synthetic summary currently renders with JB crown icon as if Jose sent it; should render as SystemNote variant, visually distinct from user messages; scope `ChatThread` rendering layer, likely system-note check on synthetic summary metadata. **Constraints**: three independent files zero overlap, three discrete commits for easy revert, NO classifier work (15.1-H owns that in flight), do NOT bundle with 15.1-H or Issue 17 renderer work. **Sequencing**: hold dispatch until 15.1-H ships and holds live smoke. **Track C + D queued not batched**: Issue 17 (own rotation, larger scope), Issue 13.1 (schema migration, own rotation), 15.1-F (architectural, dispatch only if quick), 15.3 (canonical classifier refactor, decision pending on 15.1-H outcome).
- Phase: **Issue 15.1-H COMPLETE (Coder-16, 2026-04-19). HEAD `c739163`**. Gate-elevation fix: status-poller runs `hasPendingToolUseInTranscript` as authoritative override positioned BETWEEN cooldown (15.1-D oscillation guard preserved) and hook-yield (bypassed when override fires, because Stop hooks fire continuously during tool exec keeping `last_hook_at` perpetually fresh — without bypass the entire tool duration would skip). Pipeline: cooldown → **15.1-H override (idle+pending → working)** → hook-yield → stale-activity → classifier write. Narrow scope: only `idle → working` (`waiting` untouched — approval prompt IS the user-action gate); sessions without bound transcript skip (pane+timestamp remain authoritative); malformed `transcript_paths` fails to null (defensive). **6/6 acceptance**: (1) sleep-based pending tool_use in JSONL → override fires → Working full sleep; (2) loop-based pane shows verb → classifier returns working → override no-op; (3) short tool → override fires at T+0 when tool_use written before pane redraws, continues through intermediate states, tool_result lands → pending=false → idle correctly; (4) 15.1-D cycling preserved (cooldown wins over override); (5) Issue 15 Stop-gate still defers at handler time, poller override redundant-safe; (6) Issue 8 P0 classifier unchanged. **Tests**: 7 pure-decision tests pinning full gate-ordering contract (cooldown > override > yield > stale > classify, hook-yield bypass, waiting-preservation, cooldown-wins-over-override) + 5 `latestTranscriptPath` helper tests (last-entry semantics, empty, malformed, non-string defensive). Server 348/348 (+12 new), typecheck clean. **Self-dogfood flag**: coder-16 correctly can't verify own session without server restart; deferred to Jose's canonical repro on his side post-restart — bash tool should show Working full duration including sleep windows.
- **Issue 15.1-H DISPATCH HISTORY**: dispatched P1 2026-04-19 from Jose's live smoke on 15.1-D. Repro: fresh post-restart Coder session, prompt `sleep 10 && echo done` → status Idle during full 10s sleep (BUG); comparison `for i in {1..10}; do echo; sleep 1; done` → Working (correct). Diff is pane visibility — loop has continuous spinner+verb, sleep blank. Three-state within same run: T+0s Bash card rendered + pane not yet active → Idle (BUG); T+1s `✻ Flibbertigibbeting` verb appears → Running command... (correct); T+10s tool_result arrives → Idle (correct). Middle state correct, initial + blank-pane states buggy. Likely cause: `hasPendingToolUseInTranscript` Stop-gate (Issue 15 `32107a6`) isn't authoritative in classifier pipeline — 15.1-D's allowlist-on-fallthrough (M1 `f41c74a`) and/or pane-idle branch override it during initial-tool_use window and blank-pane exec. Fix shape: elevate gate to authoritative — pending tool_use ALWAYS = Working regardless of pane state; pane = secondary signal for between-turn states only. §24.2 structured-signal-over-pane-pattern discipline. Additive (not reverting 15.1-D); <50 LOC isolated diff. 6/6 acceptance: (1) sleep-based Working full duration, (2) loop unchanged, (3) short tool Working first poll cycle, (4) 15.1-D cycling no regression, (5) Issue 15 originals pass, (6) Issue 8 P0 verb filter intact. Surfaced by Jose's live 15.1-D smoke.
- **Issue 15.1-F candidate (Jose-logged 2026-04-19, P2/P3, NOT dispatched)**: Pre-restart sessions don't benefit from post-restart classifier fixes. Symptom: after Commander restart with new classifier (e.g., 15.1-D `f41c74a`), sessions running PRE-restart continue exhibiting pre-fix behavior while POST-restart spawns show corrected behavior. Evidence 2026-04-19 ~1:30 PM post-15.1-D restart: M5 Coder (pre-restart) continues false-idle during active tool exec (chat pane shows tool cards, status bar says Idle); post-restart spawns clean. Likely cause: event subscriptions for pre-existing sessions aren't re-initialized against new server process — session still runs in tmux, produces events, classifier never sees them correctly. Fix directions: (a) server restart path re-initializes subscriptions for ALL pre-existing sessions not just new ones; (b) document that classifier fixes require session respawn to take effect; (c) warn user at restart time which sessions need respawn. Workaround: close + respawn pre-restart sessions. Priority: P2 if fix, P3 if documented-behavior. Real users hit rarely (only after Commander restarts during active sessions); Jose hits daily shipping classifier fixes. Jose to decide dispatch vs queue.
- Phase: **Issue 15.1-D + 15.1-C COMPLETE (Coder-16, 2026-04-19). HEADs `f41c74a` (15.1-D allowlist fix) + `2d4232c` (15.1-C compact refetch). **15.1-D root cause (NOT Stop-gate — that stays)**: M1 `applyActivityHints` at `2572749` had denylist-of-known-bads evidence exclusion; `'idle ❯ prompt visible'` wasn't in the denylist. Session ticks (cadence ~15s) bumped `last_activity_at` → M1 upgraded explicit-idle panes to working → cycling at tick cadence. Stop-gate ship at `32107a6` was correct but inadvertently surfaced the bug: pre-Stop-gate, 60s hook-yield after every Stop suppressed the poller so M1 rarely fired; post-Stop-gate, classifier runs more often → M1 fires on every tick → cycling. **Diagnosis**: coder-16 confirmed Stop-gate not regressing (acceptance matrix green), M1 is sole cause. **Fix (§24.2-aligned discipline correction)**: flip denylist → allowlist-on-fallthrough. `applyActivityHints` only upgrades `idle → working` when evidence is exactly `'fallthrough → idle'` — the one classifier branch meaning "couldn't determine, defaulted to idle" (legitimate M1 target: Plan UI, tool output pushing spinner off-capture, multi-line tool_result blocks). All other idle branches (IDLE_VERBS, past-tense, `idle ❯ prompt visible`, IDLE_INDICATORS pattern) fail closed. Pattern-match should always be allowlist, not denylist-of-known-bads — M1 original flipped the wrong way. **15.1-C (Option 2 compact_boundary refetch)**: `useSessionTick` grew `refetch()` re-GETing `/sessions/:id/tick`. ChatPage watches for new `compact_boundary` system records (ref-dedup'd per session) and fires both `refetchTick()` + useChat's existing `refetch()` (already resets contextTokens past last boundary). Post-compact ContextBar % refreshes within ~3s instead of waiting for next-turn activity. Single breaking change: `useSessionTick` return type `SessionTick | null` → `{tick, refetch}`; one caller (ChatPage) updated same commit. **Tests**: 15.1-D inverted 2 wrong-invariant tests (`idle ❯ prompt + fresh activity → STAYS idle`) + 1 new regression (full-statusline-chrome shape) + 4 prior tests green unchanged. Server 335→336 (+1 net). Client 234/234. Typecheck clean both. **Acceptance 6/6**: (1) idle stays idle no cycling; (2) Issue 15 originals still resolved (tool-exec upgrades via fallthrough target); (3) `/compact`/`/model`/`/effort`/`/status` hit explicit-idle branches, M1 hands-off, no cycles; (4) 15.1 A+B untouched; (5) 15 Stop-gate preserved; (6) 15 M1/M2 regression green. **§24.4 → Issue 15.3 PROMOTED to active (per Jose directive)**: 6th manifestation of pattern-match false-positive class confirmed (8 P0, 8.1, 9 P2, 15 M2, 15 M1 original denylist, 15.1-D). Canonical classifier architectural refactor promoted; awaiting separate dispatch, no implementation this rotation. **Live verification pending**: Jose reload + server restart (tsx watch unreliable per memory) → all cycling sessions settle stable Idle within one poll cycle; `/compact` label = "Compacting context..."; Thinking unmounts at compact_boundary; ContextBar % refreshes ~3s post-pill.**
- Last updated: 2026-04-19

- Phase: **Issue 15.1 Symptoms A+B COMPLETE; Symptom C deferred as 15.1-C (Coder-16, 2026-04-19). HEAD `1c4bf91`. `/compact` lifecycle event-delivery gap — three symptoms diagnosed, two shipped in one rotation. **Hypothesis verdict**: (a) subscribe-race RULED OUT (useChat.ts:141 subscribes unconditionally on sessionId change); (b) React state dedup RULED OUT for A+B; (c) watcher-bridge match race RULED OUT (claude_session_id bound by time /compact runs); **(d) compact-lifecycle event mapping gap CONFIRMED as Symptom A root cause** — `getComposingLabelIfApplicable` has no concept of compaction state, sticks on prior assistant text block; **(e) ContextBar tick-subscriber coverage CONFIRMED as Symptom C root cause** — tick only arrives on next turn activity, not during/post `/compact`, fix needs tick-emission OR client force-refetch; **(f) Thinking indicator unmount coupling CONFIRMED as Symptom B root cause** — `userJustSent` cleared only on new assistant message; `/compact` emits system `compact_boundary` record with no assistant reply. **Symptom A fix**: new pure `resolveActionLabel` helper in `contextBarAction.ts`. When session working AND `terminalHint === 'Compacting context...'`, that wins over jsonl-derived "Composing response..." label. Other terminal hints stay lower priority. Explicit literal-string match sourced from `parseTerminalHint`'s existing token allowlist — no new pattern-match class, §24-compliant. 7 new tests pin the precedence matrix. **Symptom B fix**: two new `userJustSent` clearing conditions in `ChatPage.tsx` — (1) new `compact_boundary` system message in post-send batch (specific fastest path); (2) `session.status → idle/stopped` (general backstop for /exit, /clear, /bashes, any future slash-command with no assistant reply). **Symptom C deferred as 15.1-C** — ContextBar reads `sessionTick.contextWindow.usedPercentage`; post-compact ticks only arrive on next turn activity so % stays stale 10-60s+. Three fix options documented: Option 1 statusline.mjs force-tick emission on compact-complete (most correct, needs Claude Code statusline lifecycle doc-check); **Option 2 client-side force `api.get('/sessions/:id')` on compact_boundary arrival** (recommended — smallest scope, trivially reversible, reuses endpoints); Option 3 derive ctx% from compact_boundary's preTokens + retention formula (prettier, requires knowing formula). **§24.4 stays deferred** — Symptom A fix is label-layering not classifier architecture. **Verification**: client 227→234 (+7 precedence tests), server 335/335 unchanged (Issue 15 Stop-gate + M1/M2 all green), typecheck clean both. Live verification pending: Jose needs to run `/compact` on a fresh session + confirm Thinking unmount + `Compacting context...` label during operation (client-only refresh for his tab).**
- Last updated: 2026-04-19

- Phase: **Issue 15 full-scope COMPLETE (Coder-16, 2026-04-19). HEAD `32107a6`. **Root cause (live-reproducer confirmed)**: Claude Code fires `Stop` hook every time the LLM yields to tool execution, NOT just at true turn end. Commander's Stop handler unconditionally wrote `status='idle'` + activated 60s hook-yield gate → poller locked out for 60s → M1 `applyActivityHints` (at `2572749`) never got to run. Live probe on fresh Commander Raw running `sleep 30 && echo done`: t=5s working (watcher JSONL bump), **t=10s idle (Stop fires while sleep still running)**, t=15-35s stays idle (poller skipped per yield), t=40s PostToolUse fires (bash done) but status never flipped back because only Stop/SessionStart write status. **H5 spawn-path ruled out**: code-level + live probe both show identical WS wiring for Commander-spawned vs Codeman-adopted paths. Split-view discriminator (Session A `Editing` healthy vs Session B `Plan 6/11` false-idle) is **tool-duration + plan-execution shape**, NOT spawn path. Fast in-turn Edit → Stop fires only at true turn end; long tool runs / multi-step plans → Stop fires mid-execution → false-idle. **Fix (your H4 signal #2 almost verbatim)**: `hook-event.routes.ts` Stop handler wraps idle-flip in `hasPendingToolUseInTranscript(transcriptPath)` guard — when a `tool_use` in JSONL tail has no matching `tool_result`, session is mid-tool, handler bumps `last_activity_at` + `last_hook_at` for proof-of-life and logs `[hook:Stop] … deferred — pending tool_use` instead of flipping idle. True turn-ends + transcript-less Stop events fall through to original Phase N.0 behavior unchanged. New `jsonl-parser.service.ts` helpers: `hasUnmatchedToolUseInLines` (pure, line-based) + `hasPendingToolUseInTranscript` (bounded 64KB file-tail read). Explicit-semantic gate on `tool_use.id ↔ tool_result.tool_use_id` JSONL contract, §24-compliant. **5 observations closed by one upstream fix**: (1) tool exec false-idle, (2) long gen false-idle (tool-mid-gen + M1 covers pure-gen), (3) Plan 6/11 + Idle (inter-step Stop firing), (4) Session A vs B split-view parity (generic handler, no sessionType branching), (5) H6 deferred as 15.1. **Verification**: server 300→335 (+13 new Stop-gate tests + 22 integration) — true-turn-end flips idle (Phase N.0 preserved); mid-turn Stop with pending tool_use does NOT flip idle; multi-tool mid-flight pairing; tool_result-without-use edge (bounded-tail tolerance); malformed lines; empty input; file-tail cases. Typecheck clean. All regression green: Issues 8 P0, 9 P2, 10, 15 M1, 15 M2. **8/8 acceptance matrix**: 1-5 closed via Stop-gate universality; 6-8 regression-clean. **Deferrals approved by Jose**: (a) **15.1** H6 missing-message + Obs 6 plan-widget-lag-post-completion same class (server→client event delivery gap; subscription code verified identical across spawn paths, couldn't repro in probe — needs fresh Jose repro); (b) **15.2** macOS `/tmp → /private/tmp` realpath gap at `resolveSessionCwd` (real project paths unaffected; extends §23.3 SSOT with `realpath()` resolution); (c) **15.4** impoverished Idle label post-handoff (UX refinement, distinguish `Idle — Just finished` vs first-prompt idle); (d) **§24.4** canonical pane-classifier architectural refactor STAYS deferred — Issue 15's root cause was event-semantics (Stop firing mid-turn), not pane-classifier brittleness; M1 pane+hints design intact; promotion threshold would be "classifier still insufficient after correct upstream signal" which Stop-gate removes.**
- Last updated: 2026-04-19

- Phase: **Issue 16 COMPLETE (Coder-16, 2026-04-19). HEAD `abc1e33`. Default model flipped from base `claude-opus-4-7` (200K) to `claude-opus-4-7[1m]` (1M variant) for all three sessionTypes (PM, Coder, Raw). Per Jose's M4 Coder observation: base Opus 4.7 is 200K; `[1m]` suffix unlocks 1M context. **Changes**: `packages/shared/src/constants/models.ts:21` `DEFAULT_MODEL` → `'claude-opus-4-7[1m]'`; `session.service.ts:374` + `:408` hardcoded strings folded into `DEFAULT_MODEL` import so future changes are one-line. **Extra scope bundled (minimal, flagged)**: `CreateSessionModal.tsx` had its own default state hardcoded to `'claude-opus-4-7'` — clicking Create without touching dropdown would POST base opus, which `opts.model || DEFAULT_MODEL` treats as explicit override. Modal now imports `DEFAULT_MODEL` in initial state + reset-on-open useEffect. Three-line extension; judged in-scope because acceptance #1 ("fresh PM session no override → `[1m]`") requires it. Dispatch's "dropdown options list out of scope" interpreted as MODEL_OPTIONS array only; default selection value treated in-scope. **Verification**: 2 new shared regression tests (DEFAULT_MODEL value pin + `getContextLimit(DEFAULT_MODEL) === 1_000_000`), shared 31/31, server 300/300 services + 22/22 integration, typecheck green across server+client+shared. **Issue 16.1 candidate FLAGGED NOT FIXED** per dispatch's addendum discipline: `MODEL_CONTEXT_LIMITS` at `models.ts:27-33` returns `1_000_000` for base IDs (`claude-opus-4-7`, `claude-opus-4-6`, `claude-sonnet-4-6`, `claude-opus-4-5-20251101`). Per Jose's observation base opus is 200K — table is wrong for 4 base IDs. `getContextLimit('claude-opus-4-7')` returns 1M incorrectly via the fallback branch after `[1m]` regex misses; contaminates ctx-percent UI for sessions whose stored model lacks the suffix. Existing test `models.test.ts:11-13` enshrines wrong behavior (`MODEL_CONTEXT_LIMITS['claude-opus-4-7'] === 1_000_000`); any 16.1 fix updates that assertion + decides migration story for rows still holding base IDs. Jose decides bundle vs defer.**
- Last updated: 2026-04-19

- Phase: **Issue 15 Manifestation 1 COMPLETE (Coder-16, 2026-04-19). HEAD `2572749`. **H4 confirmed, H1 ruled out — 5th manifestation of pattern-match class-of-bug family**. `classifyStatusFromPane(paneContent: string)` signature is pane-only by design; grep of caller sites confirmed `hook-event.routes.ts:447` bumps `last_activity_at` on every resolved hook + `watcher-bridge.ts:92` bumps on every JSONL append — so H1 clean, tool events DO bump activity. The failure: classifier never consults those bumps. Pure pane-reading with no structured-signal OR-combination. **Fix (additive, not replacing pane reading)**: helper `applyActivityHints(paneResult, hints)` wraps classifier output. Upgrades `idle → working` iff `last_activity_at` within 15s (`IDLE_UPGRADE_MS`). Never downgrades (leaves working/waiting/error/stopped untouched). Evidence-prefix exclusions block upgrades on `IDLE_VERBS` override + past-tense completion verbs (semantic explicit-idle — these verbs said "idle" for a reason, activity hint must NOT override). Poller wires in one line post-`detectStatusDetailed`. **Stop-hook safety** covered by existing 60s hook-yield pre-gate: classifier never runs within 60s of any hook, so Stop-idled row's `last_activity_at` is ≥60s stale by the time helper sees it — always outside 15s window. Natural composition with Phase U.1 infrastructure, no new temporal reasoning needed. **§24 5th bullet added** to PATTERN-MATCHING CONSTRAINT doc: "Structured data signals preferable to pane-text pattern matching when both available; pane reading is the fallback, not the default." Cites Issue 15 M1. **Scope deliberately narrow**: only timestamp signal shipped (Jose's listed signal #3). Signals #1 (pending `tool_use` awaiting matching `tool_result`) and #2 (plan-incomplete) NOT implemented — the cheapest signal covers the observed symptom, and the helper's `ClassifierHints` interface accepts hint extensions without a signature change. Future manifestations get those branches added incrementally. **Verification**: 10 new activity-detector.test.ts cases (fresh/stale activity, no-hint passthrough, working/waiting not-touched, IDLE_VERBS + past-tense exclusions, canonical tool-exec pane shape, user-typing branch). Server 290→300 (+10), integration 22/22 unchanged, typecheck clean.**
- Last updated: 2026-04-19

- Phase: **Issue 15 COMPLETE (Coder-16, 2026-04-19). HEAD `1c88914` (2 files, +64/-7). **H1 confirmed — 4th manifestation of pattern-match class-of-bug family** (after Issue 8 P0 / 8.1 / 9 P2). `classifyStatusFromPane` at line 394 pre-fix flipped status→idle whenever parsed `activity.elapsed > STALE_ELAPSED_SECONDS` (600s) **unconditionally** — including for live `-ing` verbs. Phase L designed this as belt-and-suspenders for "frozen footer" panes (`✻ Cooked 21261s` = Claude Code lingering on post-turn footer for hours) but ALSO fired on every legitimate 10+ minute generation. Jose's `✻ Gesticulating 10m+` fit exactly that window. **Smoking gun**: pre-existing test at `activity-detector.test.ts:234` pinned WRONG behavior as invariant — a test asserting idle on a live `-ing` verb is prima facie evidence the rule was inverted. H2 ruled out (Phase U.1 `STALE_ACTIVITY_MS=90s` tracks absence of hook events, not pane-elapsed; unchanged). H3 ruled out (spinner-glyph + verb morphology from Issue 8 P0 filter passes `-ing` through fine). **Fix**: gate stale-elapsed check on completion-verb morphology — live `-ing` verb at any elapsed trusted as working; only past-tense (`COMPLETION_VERBS` set OR `/ed$/` fallback) can combine with huge elapsed to produce idle flip. Past-tense branch at line 390 fires BEFORE stale-elapsed branch, so `Cooked 21261s` / `Schlepped 21261s` / any future `-ed` verb at huge elapsed still correctly routes to idle — Phase L invariant honest. **Why stuck-session protection not re-opened**: Phase U.1's `last_activity_at` guard in status-poller tracks ABSENCE of hook events (>90s with no inbound hook → force-idle regardless of pane state). Issue 15 removes a redundant pane-based guard that was generating false idles without adding protection the hook-absence guard doesn't already provide. **§24 compliance**: inline comment at match site cites constraint; top-of-file PATTERN-MATCHING CONSTRAINT block extended with new rule "Stale-elapsed heuristics MUST be gated on completion-verb morphology (/ed$/ or COMPLETION_VERBS set). A live `-ing` verb at any elapsed is a legitimate long generation." Constraint block now reads as design-principle roster rather than four orthogonal patches. **Test contract**: inverted one pre-existing wrong-invariant test + added two new — `✻ Ruminating… (620s · ↓ 120 tokens)` pre-fix pinned idle WRONG → post-fix working ✅; `✻ Gesticulating… (12m 45s · ↓ 890 tokens)` (Jose's repro) pre-fix idle BUG → post-fix working ✅; `✻ Schlepped (21261s)` Phase L invariant retained, still idle via past-tense branch. **5/5 acceptance**: long-gen UI stays working; legit idle transition within 10s (completion-verb branch untouched); actually-stuck eventually idle via Phase U.1 hook-absence guard; no pattern-match without §24 comment; unit test seeds long-gen pane returns working. **Verification**: server 288→290 (+2 new, +1 inverted = +2 net), typecheck clean. **§20 audit**: pre-fix had LL-L2 violation (deterministic invariant `elapsed > 10min = stale` was wrong for Claude Code 2.x). No LL-L1 silent early-returns introduced; evidence strings preserved for logging.**
- Last updated: 2026-04-19

- Phase: **Issue 13 COMPLETE (Coder-16, 2026-04-19). HEAD `b982b37` (6 files, +425/-14). Stopped-session retention service + default-filter UI to prevent accumulation. **Cascade FK audit (primary deliverable) — 4 gaps found**: `session_events` CASCADE ✅, `session_ticks` **NO FK** ⚠️ (hand-rolled in retention), `token_usage` CASCADE ✅, `cost_entries` **SET NULL** ⚠️ (hand-rolled), `file_watch_state` CASCADE ✅, `agent_relationships` CASCADE ✅, `skill_usage` **SET NULL** ⚠️ (hand-rolled), `notifications` **SET NULL** ⚠️ (hand-rolled), `hook_events` no FK (hand-rolled belt-and-suspenders), `phase_logs`/`projects` not session-scoped. 4 cascade gaps closed behaviorally via hand-rolled DELETE inside the same transaction as session DELETE; **schema migration to flip all to CASCADE deferred as future issue (13.1 candidate)**. **Retention service** (`retention.service.ts`): `purgeStopped({retentionDays, dryRun, backupPath})` pure deterministic; eligibility `status='stopped' AND parent_session_id IS NULL AND stopped_at IS NOT NULL AND stopped_at < now - N days`; teammate rows skipped (handled by existing `cleanupStaleTeammates` 7d cadence, no overlap); NULL `stopped_at` defensive-skipped (leaves for boot heal to investigate rather than purging on faith); hand-rolled DELETE for 5 cascade-gap tables inside same transaction as session DELETE; backup NDJSON written BEFORE transaction commits when not dry-run; idempotent (second run returns `deleted: 0`). **Boot wiring** in `index.ts` post-heal hook: runs `retentionService.runScheduled()` once then every 24h via `setInterval(...).unref()`. Env: `COMMANDER_RETENTION_DAYS` (default 30, NaN/negative fallback to 30); `COMMANDER_RETENTION_DRY_RUN=1` → log-only, no delete, no backup. One-line log per non-zero run: `[retention] retentionDays=30 wouldDelete=N deleted=N backup=/Users/.../purge-backup-<ISO>.jsonl`. **Route filter**: `GET /api/sessions?includeArchived=true|false` (default false); default excludes `status='stopped' AND stoppedAt older than 24h`; active rows always visible. `useSessions` hook exposes `{includeArchived, setIncludeArchived}`, re-fetches on toggle. SessionsPage shows "Show archived" / "Hide archived" pill next to Stopped collapsible header (per-mount state, does not persist). **Pagination/virtualization intentionally NOT added** — post-retention + default filter, list ceiling is active + recent-24h-stopped (active counts single-digit in practice); virtualization adds complexity for a problem already solved; revisit at >50 concurrent active sessions. **Safety rollout story**: (1) operator sets `COMMANDER_RETENTION_DRY_RUN=1`, restarts; (2) inspects `wouldDelete=N` log; (3) if N looks right, unset env + restart; (4) live run writes NDJSON backup BEFORE transaction commits; (5) rollback path NDJSON → SQL reapply via `sqlite3 ... < reapply.sql`. Defense-in-depth: dry-run AS primary gate + backup NDJSON AS secondary even on live runs. **Verification**: server 280→288 (+8 retention tests: dry-run, real purge with seeded 30d-boundary mix, teammate-exclusion, idempotent rerun, NULL stopped_at defensive skip, backup file write, backup-skip on dry-run, retentionDays=0 edge), client 227/227, typecheck clean. 8/8 acceptance verified via unit tests. §20 audit clean: deterministic invariants (LL-L2) every run from same eligibility predicate; no silent early-returns (LL-L1) — NULL stopped_at explicit + log-visible via "wouldDelete vs deleted" delta.**
- Last updated: 2026-04-19

### Deferred / queue
- **13.1 candidate**: schema migration to flip `session_ticks` (add FK), `cost_entries`, `skill_usage`, `notifications` from SET NULL → CASCADE. Behavioral hole closed via hand-roll; proper schema fix is cleaner long-term.

- Phase: **Issue 11 COMPLETE (Coder-16, 2026-04-19). HEAD `867f239` (3 files, +137/-2). **H2 CONFIRMED at data layer, not parser layer**. `sessions.transcript_paths` stored the SAME physical JSONL file twice under two case-variant claude-projects dir encodings: `/Users/.../.claude/projects/-Users-josemiguelbonilla-Desktop-Projects-JSTUDIO/5c2e28d9-...jsonl` AND `/Users/.../.claude/projects/-Users-josemiguelbonilla-Desktop-Projects-JStudio/5c2e28d9-...jsonl` — same UUID basename, different path case. macOS case-insensitive FS → same physical file. Commander listed both → chat endpoint concatenated both → every message doubled including bootstrap ack. **Cohort affected**: `6b2f29cf`, `d72d89ed`, `6201cdb6`, `61521c84`, `d5d7884f` (JSTUDIO case-mismatch cohort; all dedup correctly now even with legacy rows still carrying both paths). H1 ruled out (server log shows effort-set + bootstrap-injected ONCE per session). H3 ruled out (renderer grouping fine; chat endpoint just loaded the same file twice). **Why it happened**: Issue 10 fixed tmux spawn → sessions correctly land in user-typed path. But spawn-bind chokidar watcher watched the `JSTUDIO`-encoded dir (user input) while hook events arrived carrying `cwd: /Users/.../JStudio` (real macOS-resolved case) → `resolveOwner.cwd-exclusive` match → `appendTranscriptPath` called with `JStudio`-encoded path. Both land; exact-string `existing.includes(path)` dedup missed case-variant. This is §20's "silent equivalence collision" class — two strings that differ in bytes but reference the same entity. **Fix (two sites)**: (1) `sessionService.appendTranscriptPath` — dedup by `path.basename` UUID (canonical Claude-session identity), not exact path. Claude Code's JSONL filename is `<claude-session-uuid>.jsonl`; UUID is globally unique so basename IS canonical identity for "same file". Prevents future duplicate writes. (2) `resolveTranscripts` in `chat.routes.ts` — dedup at READ time too so pre-existing duplicate rows render as single file without needing DB migration. First-seen-basename wins. **Live verification at HEAD 867f239**: pre-existing dupe row `6201cdb6` chat endpoint `bootstrap ack count: 1` (was 2). Regression probe on coder-16 real multi-turn session: `total msgs: 7564, assistant: 227, text_blocks: 34, unique: 34` — zero legitimate messages collapsed. **5/5 acceptance live**: (1) Fresh Coder bootstrap ack once; (2) Fresh PM bootstrap ack once (identical dedup path); (3) default + custom cwd both fixed (dedup is JSONL-file-level, cwd-agnostic); (4) Raw unaffected (no bootstrap); (5) multi-turn chat NOT over-dedup'd. **+4 regression tests**: basename-UUID reject duplicate; accept legitimate different-uuid session rotation; exact-duplicate still rejected (legacy invariant preserved); resolveTranscriptsForTest pre-existing dupe normalizes at read. Server 276→280 (+4), typecheck clean. **Issue 10 + 11 complementarity**: Issue 10 fixed upstream (tmux spawn with wrong cwd); that fix brought the case-mismatch surface INTO PLAY because sessions now actually land in user-typed path. Pre-10 everything landed in `$HOME` (one canonical encoding, no dupe). Issue 11 closes the downstream consequence. Neither fix is complete without the other.**
- Last updated: 2026-04-19

- Phase: **Issue 10 COMPLETE (Coder-16, 2026-04-19). HEAD `aaca34d` (single-line fix ordering, 20/11). **H3 CONFIRMED — path normalization at a layer Issue 6 didn't cover**. Issue 6's `resolveSessionCwd` canonicalized cwd for the DB row + JSONL bind watcher, but was called AFTER tmux spawn. `tmuxService.createSession` runs `execFile('tmux', ['new-session', '-c', cwd])` — no shell. tmux silently falls back to `$HOME` on unexpanded `~`; `execFile` doesn't expand tildes. Direct test: `tmux new-session -d -s test -c '~/Desktop/Projects/JSTUDIO/'` → exit 0, pane `#{pane_current_path}=/Users/josemiguelbonilla` (HOME), no error. Claude launched in wrong cwd → bootstrap-referenced files unreadable → JSONL watcher saw no writes. H1 ruled out (server log shows `effort set` + `bootstrap injected` for every custom-cwd session including failing ones — inject is sessionType-keyed, cwd-independent). H2 ruled out (`waitForClaudeReady` returned true on all custom-cwd sessions, 12s timeout never exhausted). **Visible symptom deception**: "no /effort, no bootstrap in pane" was artifact of Claude rendering multi-line sendKeys as `[Pasted text #N]` bracketed-paste chunks in input buffer — unrelated presentation issue. Actual failure downstream: Claude in `$HOME` instead of project dir. **Fix (single-line reorder in `session.service.ts`)**: lift `resolveSessionCwd(opts.projectPath, null)` call BEFORE `tmuxService.createSession(tmuxName, inputCwd ?? undefined)`. Later `resolveSessionCwd(opts.projectPath, paneCwd)` stays for absent-input case where paneCwd fallback needed. No code duplication, no new helpers — Issue 6's helper now active at both tmux spawn + bind layers. **Live verification at HEAD aaca34d**: pre-fix session `d5340260` projectPath `~/Desktop/Projects/JSTUDIO/` → DB canonicalized, tmux pane cwd `$HOME`, spawn-bind 30s timeout; post-fix session `6201cdb6` identical input → tmux pane cwd `/Users/…/Desktop/Projects/JStudio` (macOS case-insensitive FS), `[sessions] effort set to medium (coder)` + `coder bootstrap injected` + `[spawn-bind] bound in 14.9s`, pane tail `⏺ Ready. Standing by for your first message`. **5/5 acceptance**: (1) Coder + default cwd `d1595cb0`; (2) Coder + custom cwd `6201cdb6`; (3) PM + custom cwd `3c853802` jlp-family-office absolute path; (4) Raw + custom cwd `61521c84` effort only no bootstrap; (5) Raw + default cwd `1301154d` unchanged. **§24 application**: no new pattern-matching introduced (`waitForClaudeReady`'s existing `/❯/ || /? for shortcuts/i` check targets pane chrome not chat content, §24 N/A). **§20.LL-L1 early-return audit**: `bindClaudeSessionFromJsonl(id, cwd)` early-return on `!cwd` is documented + no longer silent (Issue 6 guarantees canonical cwd non-null for typed sessions); `waitForClaudeReady` 12s timeout logs visible warn; `tmuxService.sendKeys` errors caught with per-turn warn. No additional silent offenders. **Deferred-verification gap closed**: M1 pre-dated Issue 6's canonicalization helper. Once Issue 6 landed at binding layer, path had TWO canonicalization sites and only one was covered — this fix closes that gap. **Verification**: server 276/276 pass, typecheck clean, live repro on exact Jose-case (tilde + trailing slash) goes broken→correct, all 5 acceptance sessions spawn+inject+bind correctly. **§23.3 update prompt drafted** (canonical invariants: Coder/PM inject regardless of cwd; Raw effort-only no bootstrap; cwd spec affects tmux pane cwd only not injection pipeline; `resolveSessionCwd` is SSOT before tmux spawn — `execFile` doesn't expand `~`, tmux silently falls back to `$HOME` otherwise; all call sites must agree on canonical form or downstream binding breaks silently) — awaiting PM dispatch to OS custodian.**
- Last updated: 2026-04-19

- Phase: **Issue 7.1 COMPLETE (Coder-16, 2026-04-19). HEAD `abbbbb3` (+620/-30, 16 files). Four typed renderers for high-frequency shapes falling through to UnmappedEventChip pre-7.1. Issue 5 fallback + denylist architecture preserved (additive only). **JSONL shape inventory scoped to 7.1 targets**: `edited_text_file` → `filename` + `snippet` (post-edit numbered view, NO old/new pair); `skill_listing` → `content` (newline `- name: desc`) + `skillCount` + `isInitial`; `invoked_skills` → `skills: [{name,path,content}]`; `queued_command` → `prompt` + `commandMode`. **Mapping table added rows**: `attachment: edited_text_file` → FileEditNote (upgraded from Issue 7 system_note); `attachment: skill_listing` → SessionSkillsChip (removed from DROP_ATTACHMENT_TYPES); `attachment: invoked_skills` → InvokedSkillChip (removed from DROP_ATTACHMENT_TYPES); `attachment: queued_command` → QueuedCommandChip. Everything outside 7.1 (deferred_tools_delta, command_permissions, hook_success, date_change, directory) still falls through. **Decision log**: (1) **FileEditNote chose path (b) lighter renderer, NOT (a) RenderEditContent reuse** — `RenderEditContent` in ToolCallBlock expects `old_string` + `new_string` from Edit tool_use input; `edited_text_file` JSONL carries only `snippet` (post-edit snapshot, no diff pair); lifting would require synthesizing pre-edit state we don't have. Same interaction pattern as Issue 7 FileAttachmentChip/CompactFileRefChip. Red-accent on failure not implemented: `edited_text_file` records aren't emitted on failure (successful saves emit, failed saves emit no record — Edit tool_use's `is_error` covers the failure surface). (2) **InvokedSkillChip chose inline-at-invocation-point, NOT grouped-at-top** — chronology is the signal. Sequence `tool_use(Read foo.ts) → invoked_skills(/ui-expert) → tool_use(Edit foo.tsx)` tells Jose what triggered the invocation. Grouping at top destroys ordering. Chip format (not card) keeps visual weight lighter than tool_use. **Live DOM audit across 4 sessions**: `jlp-coder` 0 edits/10 skills-listing/2 invoked/2 queued/22 unmapped; `coder-16` 2/0/0/6/21; `jlp-pm` 0/0/0/0/0 (control); `JStudioCommand-pm` 0/1/0/16/27. All four renderers fire live; unmapped counts preserve Issue 5 fallback. **Smoke 10/10** with two qualifiers: #2 (failed-edit red chrome) N/A because `edited_text_file` records don't emit on failure — Edit tool_use `is_error` path owns that surface; #9 jlp-coder tail had 0 FileEditNote instances but coder-16 same-feature session rendered 2, so audit passes on the feature not the specific session. **7.2 candidates flagged (NOT silently added)**: QueuedCommandChip "Dispatched" transition (JSONL pairs nothing to dispatched version — would require text-matching next user/assistant turn, correlation fragility, defer until Jose weighs value); `deferred_tools_delta` stays in DROP_ATTACHMENT_TYPES; `command_permissions` modal handles; `date_change` low signal; `directory` single occurrence; syntax highlighting for FileEditNote.snippet + ToolResult file bodies still pending bundle-cost review. **Verification**: server 272→276 (+4 parser tests), client 227/227 unchanged (components purely render, behavior covered at parser emit boundary), typecheck green both, Vite prod build succeeds, `pnpm audit --prod` clean. Screenshots at `/tmp/issue7.1-shots/` per-session, 1440×1200 full-page.**
- Last updated: 2026-04-19

- Phase: **Issue 9 Part 1 finish + Issue 9.1 COMPLETE after scope-recovery (Coder-16, 2026-04-18). HEAD `400591b` (un-revert of `ed27e7f`) restores Brain redesign from `b402ebc` after PM misrouted 9.1's "Out of scope" list as retracting Jose's prior direct Brain redesign ask. Clean `git revert` — no conflict with intervening `81557ec` (tokens/s removal) since different elements. **Final ContextBar target state achieved**: Brain icon `size={16}` pink `#EC4899` (teammate-palette SSOT), no progress bar, percentage color-coded via `bandForPercentage`+`bandColor` from `utils/contextBands.ts` (same function HeaderStatsWidget uses; `<50 green / <80 yellow / <90 orange / ≥90 red`), `$` cost dropped (`2ece84d`), mid-bar verb+elapsed dropped (`e346f5d`), tokens/s dropped (`81557ec`), session-total `tokens` kept, `Context:` text label kept removed (original Part 1). **Live DOM probe across 4 sessions at HEAD 400591b**: 9c4218ed at 15% → green `rgb(14,124,123)`; 6073138e at 0% → green; 03763f99 at 66% → yellow `rgb(245,158,11)`; coder-16 at 70% → yellow. Constant invariants verified uniformly: Brain `width=16`, color `rgb(236,72,153)=#EC4899`, zero progress bar inside indicator, zero tokens/s / `$` / mid-bar chips, session-total tokens present. Orange 90-94% + red ≥95% pinned by proxy (same function HeaderStatsWidget uses). **Verification**: client 227/227, server 272/272 (Part 2 classifier + Part 3 LocalCommandNote untouched), typecheck green, Vite build succeeds, `pnpm audit --prod` clean. Screenshots at `/tmp/issue9-shots/9.1-final-*.png`. **5h/7d in HeaderStatsWidget still untouched** — awaiting Jose's explicit ruling on that target. **Meta-lesson saved** as feedback memory `feedback_direct_instruction_outranks_interpretation.md`: Jose's direct in-conversation asks aren't retracted by silence in later formal dispatches; only explicit retract language cancels them. Out-of-scope in a formal dispatch means "don't add more to this ticket," not "rescind unrelated direct asks." Saved to prevent repeat.**
- Last updated: 2026-04-18

- Phase: **Issue 9 Part 1 finish + Issue 9.1 mostly COMPLETE pending tokens/s confirm (Coder-16, 2026-04-18). HEAD `b402ebc` (ContextBar Brain redesign) + `2ece84d` (drop `$` cost) + `e346f5d` (drop mid-bar verb+elapsed). Finish-pass shipped the earlier bundled scope before my 9.1 retraction message landed — net state is superset of what 9.1 required. **Progress bar removed**: `w-16 sm:w-20 h-1.5 rounded-full` track + inner fill + `barColor` constant all retired; DOM probe confirms zero `.rounded-full` inside context-indicator wrapper. **Brain icon redesign**: `size={13}`→`size={16}`, color `#EC4899` (SSOT-reused from `utils/teammateColors.ts:14` "pink" teammate palette entry — not a new hex). `aria-label` + `title` retained from Part 1 text-to-icon swap. **Percentage color-coding**: imports `bandForPercentage` + `bandColor` from `utils/contextBands.ts` — same pair HeaderStatsWidget uses via parallel `bandForBudget`, identical band math (<50 green / <80 yellow / <90 orange / ≥90 red). Live probe verified: 0% → green `rgb(14,124,123)` (`var(--color-accent)`); 66% → yellow `rgb(245,158,11)` (`var(--color-idle)`); 100% → red `rgb(239,68,68)` (`#EF4444`). Orange 90-94% pinned by proxy (same code path as HeaderStatsWidget's `#F97316`). **Missed-addenda shipped earlier**: `$ cost` drop at `2ece84d`, mid-bar verb+elapsed drop at `e346f5d`. **5h/7d left untouched** — grep confirms they live only in `HeaderStatsWidget.tsx:104,116`; awaiting Jose's ruling on whether to remove from HeaderStatsWidget (ContextBar was never the target). **Still pending confirm**: 9.1 Item 2 tokens-per-second / throughput indicator — PHASE_REPORT didn't explicitly address; either covered by `$ cost` drop (if same element) or still rendering (needs separate removal). Single-line confirm requested from coder-16. **Verification**: client 227/227 unchanged, typecheck + Vite build + `pnpm audit --prod` clean. Live DOM probe across 3 sessions at distinct tiers — all assertions pass. Screenshots at `/tmp/issue9-shots/*.png` (1440×80 ContextBar crops). No regression to Part 2 `/status` classifier or Part 3 LocalCommandNote.**
- Last updated: 2026-04-18

- Phase: **Issue 9 PARTIAL — P0 + Part 3 SHIPPED; Part 1 scope partially missed (Coder-16, 2026-04-18). HEAD `d1239a3` (10 files, +480/-159). **Part 2 P0 /status false-approval SHIPPED (H1 confirmed)**: approval classifier's match pattern was too broad at two fallback sites in `server/src/routes/session.routes.ts:313-340` — (a) `if (prompts.length === 0 && (bottomRaw.includes('Esc to cancel') || bottomRaw.includes('Enter to confirm'))) push confirm prompt` and (b) `\?\s*$` / `\d+\)` regex fallback. Claude Code prints `Esc to cancel` as viewer-modal footer for `/status`, `/compact` preview, `/login`, `/model`, file viewer — none actionable approvals. Second fallback matched tabular numbered rows and any prose ending with `?`. Neither did work a real approval's explicit branch didn't already satisfy. **Live repro captured pre-fix**: pane `Esc to cancel` + `prompts: [{type:'confirm',message:'Waiting for confirmation',context:'Status Config Usage Stats...'}]`. **Fix**: extracted detector to `server/src/services/prompt-detector.service.ts` (176 LOC, 9 tests), kept ONLY explicit-token branches: (1) trust literal `trust this folder` / `Yes, I trust`; (2) numbered-choice `❯ N.` (dot not paren) on focused option + 2+ siblings; (3) Allow/Allow always/Deny requiring both `Allow` AND (`Deny` or `allow always`); (4) `(y/n)` literal parenthetical. Kill-switches preserved (`⏵⏵ bypass permissions on`, teammate-waiting). Fallbacks removed with inline rationale. Live post-fix same pane: `prompts: []`. Stuck-state-after-No sub-fix NOT needed — the stuck state was downstream of the false-fire; with modal never appearing user never clicks No. H2 ruled out (local_command events aren't fed to classifier). H3 ruled out (no re-entry loop; classifier re-matches each poll until pane changes — fix at match pattern kills signal). **Part 3 local_command renderer SHIPPED**: `ContentBlock` gains `{type:'local_command', stream:'stdout'|'stderr', text:string}`; parser branches on `subtype==='local_command'` and extracts `<local-command-stdout>…</…>` / `<local-command-stderr>…</…>` tagged body; shape drift → `debug_unmapped` (Issue 5 preserved). `LocalCommandNote.tsx`: muted inline chip with Terminal icon (stdout) / AlertCircle (stderr error-red chrome); first-line summary collapsed, click-to-expand monospace box (matches Issue 7 FileAttachmentChip pattern). ChatThread SystemNote routes local_command through it; AssistantMessage.renderBlock exhaustive switch skips (flows through system records not assistant blocks). **Part 1 ContextBar polish PARTIAL**: (a) `Context:` text → Brain lucide icon SHIPPED with `aria-label` + `title` "Context window usage: N%" for a11y; (b) 5h%/7d% removal — coder-16 grepped + confirmed those live ONLY in `HeaderStatsWidget.tsx:104,116`, NOT in ContextBar — **spec clarification needed**: Jose likely meant remove from HeaderStatsWidget, not ContextBar; flagged, NOT shipped; (c) `$` cost drop — **MISSED** (addendum sent after dispatch, classic `feedback_bundle_all_scope_upfront` pattern); (d) mid-bar verb+total-elapsed (`✻ Gesticulating 10m 18s`) drop — **MISSED** (same addendum-miss pattern). **PATTERN-MATCHING CONSTRAINT doc** at top of `agent-status.service.ts` updated with Issue 9 P2 citation + approval-prompt constraint bullet (explicit option tokens only; not tabular shape, trailing `?`, or `Esc to cancel` alone). Third manifestation of class-of-bug documented. **9.x candidates flagged (NOT silently added)**: `/compact` preview has same `Esc to cancel` footer pattern — covered by Part 2 fix by construction; `/status` pane briefly reports `working` via Phase U.1 hook-yield cadence, resolves on next poll ~5s, pure visual blip. **Verification**: server 263→272 (+9 prompt-detector), client 227/227 unchanged, typecheck + Vite build + `pnpm audit --prod` clean. **Pending**: Part 1 addenda (`$` cost drop + mid-bar verb+elapsed drop) re-dispatch + Jose's ruling on 5h/7d location (ContextBar was empty, real target is HeaderStatsWidget).**
- Last updated: 2026-04-18

- Phase: **Issue 8.1 COMPLETE (Coder-16, 2026-04-18). HEAD `089aea5` (8 files, +477/-75). Defense-in-depth on pattern-matching pipeline following Issue 8 P0 root-cause framework. **Architectural rule locked**: all pattern-matching against external tool output (Claude Code stdout, tmux pane, JSONL fields) must be constrained by semantic shape (verb morphology, line position, explicit tokens), never just character presence. **Part 1 parseTerminalHint hardening**: pre-8.1 `client/src/hooks/usePromptDetection.ts:59` was blob-scan `/✻|✶|Thinking|Cogitat|…/i.test(joined)` — shadowed-by-luck today because ContextBar prefers `jsonlAction` over `terminalHint` when messages exist. Extracted to pure `client/src/utils/parseTerminalHint.ts` with three constraints: (1) live-spinner glyph requires line-start anchor `/^\s*[✻✶]\s/` per tail line (not blob scan); (2) verb requires standalone token + `-ing`/`-ed` morphology with word boundaries (prevents `rethinking` substring); (3) atypical-verb allowlist (`Idle`) mirrors server-side Issue 8 P0 filter; nesting hint also gated on live-signal presence (paren or spinner-line-in-tail), not prose mention. **Paths not tightened (judgment call)**: syntactic tool-call matches (`Bash(`, `Read(`) stay blob — unambiguous + rare in prose, tightening adds complexity without safety benefit. Compacting/Summariz match stays broad — Claude Code only prints those during real compaction. **Part 2 ContextBar fresh-activity gate**: new `client/src/utils/contextBarAction.ts` with `STALE_ACTIVITY_MS = 30_000` (mirrors server Phase U.1 force-idle threshold) + pure `isActivityStale(lastActivityAt, now)` helper. Missing/zero/NaN `lastActivityAt` → NOT stale (defensive; brand-new sessions without first activity event must not suppress label). Threshold is strict `>` (exactly-at-threshold → fresh). **Suppression scope narrow**: ONLY the `"Composing response..."` label — tool-based labels (`Running command...`, `Reading X...`) pass through because their staleness story is handled by tool_result append timing. `userJustSent` bypasses the gate so optimistic "Composing" still renders in sub-second window before first server activity event lands. ContextBar wiring: `const suppressComposing = rawJsonlAction?.label === 'Composing response...' && !userJustSent && isActivityStale(lastActivityAt); const jsonlAction = suppressComposing ? null : rawJsonlAction;` ChatPage threads `session.lastActivityAt` through ContextBar props. **Part 3 constraint doc**: `agent-status.service.ts` top-of-file PATTERN-MATCHING CONSTRAINT block enumerating verb morphology, line-start anchoring, activity-event verification, SPINNER_CHARS vs SPINNER_GLYPHS distinction (narrow-heuristic vs broad-then-filtered). Closing line: "When adding a new pattern match: state its semantic shape constraint inline. Not doing so is how Issue 8 P0 was possible." **Verification**: server 263/263 unchanged (all Issue 8 P0 tests still green), client 202→227 (+25: 11 parseTerminalHint + 14 contextBarAction), typecheck green both, Vite prod build succeeds, `pnpm audit --prod` clean. **5/5 smoke PASS**: reply-with-word-"thinking"-mid-line no-leak; real `✻ Cogitating (3s)` fires; streaming+fresh activity shows Composing; status=working+stale-activity suppresses Composing; all Issue 8 P0 tests still green. **No 8.1-adjacent surprises** — ContextBar `getActionInfo` tool-label paths are driven by tool_use blocks (not free text), not candidates for same gate.**
- Last updated: 2026-04-18

- Phase: **Issue 8 COMPLETE (Coder-16, 2026-04-18). HEADs `c040d9a` (P0 Part 3) + `f90a210` (Parts 1/2/4). **Part 3 P0 root cause**: `⏺` is Claude Code v2.x's REPLY-BULLET glyph, NOT a live-spinner glyph. It was in `SPINNER_CHARS` in `agent-status.service.ts`, so every Claude reply line (`⏺ Opus 4.7 (claude-opus-4-7), effort level: medium.`) satisfied `hasActiveInTail` + `detectActivity` extracted garbage verbs from reply content ("Opus", "Model", "Processing"). Session.status stuck at `working` because the "active indicator" persisted until scroll pushed `⏺ Opus…` out of pane tail — not a periodic re-entry (H2 ruled out), not a Phase U.1 defense gap (H3 partial: 60s hook-yield kept extending window on steady stream of valid-looking-but-bogus activity events). **Fix**: (1) removed `⏺` from `SPINNER_CHARS` — real live-spinner glyphs are Braille frames + `✻`/`✶`; (2) verb-sanity filter in `detectActivity` requiring verb to end in `-ing` (Thinking, Ruminating, Cogitating, Composing, Standing, Waiting) OR `-ed` (Cooked, Brewed, Crunched, Finished, Composed, Ruminated) OR be atypical "Idle"; loop `continue`s past non-matching lines so real spinner above garbage line still wins. `⏺` remains in `SPINNER_GLYPHS` (detectActivity regex charset) because genuine completion/thinking lines also start with it (`⏺ Cogitating (3s · 1234 tokens)`, `⏺ Cooked (21261s)`) — verb filter keeps those while rejecting reply content. **Live verification on Jose's exact repro pane**: pre-fix `status=working, verb=Opus, raw=⏺ Opus 4.7 (claude-opus-4-7), effort level: medium.`; post-fix `status=idle, activity=None, lastActivityAt age=2s`. >5min idle-hold confirmed via scheduled wakeup (70+s wait, no spontaneous re-entry in server logs). **Part 1 model expansion**: CLI accepts `--model <alias|full-id>[1m]`. CreateSessionModal dropdown went 4→7 entries with `[1M context]` suffix disambiguation: Opus 4.7 / Opus 4.7 [1M] / Sonnet 4.6 / Sonnet 4.6 [1M] / Haiku 4.5 / Opus 4.6 (legacy) / Opus 4.6 [1M] (legacy). Sort: Opus > Sonnet > Haiku, newest first in family, standard before [1m]. `MODEL_CONTEXT_LIMITS.getContextLimit` already detects `[1m]` — downstream token counters correct. **Part 2 effort expansion**: `claude --help` shows `--effort` accepts `low, medium, high, xhigh, max`. `@commander/shared` `EFFORT_LEVELS`: `['medium','high','xhigh','max']` → `['low','medium','high','xhigh','max']` (full ladder low→max). ContextBar imports shared (drops local 3-entry). `normalizeEffort`/`normalizeEffortLevel` preserve `low`/`medium` as-is rather than silently upgrading. **Part 4 refresh parity**: refresh was 44×44 per Phase P.2 C4 hit-area rule, Stop was `px-2 py-0.5 text-xs` (~30×18px). Refresh now inherits Stop's exact classes: `flex items-center justify-center gap-1 px-2 py-0.5 rounded text-xs`. Phase P.2 C4 a11y test updated to exclude refresh from 44×44 enforcement with Issue 8 Part 4 comment. **Tests**: +4 Part 3 P3 tests (Claude-reply-doesn't-classify-working, Model/first-word-no-verb, non-verb-rejected, genuine-Cogitating/Ruminating/Cooked-still-detected), updated Phase J.1 IDLE test to match filter. Server 259→263 (+4), client 203→202 (-1 retired refresh-44×44). Typecheck green, Vite build succeeds, audit clean. Screenshots at `/tmp/issue8-shots/`. **Issue 8.1 candidates (flagged, NOT silently added)**: (a) `parseTerminalHint` in `client/src/hooks/usePromptDetection.ts` matches `/✻|✶|Thinking|Cogitat/i` against raw pane at 2s cadence and emits "Thinking deeply..." hint — same class of bug could re-emerge via client-side hint path; not triggering now because `jsonlAction` wins over `terminalHint` when messages exist, but fragile; (b) ContextBar `getActionInfo` returns "Composing response..." when last assistant block is `text` — correct during streaming, but if `session.status` flips to `working` for any other reason (stale hook) stale "Composing" label reappears; defense-in-depth would require UI to also verify active activity event exists before showing "Composing response...".**
- Last updated: 2026-04-18

- Phase: **Issue 7 COMPLETE (Coder-16, 2026-04-18). HEAD `d4eb1eb`. Typed renderers for tool_result + attachment shapes reducing UnmappedEventChip volume. Issue 5 denylist architecture stays (purely additive). **JSONL inventory** (~13k records): tool_result string 5155 + list:text 232 (covered by string path), is_error 151 (P0 auto-expand), list:image 3 (defer), list:tool_reference 33 (defer); task_reminder 401 (P1); file 31 (P1); compact_file_reference 19 (P2); hook_success 3185 stays denylist. **Option A kept** (not B) for tool_use/tool_result pairing — existing `ToolCallBlock` + `buildToolResultMap` already merges them into one compound card with tool_use streaming first and tool_result attaching via the map when it lands; auto-expand on error is the new addition. **Shared**: ContentBlock union +3 variants (`inline_reminder`, `file_attachment`, `compact_file_ref`); event-policy.ts comment documents typed-renderer mapping (denylist sets unchanged — new renderers ADD, don't flip). **Server** (`jsonl-parser.service.ts`): task_reminder → `inline_reminder` (was system_note); file → `file_attachment` with parsed `filename/displayPath/numLines/totalLines/content` (nested `attachment.content.file.*` handled, string→number coercion for lineCount); compact_file_reference → `compact_file_ref` (filename/displayPath only, content absent per Claude Code shape). +3 parser tests. **Client** (new components): `InlineReminderNote` (dashed muted footnote, Info icon, italic, role=note aria-label="System reminder"); `FileAttachmentChip` (type-derived lucide icon per extension, numLines/totalLines summary, click-to-expand monospace content preview max 320px, disabled when no content); `CompactFileRefChip` (dashed border, History+FileText icons, italic "(referenced from earlier context)", no content by design). `ChatThread.tsx` SystemNote routes lead blocks through new renderers. `AssistantMessage.tsx` gets new variants + tool_result case returns null (exhaustive; comment explains flow through user-message `buildToolResultMap` pairing). `ToolCallBlock.tsx` enhanced: `isError` → auto-expanded initial state; Bash truncation 40 lines (was 1000 chars) with "N more lines" counter; Generic 40 lines OR 3000 chars with counter labeling which cap tripped; red-accented background + border on error in both Bash + Generic content paths. **Live smoke** at HEAD d4eb1eb: `/chat/coder@jlp-patrimonio` rendered 8 FileAttachmentChips + 2 CompactFileRefChips + 23 remaining UnmappedEventChips (confirms Issue 5 regression guard #10 + retroactive re-render #9). coder-16 self-session: 174 tool_result pairings visible + 25 unmapped (all Issue 7.1 candidates, not gaps). Screenshots at `/tmp/issue7-shots/*.png`. **10-item smoke matrix 9/10 ✅**. **#3 (syntax highlighting) partial ⚠️**: grepped client — `highlight.js` only bundled via `@react-pdf/renderer` (PDF gen path), NOT on tool_result render path. Per brief's "use whatever's in use" = nothing. Read tool output falls through to generic monospace plain. Add prism/shiki in separate task if Jose wants highlighting. **Issue 7.1 candidates (flagged, NOT silently added)**: `skill_listing` 40, `edited_text_file` 37 (upgrade existing system_note to typed diff link), `queued_command` 35, `deferred_tools_delta` 29 (plumbing, likely stay denylist), `command_permissions` 17 (modal already handles, stay denylist), `invoked_skills` 7, `date_change` 5, `directory` 1. **Verification**: server 257→259 (+2), client 203/203 unchanged, typecheck green, Vite build succeeds, `pnpm audit --prod` clean.**
- Last updated: 2026-04-18

- Phase: **Phase W.2 COMPLETE pending Jose's interactive browser smoke + self-dogfood #13 (Coder-16, 2026-04-18). HEADs `d3e8f88` (core) + `024dd57` (migration fix). Reshapes Phase W from "pin icons + implicit pairing" → "sessions independent + explicit Split View button + per-pane X + termination cascade". **UI**: Pin/PinOff icons fully removed from `SessionTab` (imports stripped, `usePaneState` no longer imported by TopCommandBar); tabs back to name+status+teammate count. New `SplitViewButton` top-right of chat header — dropdown lists all running sessions EXCEPT current+already-in-split, each with session name + status dot + PM/Coder/Raw type badge, disabled+tooltip `"Close a pane first."` when 2 panes open; also rendered (disabled) in right pane's header in 2-pane so cap is visible. Per-pane `X` (close) button removes pane from view but does NOT terminate session; left-pane-close promotes right→left via `navigate(rightId)` + closeRight; right-pane-close returns to single-pane. **Schema change**: `pinned_session_ids: string[]` → `right_pane_session_id: string | null`. Migration in `pane-state-migration.ts` handles 6 paths: already-w2, already-w2-stripped (hybrid Phase W leftovers canonicalized), w-to-w2 (drop `left`), legacy-to-w2 (from `split-state.<pm>`), legacy-all-empty, no-op. Jose's live DB was a hybrid — `rightSessionId` coexisted with stale `left`/`right`; post-boot row is canonical `{rightSessionId, dividerRatio, focusedSessionId}` only. Boot log: `[pane-state] migration: outcome=already-w2-stripped migrated=0 dropped=0 legacyKeysSwept=0`. **Termination cascade fix**: `usePaneState` session-gone listener fires on WS session-end events from any code path (user action / server cleanup / crash detect); cleans `rightSessionId` → promote right to sole pane OR `leftSessionId` (= URL) → navigate to promoted right OR `/sessions` fallback; clears `focused_session_id` pointing at gone session; emits in-pane termination toast (single-instance inline motion div, not stack — carve shared layer if W.3 needs concurrent). Stale right-pane IDs silently normalized on boot replay (smoke #10). **Coupling audit (13 entries, all documented with decision)**: zero forced pair-up at creation/termination layer; `findAdoptablePmAtCwd` + `adoptPmIntoTeam` + `upsertTeammateSession` + `agent_relationships` table all KEPT (team-config reconciliation identity + Agent Teams parent/child hierarchy — process layer per safety addendum); `agentRole==='pm'/'coder'` UI conditions KEPT (pure annotation, no enforcement); `SplitChatLayout.tsx` (679 LOC) DELETED + `MAX_TEAMMATES` + `readLegacySplit` + `auto-split-on-spawn` pref + `primeSplitState` helper in TeammateRow + P.2 H2 a11y test + App.tsx lazy route import all stripped. grep confirms zero `SplitChatLayout` refs remain in client/src. **Verification**: server 252→257 (+5 migration), client 203→203 (+1 reducer test = 17 total, some deletions net to same), typecheck green, Vite prod build succeeds, `pnpm audit --prod` clean. Headless chromium geometry at 1440×900: 1-pane 852×1200 input y=881 SplitViewButton enabled; 2-pane both 852px input y=881, both X buttons present, SplitViewButton disabled; 4 screenshots at `/tmp/phase-w2-shots/*.png`. All 12 smoke matrix items evidenced at reducer/geometry level. **Pending Jose's interactive browser smoke**: #3 (dropdown→select animation), #6 (different-session-from-list clears split), #7/#8 (terminate-cascade toast + nav), #11 (tooltip visual), and **#13 self-dogfood** (open JStudioCommand PM → Split View → coder-16 appears → select → right pane opens → X → coder-16 pane leaves view but stays alive in tmux → reopen → history intact). Coder-16 DID run #13 autonomously via headless chromium against its own session row (HEAD `024dd57`): 2-pane dogfood → X right pane → `pane-state.rightSessionId` cleared, coder-16 `status != stopped`, `stoppedAt=null`, `tmuxSession=%57` unchanged, `claude_session_id=bbe98809-...` unchanged. 7/7 assertions PASS. Direct proof of life: the PHASE_REPORT was sent from the same process that was X'd. Safety-addendum compliance verified: `closeRight` + `session-gone` listener are pure read/state ops (no DELETE, no kill, no tmux signal); process-layer coupling (agent_relationships, parent_session_id, tmux_session, pane_id, upsertTeammateSession, findAdoptablePmAtCwd, adoptPmIntoTeam, team-config reconcile, purgeTeamSession-only-deletes-relationship-rows, tmux-pane discovery) all untouched. Screenshots `13a-dogfood-dual.png` + `13b-dogfood-after-close.png` at `/tmp/phase-w2-shots/`. Full matrix 13/13 at logic/geometry layer. Interactive eyes-verify (dropdown→select, toast-on-terminate animation) still pending Jose's live hands but non-blocking.**
- Last updated: 2026-04-18

- Phase: **Phase W P0 hotfix (Coder-16, 2026-04-18). HEAD `541b493`. Flex-height propagation bug in `PaneContainer.tsx` root layout: 2-pane outer used `flex-1` inside DashboardLayout `<main>` (block-level, `flex-1 overflow-hidden`, NOT `flex flex-col`), so `flex-1` resolved to 0 basis → both pane columns collapsed, chat stream + input bar painted with 0 available height, overflowed past viewport with no scrollbar to expose them. 1-pane returned `<Pane />` directly with only `h-full` applied inside block `<main>` — worked at some viewport heights, collapsed at others. Pre-W SplitChatLayout had the correct pattern (`flex h-full w-full min-h-0` outer, `h-full min-h-0 overflow-hidden` per column) — not ported to PaneContainer initially. Fix: outer `flex h-full w-full min-h-0` + per-column `h-full min-h-0 overflow-hidden` + 1-pane wrapped in same-class container div. Pane child layout was already correct; only PaneContainer root was wrong. `min-h-0` on flex child is the key — without it flexbox refuses to shrink below intrinsic content height and scroll-inside-pane becomes impossible. Headless chromium geometry evidence at HEAD 541b493: 1440×900 2-pane both panes 852px tall with scrollHeight 55665/26986 vs clientHeight 735/691 (overflow inside pane not past it), textareas at y=881 visible within 900px viewport. 1100×600 narrow resize: panes 552px, textareas at y=581 within 600px. 1-pane unpin: 1200px wide, scroll + input correct, identical to pre-W. 4/5 smoke cases verified at geometry layer; #3 (divider drag live motion) needs Jose's mouse. Reducer/hooks/migration/drawer/ChatPage/DashboardLayout all untouched.**
- Last updated: 2026-04-18

- Phase: **Phase W COMPLETE pending Jose's browser smoke (Coder-16, 2026-04-18). HEADs `597fc14` (shared+red) → `1e52816` (migration green) → `79cc6d5` (client). Generalized PM/Coder special-cased split into generic pin-a-second-session mechanism with max-2 panes + per-pane Cmd+J terminal drawer. **Server:** `@commander/shared` adds `PaneState`+`SessionUi` types + `PANE_STATE_KEY` + `sessionUiKey()` + clamp constants. `migrateLegacySplitState()` one-shot boot migration (tie-breaks on most-recently-updated `split-state.<pmId>`, clamps percent to [0.3,0.7], idempotent, handles malformed shapes, always deletes `split-state.*` after). Wired into `index.ts` after `getDb()`. **Persistence choice**: reused existing `preferences` table + `/api/preferences/:key` routes + `preference:changed` WS broadcast — no new table, no new routes, zero schema migration risk, free cross-tab convergence via existing mechanism. Keys: `pane-state` (global) + `session-ui.<sessionId>` (per-session). **Client:** `paneStateReducer` pure helper encodes 4 invariants (right-without-left impossible, focus bound to pinned set, dividerRatio clamped, max-2 pin cap). `usePaneState()` + `useSessionUi(id)` typed facades over `usePreference<T>`. `PaneContainer` supersedes `SplitChatLayout` at `/chat/:sessionId` — 1-or-2-pane layout, draggable divider (320px min per pane), focus-on-click with accent-border. `TerminalDrawer` per-pane wraps existing `SessionTerminalPreview`, read-only, 180ms slide up, drag-resize [200px, 70%*pane] on top edge (ns-resize). Cmd+J/Ctrl+J handler on focused Pane (not drawer so it can open closed) with INPUT/TEXTAREA keydown-target skip so search boxes don't hijack. Pin/PinOff lucide icon on `SessionTab` in `TopCommandBar` writes pane-state via pin/unpin reducer actions; disabled + tooltip when 2 others pinned; `aria-pressed` reflects state. **Routing decision (PM-approved)**: (a) URL owns `left`, pin owns `right` + persistence. `/chat/C` sets pane-state.left=C while preserving right=B. Fallback when nothing pinned: URL `:sessionId` renders as transient single pane (documented in `PaneContainer.tsx` header). Unpin-left-while-right-pinned: reducer promotes right→left (no orphan right state ever persists). Focus-on-unpinned-click: silently ignored. **Migration generalization**: old split-view inner tabs + Minimize2 + auto-split toggle + "Direct Mode · {role}" badge all removed with SplitChatLayout route swap; no PM/Coder hardcoded assumptions needed preserving (old split was already role-agnostic internally). Teammates API still exists for other callers — PaneContainer doesn't use it; right-pane is explicit user pin only. **SplitChatLayout.tsx (679 LOC) kept on disk as rollback reference — NOT referenced from any route.** Next rotation should delete cleanly after Jose confirms browser smoke (+ unused `MAX_TEAMMATES` constant, teammate-split-flow fields on `usePreference` callers). Verification: server 245→252 (+7 migration) all pass; client 187→203 (+16 reducer invariants) all pass; typecheck server+client green; Vite prod build succeeds; `pnpm audit --prod` clean. Boot smoke on `79cc6d5`: migration fired, Jose's `split-state.6073138e-…` → `pane-state {left: 6073138e-…, right: coder-16@jstudio-commander, dividerRatio: 0.55, focusedSessionId: coder-16@jstudio-commander}`, zero `split-state.*` rows remain. REST round-trip PUT/GET identical. **Can't verify without Jose's browser**: pin/unpin visual (smoke 1, 2, 7, 8 covered at reducer level only), divider-drag-across-reload visual (3 verified REST layer only), per-pane input scoping (4 untouched from SplitChatLayout's `sessionIdOverride` pattern), Cmd+J per focused pane (5, 6 handler mounted per-pane but needs keyboard), reload restoration visual (9 verified preferences layer only), legacy migration visual parity (10 data layer verified, not visual). Mobile layout untouched per desktop-only spec. No perf issues at 2 panes — SessionTerminalPreview IntersectionObserver-gates polling so two drawers don't double cost unless both on-screen.**
- Last updated: 2026-04-18

### Pending queue
- **Phase W browser smoke (Jose)** — 10 interactive smoke tests from spec need live browser verification before phase is fully closed. Tests 1-10 listed in dispatch brief; reducer/REST/build/typecheck are all green.
- **SplitChatLayout cleanup** — 679 LOC orphaned after Phase W; delete file + unused `MAX_TEAMMATES` + teammate-split-flow `usePreference` callers. One-commit sweep after browser smoke confirms Phase W stable.
- **Phase U.1 deferred items** — (a) `sessions.force_idled_at` column + 60s poller cooldown, (b) oscillation telemetry with 15s-window idle↔working flip detection + per-session 60s dedup. Designs in `audits/PHASE_U1_INVESTIGATION.md` + CODER_BRAIN Phase U.1 section.

- Phase: **Issue 6 COMPLETE (Coder-16, 2026-04-18). HEAD `7e23886` green (+ `19fd7db` red). Root cause: H3-variant + downstream resolver dependency. `sessionService.createSession` DID call `bindClaudeSessionFromJsonl` for every session (line 442), BUT the helper early-returns on null cwd (line 238). Raw-Coder sessions created via the modal without user-typed projectPath passed `opts.projectPath=undefined` → null cwd → watcher never armed → no spawn-bind; simultaneously row inserted with `project_path=null`, which broke `resolveOwner.cwd-exclusive` deterministic strategy too. Both deterministic paths failed silently; heuristic auto-learn correctly excluded by Phase U Patch 1; transcript_paths stayed empty. DB audit confirmed: every BOUND row had populated project_path (OvaGas-ERP, approval-test, ovagas coder 2); every UNBOUND row had empty project_path (render-Testing, Render Test, Testing Raw, noble-owl). Raw Coder row had literal `~/Desktop/…/` with tilde + trailing slash that mismatched hook cwd — secondary normalization bug. **Fix**: new `tmuxService.resolvePaneCwd(target)` queries `display-message -p -t <target> '#{pane_current_path}'`; new pure `resolveSessionCwd(userInput, paneCwd)` expands `~`, trims trailing `/`, returns canonical absolute path, falls back to pane cwd when userInput empty; `createSession` resolves cwd right after `resolveFirstPaneId`, feeds it + `opts.projectPath` through helper, uses result for BOTH `bindClaudeSessionFromJsonl(id, canonicalCwd)` AND `upsertSession({projectPath: canonicalCwd})`. **Architecture call (per brief)**: bind explicitly at spawn, not via post-hoc auto-learn. Patch 1's heuristic-exclusion is still correct; the real invariant is every Commander-spawned row MUST have a real cwd column so (a) deterministic spawn-bind watcher fires AND (b) deterministic `cwd-exclusive` strategy wins when watcher misses. Both paths now load-bearing + deterministic — no heuristic auto-learn needed for raw sessions. Smoke (fresh POST /api/sessions `{sessionType:"raw"}` with no projectPath): response `projectPath` = tmux pane cwd auto-resolved; after 10s `claude_session_id` populated + `transcript_paths` length 1; server logs `[spawn-bind] bound in 7.0s` + `[hook-event] matched via cwd-exclusive` (both paths fired); GET /api/chat/<id> returns `{messages: [...]}` with real content. Tests: server 239→245 (+6 resolveSessionCwd), client 187→187, typecheck green, audit clean. Ruled out: H4 (Patch 1 exclusion not at fault — zero strategies matched on null project_path), H1 (helper never armed, timeout path irrelevant), H2 (chokidar never opened).**
- Last updated: 2026-04-18

### Pending queue
- **Issue 3** — Terminal surfacing UX. Jose's pick: Option 1 (Cmd+J keyboard drawer overlaid on ChatPage). Awaiting CTO spec detail (scroll region, input surface, theme/a11y) before dispatch.
- **Phase U.1 deferred items** — (a) `sessions.force_idled_at` column + 60s poller cooldown, (b) oscillation telemetry with 15s-window idle↔working flip detection + per-session 60s dedup. Designs in `audits/PHASE_U1_INVESTIGATION.md` + CODER_BRAIN Phase U.1 section.

- Phase: **Direct-Mode bug sweep COMPLETE (2026-04-18). HEAD `3bc6c0e` (Issue 5 architecture-lock refactor after initial `6b14889`). Four shipped Issues (1, 2, 4, 5) closing chat-layer v1 UX gaps Jose surfaced via live repro on the Command Center. Session spawn-breakdown doc at `docs/SESSION_SPAWN_BREAKDOWN.md` (`5bebe53`). Issue 1 (`6abf4bd`) — permission-modal blocker: `SessionTerminalPreview.tsx` missing `type: 'choice'` branch for v2.1.114 numbered-option prompts; new shared `client/src/utils/promptActions.ts` helper is single source of truth for `choice`/`trust`/`permission`/`confirm` button mapping; `onSendKeys` callback removed (unused after refactor); `PermissionPrompt.tsx` + `SessionTerminalPreview.tsx` both consume the same helper. Hypotheses 1 (classifier) + 3 (z-index) ruled out via live pane capture + `/output` endpoint returning `type:'choice'`. Issue 2 (`6abf4bd` same commit) — chat-fidelity: `jsonl-parser.service.ts` was discriminating on `record.subtype` (never populated for attachments) instead of `record.attachment.type`; restored `edited_text_file`, added `task_reminder → system_note`; noise types (`hook_success`, `stop_hook_summary`, `turn_duration`) explicitly kept dropped. Issue 4 (`79c6c31`) — token-counter value fix: `LiveActivityRow.tsx:22` preferred `tick.contextWindow.totalInputTokens` (cumulative) over `activity.tokens` (pane turn-delta); ~20x inflation (44,146 vs 2.2k on same turn). Per-Jose option (a): fixed the value not the label; activity.tokens owns the "tokens" segment, no silent fallback to cumulative (segment omitted instead). Issue 5 (`6b14889` initial; `3bc6c0e` architecture-lock refactor per Jose's follow-up) — policy inversion: chat render pipeline flipped from allowlist-default-drop to denylist-default-render with visible debug placeholder for unknowns. SSOT at `packages/shared/src/constants/event-policy.ts` — `DROP_RECORD_TYPES` + `DROP_SYSTEM_SUBTYPES` + `DROP_ATTACHMENT_TYPES` as ReadonlySet with per-entry rationale; shared type gains `debug_unmapped` ContentBlock + `UnmappedKind` discriminator (record_type | system_subtype | attachment_type | assistant_block). `jsonl-parser.service.ts` imports shared denylists + `unmapped()` helper emits `debug_unmapped` blocks on all four unknown-shape branches. New `UnmappedEventChip.tsx` component (muted dashed chip, AlertCircle icon, click-to-expand raw payload) — not a reused component. `AssistantMessage.renderBlock` debug_unmapped case + default both route through chip; `ChatThread.renderFragment` default same. Real-JSONL inventory table in PHASE_REPORT. 7887bd4 red commit had 4 failing assertions pinning silent-drop contract + 3 regression guards. Totals: server 232→239 (+7) unit + 22 integration unchanged + client 187 unchanged = **448**. Typecheck green. `pnpm audit --prod` clean. Server restart + smoke verified on coder-16 chat endpoint. **Important Issue 5 finding**: the empty `render-Testing` pane Jose reported was NOT caused by the parser — the DB row had `transcript_paths=[]` + blank `claude_session_id`, so the chat endpoint correctly returned `awaitingFirstTurn: true` and SessionTerminalPreview fell through. Parser hardening is still a correct prophylactic but the live-repro bug is a binding-path bug in `bindClaudeSessionFromJsonl` or the raw-session spawn path, surfaced as Issue 6 candidate for next rotation.**
- Last updated: 2026-04-18

- Phase: **Phase V COMPLETE + Phase S.1 chat stale-gate fix (2026-04-18). HEAD `7a82518`. Phase V red (`829e712`) shipped by coder-16 pre-compact; green (`646cc75`) applied by PM as hotfix per Delegation Law precedent — cpus() delta-based CPU sampling + `HeaderStatsWidget` chip subtitles stripped (`CPU %` / `Mem %` only; bytes retained in Mem tooltip; CPU tooltip rewritten "Instantaneous CPU usage across N cores"). Server tests 214→227 (+13 Phase V suite). Follow-on `7a82518` fixes Phase S.1 audit finding: ChatPage's `isSessionWorking` now ORs `userJustSent` with `(session.status === 'working' AND !heartbeatStale)`, so a stale `working` flag after Phase U force-idle no longer keeps `liveComposing` sticky-ref populated. Observed live on coder-16: UI rendered "Composing response ⏺ Stale 4037s" against server idle + clean DB + idle pane. Tree clean; typecheck green.**
- Last updated: 2026-04-18
- Phase: **Phase U.1 COMPLETE with deferred items (Coder-16 investigation + PM hotfix, 2026-04-17). HEAD `6cdad1d`. Root-cause Fix 2 shipped and verified live. Oscillation killed at source; zero flip events observed in 30s+ of post-restart runtime; `coder@ovagas-r2` flipped from 3hr-stuck `working` to correct `idle`; all other sessions stable. Coder-16 did the investigation (commit `8c74b73` — `audits/PHASE_U1_INVESTIGATION.md` + `PHASE_U1_PANE_59_CAPTURE.txt`, 123-line captured fixture with branch-by-branch trace) then paused at 87% context pre-compact. PM applied the 4-line exclusion fix + 3 regression tests as hotfix `6cdad1d` while coder was pre-compact: new `STATUSLINE_CHROME_MARKERS` regex list (`/ctx\s*\d+%/i`, `/\b\d+[hd]\s+\d+%/i`, `/⏵⏵\s+bypass permissions on/i`, `/⏵⏵\s+accept edits on/i`), `isStatuslineChrome` predicate, filter applied in both `hasActiveInTail` AND the "active content after ❯" branch. Does NOT touch `ACTIVE_INDICATORS` itself — real signals ("47% complete", "Reading 123", "Writing foo", "Editing N files", "Running N tests") unaffected. Captured-pane fixture test plus chrome-matches-chrome test plus chrome-doesn't-match-real-activity test. Server 197 → 200 (+3), typecheck green, `pnpm audit --prod` clean. **Deferred to future session as backlog** (Jose close-out 2026-04-17): (a) Fix 1 — `sessions.force_idled_at` column + 60s poller cooldown gate ordered BEFORE `last_hook_at` yield, as defense-in-depth against future false-positive classes; (b) Fix 3 — oscillation telemetry with 15s-window idle↔working flip detection and per-session 60s dedup. Both designs are in `audits/PHASE_U1_INVESTIGATION.md` + CODER_BRAIN Phase U.1 section; a future rotation can resume from that design doc directly.**
- Last updated: 2026-04-17
- Phase: **Phase U complete (Coder-16, one rotation). HEAD `f81f87f`. CTO follow-up on the stuck-state class — three surgical patches + 24 tests. 4 commits on top of `684706e` (team-lead's analytics snapshot). (`12969cb`) Patch 1 — auto-learn `claude_session_id` on deterministic-strategy match: exported `DETERMINISTIC_STRATEGIES` set (`claudeSessionId` / `transcriptUUID` / `sessionId-as-row` / `cwd-exclusive`), exported `maybeAutoLearnClaudeSessionId(matchId, strategy, transcriptPath)` helper issues `UPDATE sessions SET claude_session_id = ? WHERE id = ? AND claude_session_id IS NULL` (conditional — races with concurrent hooks can't overwrite); heuristic rotation strategies (pm-cwd-rotation / coder-team-rotation) deliberately excluded to prevent poisoning a row with a wrong UUID; call site in `processHook` emits `session:updated` on backfill. Closes Mode 1 (PM-spawned teammates never binding) retroactively via first deterministic-strategy hook. (`7968b0a`) Patch 2 — 90s stale-activity force-idle guard: `STALE_ACTIVITY_MS = 90_000` threshold in status-poller. Ordering is (1) Phase T `last_hook_at` yield — recent hook wins, (2) stale-activity force-idle — flip `working`→`idle` when `now - last_activity_at > 90s`, (3) pane classifier. Narrow transition only (working→idle); idle/waiting/stopped untouched. UPDATE guarded with `AND status = 'working'` to avoid racing a concurrent Stop hook. Emits `session:status` with evidence `stale-activity-force-idle (Ns)`, records in flip-history ring buffer, updates lastKnownStatus + workingSince + idleSince caches, `continue`s past classifier. Closes Mode 2 (stale pane-regex hits after 60s hook-yield expires — "Composing response 1100s"). (`3586e8c`) Patch 3 — SessionCard `⚠ stale?` visibility chip: pure predicate `shouldShowStaleBadge(rawStatus, secondsAgo)` exported; renders when server says `working` AND heartbeat >60s (threshold above the 30s band that drives `applyStaleOverride`); small orange text-[10px] Montserrat chip with tooltip `"Status shows working but no activity in X seconds — may be a routing gap."`. Pure display — no state mutation, no server write. Surfaces any residual drift so regressions in Patches 1/2 can't hide. (`f81f87f`) Patch 4 — tests: `phase-u-auto-learn.test.ts` (10 — DETERMINISTIC_STRATEGIES set size pin, 4 deterministic writes, 2 heuristic non-writes, already-populated no-op, non-UUID no-op, unknown session no-op), `phase-u-force-idle.test.ts` (9 — constants pin 60s/90s, 91s force-idle, 89s/90s boundary no-change, idle/waiting/stopped scope guards, hook-yield-wins-over-force-idle @ 1s, 60s-exact boundary into force-idle, integration seeds working+stale row and runs statusPollerService.start() once then asserts DB flip + `session:status` bus emit with stale-activity-force-idle evidence), `staleBadge.test.ts` (5 — threshold constant pin, working+>60s → true, exactly-60s → false, ≤60s → false, non-working × every secondsAgo → always false). Also exports `HOOK_YIELD_MS` and `STALE_ACTIVITY_MS` from status-poller so tests pin by import, not duplicate values. Totals: server 178→197 (+19) unit + 22 integration unchanged + client 174→179 (+5) = **398 (up from 374 pre-U)**. Typecheck green server + client. `pnpm audit --prod` clean (no new deps). Flagged for team-lead: server restart + live before/after hookMatchStats.skipped + OvaGas coder@ovagas-r2 routing-gap capture — per dispatch note, team-lead handles that as the real verification.**
- Last updated: 2026-04-17
- Phase: **Phase T complete (Coder-16, one rotation). HEAD `3419d70`. CTO-revised prescription for the stuck-state / dropped-hook class. 3 commits. (`4474756`) Patch 2 REVISION — `sessions.last_hook_at INTEGER NOT NULL DEFAULT 0` column migration (idempotent). `sessionService.bumpLastHookAt` single write surface. Hook routes (Stop / SessionStart / SessionEnd / transcript-match branch) all bump it alongside `bumpLastActivity`. Status-poller SELECT reads `last_hook_at`; yields for `HOOK_YIELD_MS = 60_000` whenever `Date.now() - last_hook_at < 60s`, no status predicate, no `lastKnownStatus` cache dance. Replaces Phase N.0 Patch 2 (`cfd1e65`) 10s/status=idle gate — that was too narrow because append-only transcript writes never bumped updated_at. Old `ms_since_update` strftime calc dropped from poller SELECT. (`cc67146`) Patch 0 — `bindClaudeSessionFromJsonl` helper in session.service.ts: chokidar watch on `~/.claude/projects/<encoded-cwd>/` (depth:0, persistent:false, ignoreInitial:true), kicked off BEFORE the `claude` keystroke in `createSession` so the first add event cannot race past us. First UUID-named `.jsonl` add → upsert `claude_session_id` + appendTranscriptPath + `session:updated` WS. 30s safety timeout with warn + fall-through to resolveOwner cascade. No cwd → skip. Dropped the `ignored` predicate (chokidar v4 runs it against the watch dir itself and would ignore the whole directory); add-handler filename-regex filter suffices. Result: `resolveOwner`'s primary strategy (`claude_session_id === body.session_id`) becomes an O(1) hit from the very first hook, so CTO snapshot §7 drop-path collapses from 5-strategy-cascade-guessing to primary-strategy-always-hits for Commander-spawned sessions. (`3419d70`) Patch 5 tests — `poller-yield.test.ts` rewritten to new contract (5 tests: recent yield status-agnostic, stale past 60s no-yield, never-hooked default-0 no-yield, status-agnostic on stale, 60s-edge boundary off-by-one guard). New `spawn-jsonl-bind.test.ts` (5 tests: encode contract, chokidar add wiring via `ready` event on macOS fsevents, UUID regex rejects non-Claude JSONLs, safety timeout fires, `ignoreInitial` hides pre-existing files). New `integration/phase-t-hook-yield.test.ts` (3 tests E2E-light via Fastify inject: Stop hook writes last_hook_at → poller yields 60s, status-agnostic on idle, unknown-session no row fabrication). `phase-p3-hardening.test.ts` H2 split anchor tightened to `appendTranscriptPath(sessionId: string, path: string)` so the new spawn-bind helper's sessionService.appendTranscriptPath call doesn't shadow the method definition. Totals: server 173 → 178 (+5) unit + 19 → 22 (+3) integration + client 174 unchanged = **374 (up from 366 pre-T)**. Typecheck green. Don't touch Phase T's shipped commits — they're the primary fix for the hook-drop class.**
- Last updated: 2026-04-17
- Phase: **Phase S.1 complete (Coder-16, one rotation). HEAD `b54fab6`. 4 commits fixing two live bugs Jose reported + adding guard rails + tests. Patch 1+3 (`4efa704`): `tmuxService.resolveFirstPaneId(name)` helper + `createSession` resolves first pane id immediately after `tmux new-session` and stores THAT in `tmux_session` (not the session name); all subsequent sendKeys in createSession target the resolved pane id; `index.ts` orphan-discovery does the same; `sendKeys` guard DEV-throws on non-pane non-retired targets, PROD-warns+proceeds, `retired:*` silent no-op. Post-sessions integration test assertion updated `/^jsc-/` → `/^%\d+$/`. Patch 2 (`6684a90`): `sessionService.healLegacySessionNameTmuxTargets` runs at boot AFTER `healOrphanedTeamSessions` and BEFORE `healCrossSessionTeammates`; resolves legacy `jsc-*` rows into pane ids OR marks stopped when tmux session gone; emits session:updated; idempotent. Patch 4 (`1bceafc`): `ContextBar` reads `sessionTick.contextWindow.usedPercentage` as the authoritative ctx% source; falls back to token/contextLimit ratio only when no tick; extracted `resolveContextPercent(tick, tokens, contextLimit)` as pure exported helper. Server `session:status` WS event extras have NO context field — verified, no server changes needed. Patch 5 (`b54fab6`): +3 new test files (`tmux-guard.test.ts` — guard DEV throw/PROD warn/retired no-op/pane bypass; `resolveFirstPaneId` idempotency + null on gone + first-pane-despite-active via real tmux split-and-select; pane-id send-keys routing regression with two-pane split + capture-pane assertion; `session-name-heal.test.ts` mirrors heal SQL against in-memory DB with injectable resolver across 5 scenarios; `ContextBar.test.ts` locks tick-first invariant in 7 cases). Test totals: server 159→173 (+14) unit + 19 integration + client 167→174 (+7) = **366 (up from 345 pre-S.1)**. `pnpm audit --prod` clean (no new deps). Typecheck green server + client. **Patch 6 (wider display audit doc `audits/PHASE_S1_DISPLAY_AUDIT.md`) DEFERRED** — compact pressure hit 86% before it was started. Next rotation should dispatch it as a focused task with Explore agent to produce the per-component WS/REST matrix.**
- Last updated: 2026-04-17
- Phase: **Phase S + Q + R complete (Coder-16, one rotation). HEAD `bc0295b`. 11 commits across 3 phases. Phase S (3): (`64fa7af`) loopback-gated `/api/upload/:sessionId` multipart endpoint (@fastify/multipart stream-mode; per-chunk size enforcement 5MB img / 10MB file; filename allowlist `[A-Za-z0-9._-]`; ISO timestamp prefix on collision; 500MB per-session quota warn; `removeSessionUploads` wired into `purgeTeamSession` + `cleanupStaleTeammates`). (`b5e4f0a`) client drag/drop/paste/picker UI: `useAttachments` hook (module-level cache, drag counter, File/Blob validation); AttachmentChipRow with image thumbnails (URL.createObjectURL + revoke); drop-overlay gated on `DataTransfer.types.includes('Files')`. (`3473852`) send flow: FormData + fetch upload with PIN header; `buildInjectedPayload(paths, message)` emits `@path1 @path2 message` single-line (not bracketed paste); tmux inject via existing sendKeys. Phase Q (3): (`29cfd98`) pre-compact service + state machine (idle/warned/compacting/emergency), WARN=85, EMERGENCY=95, RESET=40, HYSTERESIS_FLOOR=75; `auto_compact_enabled INTEGER NOT NULL DEFAULT 1` column with one-shot PM/lead-pm heal to 0 in same migration; `READY_TO_COMPACT` case-sensitive substring match scanned against user-role text blocks by watcher-bridge. (`1964bb6`) `GET /api/pre-compact/status` + `PATCH /api/pre-compact/sessions/:id` + WS `pre-compact:state-changed` broadcast on sessions topic. (`8d87545`) SessionCard pre-compact indicator (⚠ Waiting for state save / ⟳ Compacting; idle renders nothing) + Zap/ZapOff toggle. Phase R (5): (`4ace394`) drop legacy `transcript_path` column with one-shot consolidation into `transcript_paths` JSON array BEFORE drop (supersedes index.ts boot-heal); removed `transcriptPath` from UpsertSessionInput + SESSION_COL_MAP. (`52aa8a1`) drop `file_watch_state.last_line_count` (divergent on truncate-rewrite; nothing read it). (`7ac6a90`) `getAggregateRateLimits` JOINs sessions + filters `status != 'stopped'` so stopped-session rate-limit ticks don't pollute the aggregate. (`b439a9d`) `HOOK_QUEUE_MAX = 20` returns 503 + `Retry-After: 1` when saturated; `_setHookQueueDepthForTests(n)` helper deterministically saturates via stalled promises (Promise.all concurrency was racy). (`bc0295b`) poller `ms_since_update` uses `(strftime('%s','now') - strftime('%s', updated_at)) * 1000` — integer-equivalent to julianday on UTC, NULL on unparseable; boot-time LIKE-scan of `sessions.updated_at` warns on non-UTC drift shapes (AM/PM, `+HH:MM`, `-HH:MM`, trailing Z). Tests: +22 Phase S (6 server integration + 16 client) + 19 Phase Q (13 server + 6 client) + 9 Phase R (7 server unit + 2 integration) = 50 new. Totals: server 159 + 19 integration + client 167 + 5 E2E = **350 total**. `pnpm audit --prod` clean (0 CVEs). Typecheck green. Known live bugs for Phase S.1: (1) OvaGas PM row tmux_session stored session NAME not pane ID; (2) context-bar drift across SessionCard/ContextBar/LiveActivityRow — some UI surfaces still use old pane-regex ctx% instead of `useSessionTick`.**
- Last updated: 2026-04-17
- Phase: **Phase P.4 complete (Coder-16). E2E buildout. HEAD `8b62bb3`. 6 commits: (`933e498`) repair core-flows — hardened sessions-page smoke (waits for /api/sessions truth, asserts either empty-state card OR grid renders; deleted 3 soft-skip REST probes that now live in integration tests). (`c11237e`) session-creation E2E — UI click → modal → POST /api/sessions → real tmux spawn → DB + WS → SessionCard render; afterEach DELETE cleanup. (`e6f7177`) hook-stop-flip E2E — seed session via REST, fire Stop hook at session UUID (sessionId-as-row strategy), assert REST flip to idle within 5s + StatusBadge "Idle" via `.filter({ has: heading })`. (`92fd593`) statusline-tick E2E — POST tick with distinctive values, deterministic GET /api/sessions/:id/tick round-trip + loose widget chip pct assertion (other sessions tick every 300ms so exact-value won't stick). (`2d86e5c`) Fastify integration harness — 3 specs / 11 tests via `app.inject()` (remoteAddress defaults to 127.0.0.1, passes P.1 loopback gate); adds `COMMANDER_DATA_DIR` env override to `config.ts` for tmpdir isolation; `test:integration` uses `--test-force-exit` (13s → 1s by escaping createSession's `waitForClaudeReady` timer chain). (`8b62bb3`) Playwright config hardening — `workers: 1` explicit, `client/e2e/README.md` with env var matrix + side-effect disclosure, no webServer block (ambient `pnpm dev` races auto-spawn). Test counts: server 137 unit (unchanged) + 11 integration + client 145 unit (unchanged) + 5 Playwright = **298 total**. Closes audit C-1 (no session-creation E2E), C-2 (no hook-event integration incl. non-loopback 403), H-1 (no Fastify route integration), H-2 (no WS emit contract tests — `waitForBusEvent` helper asserts session:created/status/deleted shapes). DEFERRED per team-lead: chat-ingestion spec, heartbeat-live-ago spec, React component-render tests, file-watcher chain tests, CI setup.**
- Last updated: 2026-04-17
- Phase: **Phase P.3 complete (Coder-16). QA hardening + terminal removal + hook rewrite. (H1 `f77ad2b`) `readJsonlOrigin` switched to bounded `openSync`/`readSync`/`closeSync` with `statSync`-sized buffer cap — no more full-file slurp on 50 MB JSONLs. (H2 `4a80ec0`) `appendTranscriptPath` no longer bumps `updated_at` — append-only mutations don't represent status change and were invalidating the N.0 P2 poller 10s yield window. (H3 `119ba4e`) `hooks/commander-hook.sh` replaced by pure-Node `hooks/commander-hook.js` — no more `/usr/bin/python3` dependence + silent `{"event":"unknown"}` fallback. `install-hooks.mjs` deploys + migrates legacy `.sh` matchers → `.js` in place. (H4+L2+L3 `1f4235f`) REMOVED Terminal page + xterm.js/node-pty deps — product decision, half-built preview out, proper rebuild is a future sprint. MobileNav swapped Terminal tab for Analytics. pnpm audit still clean post-removal. (M2 `06275ea`) `healOrphanedTeamSessions` now runs at top of every team-config reconcile, not only boot — mid-session team-dir deletion cleaned without restart. (L1 `73b1669`) client `[ws] Connected` log now `import.meta.env.DEV`-gated. (Follow-up `390a7ed`) commander-hook resolves port via `COMMANDER_HOOK_URL` → `~/.jstudio-commander/config.json` → 11002 default (Jose's dev on :3002 was silently dropping events); `~/.claude/hooks/package.json { type: module }` deployed so Node loads .js as ESM natively. Server 126→137 (+11), client 147→145 (net; TerminalTabs test removed). Typecheck clean; `pnpm audit --prod` → No known vulnerabilities.**
- Last updated: 2026-04-17
- Phase: **Phase P.2 complete (Coder-16). UI/UX criticals C1-C4 + H2 — 5 commits. (C1 `c4ab101`) global `:focus-visible` ring in `index.css` (accent color, 2px, +2px offset) + `outline-none` re-enablement. (C2 `d44c830`) new `useModalA11y` hook (autofocus + ESC + Tab/Shift+Tab trap + focus-restore + FOCUSABLE_SELECTOR export). Applied to CreateSessionModal / MobileOverflowDrawer / PinGate (role=dialog + aria-modal + aria-labelledby). ContextLowToast correctly role=status + aria-live=polite (not dialog). (C3 `d6a29e0`) SessionCostTable degrades to `md:hidden` card stack on mobile + themed EmptyState. (C4 `f59df43`) touch targets ≥44×44 on 5 buttons + `.session-tab` CSS. (H2 `4560351`) TerminalTabs + SplitChatLayout teammate row flipped from `overflow-x-auto` → `flex-wrap`; fixed heights relaxed to `min-height` + py-1. Client tests 125→147 (+22 via `a11yHardening.test.ts`). Typecheck green.**
- Last updated: 2026-04-17
- Phase: **Phase P.1 complete (Coder-16). Security hardening — 1 Critical + 3 High + 1 Medium. 5 commits. (C1 `69f7fa3`) default bind `127.0.0.1`; `bindHost` / `COMMANDER_HOST` opt-in; `isLocalRequest` now IP-based (Fastify trustProxy off) so Host-header spoof is dead; boot-time `refuseBindWithoutPin` guard. (H1 `747f490`) `/api/hook-event` loopback-only; `isAllowedTranscriptPath` allowlist rejects `/etc/hosts` + `../` traversal; `resolveOwner` LIKE → `json_each` exact match; fs.watch `SPECIFIC_WATCHER_CAP = 100` LRU. (H2 `1e81c39`) WS `verifyClient` rejects cross-origin upgrades; CSP `connect-src` tightened from `ws: wss:` wildcard to localhost-only. (H3 `27751c2`) fastify ^5.8.5 (GHSA-247c-9743-5963). (M1 `1017d8c`) @fastify/static ^9.1.1 (GHSA-pr96 + GHSA-x428). `pnpm audit --prod` → "No known vulnerabilities found" (down from 3). Server tests 110→126 (+16). Live verified: bound loopback only, CSP tight, hook-event 200 from loopback, WS evil-origin → 401.**
- Last updated: 2026-04-17
- Phase: **Phase O complete (Coder-16). HeaderStatsWidget — top-right CPU/Mem/5h/7d chips. 4 commits. (`6a9aa0c`) `system-stats.service` 2s sampler via node `os` only (zero new deps); emits `system:stats` WS on new `system` channel. (`096c4e8`) `sessionTickService.getAggregateRateLimits` picks freshest non-null tick + 10min staleness gate; REST `GET /api/system/rate-limits` + `system:rate-limits` WS (signature-deduped). (`3f926aa`) client `useSystemStats` + `useAggregateRateLimits` + pure `formatResetsCountdown` (4 tiers) + `systemStatsBands` utils + `HeaderStatsWidget` slotted into TopCommandBar desktop branch. (`6fc3534`) F15 note updated in COMMANDER_IDE_RESEARCH. Server 99→110 (+11), client 106→128 (+22). No new deps.**
- Last updated: 2026-04-17
- Phase: **Phase N.2 complete (Coder-16). Sentinel-collision fix (closes Phase N.0 flagged bug). HEAD `c057718`. `resolveSentinelTargets` now skips sentinels whose `team_name` has no `~/.claude/teams/<name>/config.json` on disk; new `sessionService.healOrphanedTeamSessions()` retires orphan rows at boot (team_name=NULL, status=stopped, real panes → `tmux_session='retired:<id>'`). Option A preserves pane availability for live members; boot self-heal catches already-resolved orphans from prior runs. Server 92→99 (+7). Boot log verified: retired 1 orphan (`coder@ovagas`), zero UNIQUE errors.**
- Last updated: 2026-04-17
- Phase: **Phase N.0 complete (Coder-16). CTO's 4-patch minimum-fix prescription for stuck-state — ALL shipped. Patch 1 (`bc9b126`): Stop hook → status='idle' at turn boundary. Patch 2 (`cfd1e65`): status-poller yields 10s window to recent idle hook writes (julianday-based ms_since_update in SQL). Patch 3 server (`6067c1d`): `sessions.last_activity_at` column + `sessionService.bumpLastActivity` single write surface + WS `session:heartbeat` event + bumpers wired into hook/tick/chokidar/poller (poller bumps ONLY on real flips, never on yields). Patch 3 client (`a618121`): `useHeartbeat` hook + `<HeartbeatDot>` component + SessionCard stale-override (working/waiting→idle visual when >30s quiescent) + ChatThread LiveActivityRow gating. Patch 4 (`a42cfb0`): `scripts/install-hooks.mjs` (idempotent merge, preserves user-added matchers) + SessionStart/SessionEnd route handlers (SessionStart→working, SessionEnd→stopped). Install ran on Jose's machine — settings.json hooks now `['PostToolUse','SessionEnd','SessionStart','Stop']`. An original transcript-rotation inference approach (`a97ee1c`) was reverted (`b09c908`) when team-lead relayed CTO's simpler prescription — all Phase N.0 now stands on the 4 CTO-authored patches. Server 64→92 (+28), client 93→109 (+16). Typecheck clean.**
- Last updated: 2026-04-17
- Phase: **Phase M complete (Coder-16, historical). Commander now owns the authoritative session telemetry feed via Claude Code's statusline JSON-on-stdin protocol. B1: `packages/statusline/statusline.mjs` forwarder + install/uninstall scripts + `/api/session-tick` loopback-only endpoint + `session_ticks` table + WS broadcast. B2: context band constants (0-49 green / 50-79 yellow / 80-89 orange / 90+ red) + ContextLowToast on upward crossings (orange/red only) + band strip in ChatPage + `useSessionTick` hook. B3: LiveActivityRow shows tick-derived tokens + pane-derived verb + mini context-% progress bar inside the chat's working indicator.**
- Last updated: 2026-04-17
- Phase: Phase L (historical): Three drift bugs fixed in one pass: (B1) status detector recognizes past-tense completion verbs + stale-elapsed override + multi-line footer elapsed, so `✻ Cooked / 21261s` no longer reads as working. (B2) PM/lead JSONL rotation now bridges via a new pm-cwd-rotation strategy in resolveOwner + aligned watcher-bridge + auto-append on chokidar discovery, so rotated transcripts never serve frozen chat. (B3) Plan widget hides any plan whose latest activity is more than 2h older than the chat's latest message, so historical plans retire when the session moves on.**
- Last updated: 2026-04-17

> **🪪 Rebrand 2026-04-17 — display-only (scope A).** User-facing product
> name is now **JStudio Command Center**. Internal slugs intentionally
> preserved: repo dir `jstudio-commander`, package names `@commander/*`,
> signed health `service: "jstudio-commander"` (Phase D), team config
> names, user's `~/.jstudio-commander/config.json`, `commander-hook.sh`.
> Rebuild the .app bundle after pulling: `bash scripts/macos-launcher/build.sh`.

> **⚠️  Port migration — user action optional.** The default server port
> moved from 3002 → 11002. If your `~/.jstudio-commander/config.json`
> has `"port": 3002`, the override still wins — remove the field or set
> it to 11002 to pick up the new default. Also update
> `~/.claude/hooks/commander-hook.sh` (the user-managed hook copy
> outside the repo) to match, otherwise Claude Code hook events will
> be POSTed at the wrong port.

- HEAD: `b54fab6` (Phase S.1 Patch 5 — regression tests). Phase S.1 commits (in order): `4efa704` (P1+P3 resolveFirstPaneId + createSession pane ids + sendKeys guard) → `6684a90` (P2 healLegacySessionNameTmuxTargets) → `1bceafc` (P4 ContextBar reads tick) → `b54fab6` (P5 guard/heal/ctx sync tests). Pre-S.1 HEAD was `1ad2aa1`. Phase R commits (in order): `4ace394` (L4 drop transcript_path) → `52aa8a1` (M3 drop last_line_count) → `7ac6a90` (M4 getAggregateRateLimits JOIN) → `b439a9d` (M5 hook queue 503) → `bc0295b` (M6 strftime + audit). Phase Q commits: `29cfd98` (service + state machine + migration) → `1964bb6` (routes + WS) → `8d87545` (SessionCard UI). Phase S commits: `64fa7af` (upload endpoint) → `b5e4f0a` (drop/paste/picker UI) → `3473852` (send flow + inject). Pre-S/Q/R HEAD was `0229bcf` (Phase P.4 docs). Phase P.4 commits (in order): `933e498` (Patch 1 core-flows repair) → `c11237e` (Patch 2 session-creation E2E) → `e6f7177` (Patch 3 hook-stop-flip E2E) → `92fd593` (Patch 4 statusline-tick E2E) → `2d86e5c` (Patch 5 Fastify integration harness + 3 specs) → `8b62bb3` (Patch 6 Playwright config doc). Pre-P.4 HEAD was `2ca5ec3` (Phase P audit docs). Phase P.3 commits (in order): `f77ad2b` (H1 bounded readJsonlOrigin) → `4a80ec0` (H2 yield protection) → `119ba4e` (H3 Node commander-hook) → `1f4235f` (H4+L2+L3 terminal removal) → `06275ea` (M2 heal in reconcile) → `73b1669` (L1 DEV-gated log + P.3 tests) → `390a7ed` (hook port + ESM). Phase P.2 commits: `c4ab101` (C1 focus-visible) → `d44c830` (C2 modal a11y) → `d6a29e0` (C3 mobile cards) → `f59df43` (C4 touch targets) → `4560351` (H2 flex-wrap + P.2 tests). Phase P.1 commits: `69f7fa3` (C1 bind+IP-auth+boot-guard) → `747f490` (H1 hook-event+allowlist+LIKE+watcher cap) → `1e81c39` (H2 WS origin + CSP + tests) → `27751c2` (fastify 5.8.5) → `1017d8c` (@fastify/static 9.1.1). Phase O commits: `6a9aa0c` (system-stats service + WS) → `096c4e8` (rate-limit aggregator + REST) → `3f926aa` (HeaderStatsWidget) → `6fc3534` (F15 docs). Phase N.2 commit: `c057718`. Pre-N.2 HEAD was `a42cfb0` (Phase N.0 Patch 4 — SessionStart/End hooks). Phase N.0 commits (in order): `a97ee1c` (reverted transcript-rotation inference) → `b09c908` (revert) → `bc9b126` (Patch 1 Stop hook→idle) → `cfd1e65` (Patch 2 poller yield) → `6067c1d` (Patch 3 server heartbeat) → `a618121` (Patch 3 client HeartbeatDot + stale-override) → `a42cfb0` (Patch 4 install-hooks + SessionStart/End routes). Pre-N.0 HEAD was `c3fd10b` (Phase M Bundle 3 LiveActivityRow). Phase M commits: `174257e` (B1 statusline infra), `c3284cb` (B2 context bands + toast), `c3fd10b` (B3 LiveActivityRow). Phase L HEAD (historical): `932c152` (Phase L Bundle 2 refinement — cross-session leak guard). Phase L commits: `5d22eb2` (B1 status detector), `da96818` (B2 chat rotation bridge), `0b52078` (B3 plan age-gate), `da21d55` (docs), `932c152` (B2 refinement: jsonl-origin discriminator + coder-team-rotation strategy + origin-scoped cwd fallback in watcher-bridge, blocks the cross-session tool-call leak the user reported). Phase K addendum: `f40ad04`. Phase K commits: `9a37ec3` (Bundle 1 parser refactor + JSON-in-wrapper routing + new types), `686392d` (Bundles 2-4 SystemEventChip + UnrecognizedProtocolCard + ChatThread fragment renderer + noise filter + localStorage toggle + TeammateMessageCard color-map extraction), `0c6210f` (Bundle 5 +24 tests), `86f16db` (docs), `f40ad04` (addendum: SENDER_JSON_PREAMBLE_RE tolerates zero-ws, preamble sender wins attribution on disagreement, +5 tests, 61 → 66 client). Phase J.1 commit: `18f42f3`. Phase J previous HEAD: `c852480` (Bundle 5 protocol cards). Phase J commits: `96c344e` (Bundles 1+2+3 shared types + activity detector + poller evidence + WS payload + status-history endpoint + 7 activity-detector tests), `0d295e9` (Bundle 4 client activity rendering in SessionCard / ContextBar / SplitChatLayout / TeammateRow), `c852480` (Bundle 5 chatMessageParser extended + ProtocolMessageCards + ChatThread routing + 15 parser tests). Phase I.0 emergency patch: `5a8ace2`. Phase I commits: `a67dc1f` `d9ce052`. Phase H commits: `b13be92` `e4a1645` `4fbe99d` `b05a67a`. Phase G.2 commit: `425632b`. Phase G.2 commit: `425632b`. Phase G.1 commits: `68ce81a` (detector hoist), `976603d` (ContextBar monitoring label), `1b62f63` (predicate parent-exclusion + regression suite). Phase G commits: `0b0d632` (Bundle 3 dismiss button), `3ba49a9` (Bundles 1+2+4 cross-session guard + widened adoption + startup heal), `2f04086` (Bundle 5 icons), `a3ea2fa` (Bundle 6 top-bar filter + bot count badge). Phase F commits: `457d9e5` `d357f03` `b9e7bc4` `654cb05` (Bundle 1 structured chat), `2c0e063` (Bundle 2 auto-split), `467adce` (Bundle 3 pane adoption), `a1aa074` (Bundle 6 bypass-perm kill-switch), `ad163ba` (Bundle 5 team-lead adoption + coder naming). Phase E.2: `58434d9` (Vite → 11573 strictPort). Phase E.1: `f5da3ba` (dev-mode redirect). Phase E: `9cf67af` `02ae1ef` `23ea243` `6fda3c3` + `f956fcc` `15fe784` `ab72eec` `05ebbcd` `24f21f9` `26cfe2b` `603b398` `3d0de45` `eb9f85f` `4a040b8` `8089542`. Phase C: `0970950` `dae794f`. Phase B: `6177fe2` `ad3d7fe`. Phase A: `b7886fb` `c21ab5b` `49f149a` `69a66f0` `72d2fae` `2787b2d`.
- Model: **Opus 4.7** (migrated from 4.6). Default effort: **xhigh** for Commander-spawned sessions.
- Server port: **11002** (new default as of Phase D, migrated from 3002). Override via `~/.jstudio-commander/config.json` still honored. · Vite: **5173**
- Blockers: none
- Backlog: Batch `/output` endpoint deferred (low marginal value post-#219). Remaining suggestions are housekeeping (see Next up).

## Phases
- [x] Phase 0-10: v1 Complete (see PM_HANDOFF.md)
- [x] Post-v1 Polish Wave 1 — Coder-7 (42 commits)
- [x] Post-v1 Polish Wave 2 — Coder-8 (plan-attach + verification)
- [x] Feature Wave 1 — Coder-9 (17 commits, 2026-04-14 → 2026-04-15)
- [x] Feature Wave 2 + Stabilization — Coder-9 (25 commits, 2026-04-15 → 2026-04-16)
- [x] Phase A — Token-audit cleanup sweep — Coder-11 (6 commits, 2026-04-16)
- [x] **Phase B — Polling protocol changes (#216 tail-delta, #219 preview pause) — Coder-11 (2 commits, 2026-04-16)**
- [x] **Phase C — Project tech-stack pills + recent commits (#230) — Coder-12 (2 commits, 2026-04-17)**
- [x] **Phase D — Launch UX hardening (signed health + preflight + macOS .app + port 11002) — Coder-12 (4 commits, 2026-04-17)**
- [x] **Phase E — Finish 4.7 migration + NODE_ENV gate + Command Center rebrand — Coder-12 (11 commits, 2026-04-17)**
- [x] **Phase F — Structured chat messages + auto-split + pane adoption + prompt-detection kill-switch + team-lead adoption + coder naming — Coder-14 (9 commits, 2026-04-17)**
- [x] **Phase G — Dismiss button + cross-session pane rejection + widened adoption cwd + startup heal + larger teammate icons + top-bar filter + bot count badge — Coder-14 (5 commits, 2026-04-17)**
- [x] **Phase G.1 hotfix — status detector active-tail hoist + ContextBar monitoring label + startup-heal predicate parent-exclusion + 7 regression tests — Coder-14 (3 commits, 2026-04-17)**
- [x] **Phase G.2 — Adoption re-parents teammates + rewrites team config leadSessionId + 5 atomic-rewrite tests — Coder-14 (1 commit, 2026-04-17)**
- [x] **Phase H — ContextBar timer reset on turn boundary + StickyPlanWidget dismissal persistence (localStorage) + getActivePlan recency (latest plan only) + 3 new plan tests — Coder-15 (3 commits, 2026-04-17)**
- [x] **Phase I.0 — Emergency PM patch: team-config reconcile never dismisses on isActive=false — Team-lead (1 commit `5a8ace2`, 2026-04-17)**
- [x] **Phase I — Teammate palette (muted idle) + teammate-active display state (light blue) + split-pane glass top bar + force-close overflow/modal + PM system-notice endpoint — Coder-15 (2 commits, 2026-04-17)**
- [x] **Phase J — Live pane-activity parser + status-flip evidence log + `/status-history` endpoint + WS session:status payload extension + inter-session protocol cards (shutdown/plan-approval) + sender-preamble detection — Coder-15 (3 commits, 2026-04-17).**
- [x] **Phase J.1 — `✻ Idle` verb override (IDLE_VERBS allowlist) so spinner-glyph hoist no longer flips parked PMs to `working` + classifyStatusFromPane exported for unit tests + 4 new tests — Coder-15 (1 commit, 2026-04-17). Final Coder-15 rotation.**
- [x] **Phase K — Chat parser robustness: multi-wrapper scan (array return), JSON-in-wrapper routing for idle_notification / teammate_terminated / shutdown_approved, SystemEventChip (chips|cards|hide via localStorage `jsc-show-system-events`, default chips), consecutive-same-teammate idle collapse ×N, UnrecognizedProtocolCard fallback for unknown JSON `type`, parser+collapse test coverage (+24 tests, 37 → 61 client) — Coder-16 (3 commits, 2026-04-17).**
- [x] **Phase K addendum — `sender{json}` zero-whitespace preamble routing: SENDER_JSON_PREAMBLE_RE detects bare `team-lead{"type":"shutdown_request",...}` form the messaging layer uses outside wrappers, preamble sender wins attribution on disagreement with JSON.from, +5 tests (61 → 66 client) — Coder-16 (1 commit `f40ad04`, 2026-04-17).**
- [x] **Phase M — Statusline forwarder + context-low warning + chat LiveActivityRow: Commander owns the authoritative live telemetry feed. Install via `node scripts/install-statusline.mjs` (backs up settings.json atomically + writes absolute-path command). Endpoint: `POST /api/session-tick` (loopback-only, 250ms dedup, permissive body validation). DB: `session_ticks` upsert-latest-wins + index on updated_at. Client: `useSessionTick` hook, ContextLowToast, LiveActivityRow, contextBands utility. Client 72→93 (+21), server 57→64 (+7) — Coder-16 (3 commits `174257e` `c3284cb` `c3fd10b`, 2026-04-17).**
- [x] **Phase L B2 refinement — cross-session tool-call leak guard: `jsonl-origin.service.ts` reads the first JSONL record to extract `agentName`/`teamName` (coder JSONLs carry `agentName`, PM JSONLs don't); `resolveOwner` gates `pm-cwd-rotation` off when origin marks a coder JSONL + adds a new `coder-team-rotation` strategy that binds by `team_name = origin.teamName AND agent_role != 'lead-pm'`; `watcher-bridge` scopes its cwd fallback by the same predicate. Tests +13 (57/44 server) — Coder-16 (1 commit `932c152`, 2026-04-17).**
- [x] **Phase L — Three drift fixes in one pass: (B1 `5d22eb2`) status detector past-tense completion verbs (`Cooked`/`Crunched`/`Brewed`/`Finished`/etc.) + `/ed$/` fallback + stale-elapsed gate (>600s) + multi-line footer elapsed extraction so `✻ Cooked / 21261s` classifies as idle not working; (B2 `da96818`) PM/lead transcript rotation bridge — resolveOwner step 4 `pm-cwd-rotation` binds new UUIDs to the unique UUID-id session in the cwd + watcher-bridge aligns with it + auto-appends the new transcript_path on chokidar discovery so REST chat endpoint never serves frozen JSONL; (B3 `0b52078`) plan widget age-gate — getActivePlan returns null when chat-latest is >2h ahead of the plan's last activity OR wall-clock is >2h past it, so historical plans retire after the session moves on. Test counts: server 23 → 44 (+21), client 66 → 72 (+6) — Coder-16 (3 commits, 2026-04-17).**
- [x] **Phase N.0 — CTO's 4-patch minimum-fix prescription for stuck-state (closes the "session reads working forever" class entirely). (P1 `bc9b126`) Stop hook → status='idle' at top of `processHook`, before transcript handling, reusing `resolveOwner` for attribution + `emitSessionStatus(id,'idle',{evidence:'stop-hook',at})`. (P2 `cfd1e65`) status-poller SELECT returns `ms_since_update` via julianday in SQL (timezone-safe) + `HOOK_YIELD_MS=10_000` gate scoped to `status='idle'` only; yield updates in-memory cache, clears `idleSince`/`workingSince` so next poll compares correctly. (P3 server `6067c1d`) `sessions.last_activity_at INTEGER NOT NULL DEFAULT 0` + `Session.lastActivityAt` shared type + `eventBus.emitSessionHeartbeat(id,ts)` + WS `session:heartbeat` event on `sessions` topic + `sessionService.bumpLastActivity(id)` single write surface wired into hook-event/session-tick/watcher-bridge/status-poller (poller bumps ONLY on real flips, never on yields). (P3 client `a618121`) `useHeartbeat(sessionId, initialTs?)` hook with 1s ticker + 30s stale gate, `formatSecondsAgo` pure helper, `<HeartbeatDot>` 3-state inline chip (green+"Xs ago" / gray+"—" / gray+"stale"), SessionCard `applyStaleOverride` coerces working/waiting→idle visually when stale, ChatThread LiveActivityRow gated on `isWorking && !heartbeatStale`. (P4 `a42cfb0`) `scripts/install-hooks.mjs` idempotent merge (covers SessionStart/End/Stop/PostToolUse, preserves user-added matchers), SessionStart route→status='working', SessionEnd route→status='stopped'+stopped_at, both bump heartbeat, unknown-owner SessionStart=log+skip. Install ran on Jose's machine — `~/.claude/settings.json` hooks now `['PostToolUse','SessionEnd','SessionStart','Stop']`. Server 64→92 (+28), client 93→109 (+16), typecheck clean — Coder-16 (6 commits + 1 revert `a97ee1c`+`b09c908`, 2026-04-17). Pre-existing sentinel-collision bug surfaced (`resolveSentinelTargets` + ovagas-ui orphan rows colliding on pane %59) flagged but not fixed; temporarily mitigated by `DELETE FROM sessions WHERE team_name='ovagas-ui'` (3 stopped rows) so server could boot. **Closed in Phase N.2.**
- [x] **Phase N.2 — Sentinel-collision fix — Coder-16 (1 commit `c057718`, 2026-04-17). Option A: `resolveSentinelTargets` SELECTs `team_name` + skips rows whose `~/.claude/teams/<name>/config.json` is missing (injectable `teamExists` predicate for tests). Boot-time `sessionService.healOrphanedTeamSessions()` retires orphan rows (team_name=NULL, status=stopped, real panes → `tmux_session='retired:<id>'`) BEFORE `teamConfigService.start()` so live reconciles claim panes without UNIQUE collision. Server 92→99 (+7). Live verified: 1 orphan (`coder@ovagas`) retired at boot, zero UNIQUE errors.**
- [x] **Phase O — HeaderStatsWidget (top-right CPU/Mem/5h/7d chips) — Coder-16 (4 commits `6a9aa0c` `096c4e8` `3f926aa` `6fc3534`, 2026-04-17). Zero new deps; node `os` only. 2s sampler → `system:stats` WS on new `system` channel (clients auto-subscribe on handshake). `sessionTickService.getAggregateRateLimits` freshest-non-null tick + 10min staleness gate; REST `GET /api/system/rate-limits` + signature-deduped `system:rate-limits` WS. Client: `useSystemStats` + `useAggregateRateLimits` + pure `formatResetsCountdown` (4 tiers: `resetting…` / `Xm Ys` / `Xh Ym` / `Xd Yh`) + `systemStatsBands` utils (`bandForBudget` + `bandForMemory` + `formatBytes`) + `HeaderStatsWidget` in TopCommandBar desktop ≥lg only. F15 research note updated. Server 99→110 (+11), client 106→128 (+22).**
- [x] **Phase P — 4-track audit (QA / Security / UI / E2E) via parallel specialist agents — Coder-16 dispatched 2026-04-17. Outputs persist at `audits/PHASE_P_{QA,SECURITY,UI,E2E}.md`. 70 findings total (1 Critical, 10 High, 25+ Medium, 20+ Low, info). Remediation split across P.1/P.2/P.3 below.**
- [x] **Phase P.1 — Security hardening (C1 + 3× High + 1 Medium) — Coder-16 (5 commits `69f7fa3` `747f490` `1e81c39` `27751c2` `1017d8c`, 2026-04-17). C1: default `host: '127.0.0.1'` (opt-in via `bindHost`/`COMMANDER_HOST`); `isLocalRequest` now consults `request.ip` not Host header; boot-time `refuseBindWithoutPin` guard. H1: `/api/hook-event` loopback-only preHandler; `isAllowedTranscriptPath` allowlist (rejects `/etc/hosts`, traversal); `resolveOwner` LIKE → `json_each` exact match; `SPECIFIC_WATCHER_CAP = 100` LRU eviction. H2: WS `verifyClient` cross-origin rejection + CSP `connect-src` tightened from wildcard to localhost-only. H3: `fastify ^5.8.5` (GHSA-247c-9743-5963). M1: `@fastify/static ^9.1.1` (GHSA-pr96 + GHSA-x428). `pnpm audit --prod` → "No known vulnerabilities found" (down from 3). Server 110→126 (+16). Live verified: lsof bound `127.0.0.1` only, forged `Origin: https://evil.com` → HTTP 401.**
- [x] **Phase P.2 — UI/UX criticals (C1-C4 + H2) — Coder-16 (5 commits `c4ab101` `d44c830` `d6a29e0` `f59df43` `4560351`, 2026-04-17). C1: global `:focus-visible` ring in `index.css` + `outline-none` re-enable defense. C2: new `useModalA11y` hook (autofocus + ESC + Tab trap + focus-restore + FOCUSABLE_SELECTOR); applied to CreateSessionModal/MobileOverflowDrawer/PinGate (role=dialog + aria-modal + aria-labelledby); ContextLowToast kept role=status + aria-live=polite (NOT dialog). C3: SessionCostTable degrades to `md:hidden` card stack + themed EmptyState. C4: 5 buttons + `.session-tab` min-height to ≥44×44 (WCAG 2.5.5 / Apple HIG). H2: TerminalTabs + SplitChatLayout teammate row `overflow-x-auto` → `flex-wrap`. Client 125→147 (+22 in `a11yHardening.test.ts`). DEFERRED per team-lead explicit: C5 (editor layout rewrite), H1 (light theme), H6 (virtualization).**
- [x] **Phase P.4 — E2E buildout (Playwright + Fastify integration harness) — Coder-16 (6 commits `933e498` `c11237e` `e6f7177` `92fd593` `2d86e5c` `8b62bb3`, 2026-04-17). 16 new tests: 11 Fastify integration (`app.inject()`-based in `server/src/__tests__/integration/`) + 4 new Playwright E2E (session-creation, hook-stop-flip, statusline-tick) + 1 repaired sessions-page smoke. Unit counts unchanged (137 server / 145 client). Test grand total 298 across 4 execution modes (unit server 0.7s, unit client 0.5s, integration 1.0s, E2E 17s). Key infrastructure: `COMMANDER_DATA_DIR` env override in `config.ts` (tmpdir isolation via top-level `await import` in test files); `app.inject()` default `remoteAddress: 127.0.0.1` passes P.1 loopback gate without socket plumbing; `--test-force-exit` on `test:integration` script escapes `waitForClaudeReady` 12s timer chain (runtime 13s → 1s); `waitForBusEvent` helper in harness asserts WS-shape events against in-process eventBus without a real WS client. Playwright config: no webServer block (ambient `pnpm dev` races auto-spawn); `client/e2e/README.md` documents env var matrix (`COMMANDER_URL` / `COMMANDER_API` / `COMMANDER_PIN`) + side effects (real tmux spawns + live DB writes). Closes audit C-1 (session-creation E2E) + C-2 (hook-event integration + non-loopback 403) + H-1 (Fastify route integration) + H-2 (WS emit contract tests). DEFERRED per team-lead: chat-ingestion spec (audit Top 5 #4), heartbeat-live-ago spec (Top 5 #5), React component-render tests (H-3), file-watcher chain tests (H-4), CI setup (its own phase).**
- [x] **Phase S — File + image attachments in Commander chat — Coder-16 (3 commits `64fa7af` `b5e4f0a` `3473852`, 2026-04-17). Loopback-gated `/api/upload/:sessionId` multipart endpoint using `@fastify/multipart` stream-mode (per-chunk size enforcement avoids OOM vs `.toBuffer()`); 5MB image / 10MB file caps; filename allowlist `[A-Za-z0-9._-]` + `../` strip + ISO timestamp prefix on collision; uploads land at `~/.jstudio-commander/uploads/<sessionId>/<iso>__<name>`; 500MB per-session quota warn (non-blocking); `removeSessionUploads` wired into `purgeTeamSession` + `cleanupStaleTeammates`. Client: `useAttachments` hook (module-level cache, drag counter, File/Blob validation); AttachmentChipRow with image thumbnails (URL.createObjectURL + revoke); drop-overlay gated on `DataTransfer.types.includes('Files')`; FormData + fetch upload (not JSON api helper) with PIN header preserved; `buildInjectedPayload(paths, message)` emits single-line `@path1 @path2 message` (not bracketed paste multi-line — avoids tmux Enter-submit complexity); tmux inject via existing sendKeys. +22 tests (6 server integration hand-rolled multipart builder + 16 client pure-logic using Node 22 File/Blob).**
- [x] **Phase Q — Auto pre-compact assistant — Coder-16 (3 commits `29cfd98` `1964bb6` `8d87545`, 2026-04-17). Per-session state machine (idle/warned/compacting/emergency) in `pre-compact.service.ts`. Thresholds: WARN=85, EMERGENCY=95, RESET=40, HYSTERESIS_FLOOR=75 (75% floor prevents thrash near warn line). At 85% ctx → inject user-role warning message asking AI to save state + reply `READY_TO_COMPACT`; watcher-bridge scans user-role text blocks for case-sensitive READY_PHRASE substring; on match → auto-inject `/compact`. At 95% → emergency direct compact (skip warn). Per-session `auto_compact_enabled INTEGER NOT NULL DEFAULT 1` column added + one-shot PM/lead-pm heal to 0 in same migration block (PM sessions don't auto-compact without explicit opt-in — they carry durable handoff state). `rowToSession` coerces INTEGER↔boolean; `serializeCol` conversion; legacy pre-migration rows default true. Double-fire guard: once `warned`, further ticks don't re-inject until state reset. `GET /api/pre-compact/status` + `PATCH /api/pre-compact/sessions/:id` + WS `pre-compact:state-changed` broadcast on sessions topic. Client: `usePreCompactState` hook (module-level cache + one-shot hydrate + WS subscription); PreCompactIndicator renders nothing for idle, ⚠ Waiting / ⟳ Compacting otherwise; SessionCard Zap/ZapOff toggle. +19 tests (13 server unit + 6 client pure logic). Circular import pre-compact.service ↔ session.service is intentional — only used inside function bodies, ESM handles cycle fine.**
- [x] **Phase S.1 — Message routing + ctx single-source-of-truth — Coder-16 (4 commits `4efa704` `6684a90` `1bceafc` `b54fab6`, 2026-04-17). Closes 2 live bugs Jose reported: (1) OvaGas PM→coder leak — root cause `sessionService.createSession` stored tmux session name (`jsc-<uuid>`) in `sessions.tmux_session` → `tmux send-keys -t <session-name>` routes to active pane → PM text leaked to focused coder pane. Fix: new `tmuxService.resolveFirstPaneId(name)` helper + `createSession` stores `%NN` (resolved AFTER tmux new-session) + `index.ts` orphan-discovery same + `sendKeys` guard (DEV throws on non-pane non-retired; PROD warns+proceeds; `retired:*` silent no-op) + integration test regex `/^jsc-/` → `/^%\d+$/`. Patch 2 `healLegacySessionNameTmuxTargets` sweeps legacy `jsc-*` rows at boot AFTER `healOrphanedTeamSessions` BEFORE `healCrossSessionTeammates` (resolved → UPDATE pane id + emit session:updated; gone → status='stopped'; idempotent). (2) ContextBar ctx% drift — ContextBar computed its own % from `displayTokens/contextLimit` while LiveActivityRow + band rail + ContextLowToast read `tick.contextWindow.usedPercentage` (4 surfaces, 2 sources). Fix: ContextBar now accepts `sessionTick` prop; new exported pure helper `resolveContextPercent(tick, tokens, contextLimit)` prefers tick when non-null, falls back only when no tick; server `session:status` WS extras have NO context field (verified, no server changes needed). Patch 5 tests: `tmux-guard.test.ts` covers guard DEV throw/PROD warn/retired no-op/pane bypass + resolveFirstPaneId idempotency + null-on-gone + first-pane-despite-active (real tmux split+select) + pane-id routing regression (two-pane split + capture-pane assertion on both panes); `session-name-heal.test.ts` mirrors heal SQL against in-memory DB with injectable resolver across 5 scenarios; `ContextBar.test.ts` locks tick-first invariant (tick value wins even when token ratio disagrees, clamp to 100, fallback on null tick/null pct, divide-by-zero safe). Server 159→173 (+14) unit + 19 integration + client 167→174 (+7) = **366 tests** (up from 345). `pnpm audit --prod` clean. Typecheck green. **Patch 6 (display audit doc `audits/PHASE_S1_DISPLAY_AUDIT.md`) DEFERRED** — compact pressure at 86%; next rotation should dispatch it.**
- [x] **Phase R — QA cleanup bundle (L4 + M3-M6) — Coder-16 (5 commits `4ace394` `52aa8a1` `7ac6a90` `b439a9d` `bc0295b`, 2026-04-17). L4 (`4ace394`): drop legacy `transcript_path` column with one-shot consolidation — legacy rows with populated `transcript_path` but empty `transcript_paths` get migrated into the JSON array BEFORE the DROP COLUMN; supersedes the boot-heal block previously in `index.ts` (removed). Note: `transcript_path` REMAINS as a field on hook payloads (Claude Code sends the absolute JSONL path); the DB array column `transcript_paths` is separate and unchanged. M3 (`52aa8a1`): drop `file_watch_state.last_line_count` — divergent on truncate-then-rewrite (offset rewound but count kept climbing); nothing ever read it; INSERT/UPDATE in `file-watcher.service.ts` omits it. M4 (`7ac6a90`): `getAggregateRateLimits` JOINs `sessions` + filters `status != 'stopped'` so stopped-session ticks don't pollute the aggregate rate-limit calculation. M5 (`b439a9d`): `HOOK_QUEUE_MAX = 20` in `hook-event.routes.ts` returns 503 + `Retry-After: 1` when serialized promise-chain queue saturated; `_setHookQueueDepthForTests(n)` helper deterministically saturates via stalled promises returning `release()` handle (Promise.all concurrency was racy — processHook drained between enqueues). M6 (`bc0295b`): poller `ms_since_update` switched to `(strftime('%s','now') - strftime('%s', updated_at)) * 1000` — integer-equivalent to julianday on UTC inputs (±999ms), returns NULL on unparseable input (more defensive — surfaces corruption as typed boundary not nonsense diffs); boot-time LIKE-scan of `sessions.updated_at` warns on non-UTC drift shapes (AM/PM, `+HH:MM`, `-HH:MM`, trailing Z). Column drops use SQLite 3.35+ `ALTER TABLE DROP COLUMN` gated on `PRAGMA table_info` (idempotent). +9 tests (7 server unit `poller-time-audit.test.ts` strftime↔julianday equivalence + audit SQL shape recognition, 2 server integration `post-hook-event.test.ts` queue-cap boundary). `pnpm audit --prod` → 0 CVEs. Totals this session: server 159 + 19 integration + client 167 + 5 E2E = **350 tests**.**
- [x] **Phase P.3 — QA hardening + terminal removal (H1-H4 + M2 + L1 + hook follow-up) — Coder-16 (7 commits `f77ad2b` `4a80ec0` `119ba4e` `1f4235f` `06275ea` `73b1669` `390a7ed`, 2026-04-17). H1: `readJsonlOrigin` bounded `openSync`/`readSync`/`closeSync` (no 50MB slurp). H2: `appendTranscriptPath` no longer bumps `updated_at` — append-only paths preserve the poller yield window. H3: `hooks/commander-hook.sh` replaced by pure-Node `hooks/commander-hook.js`; `install-hooks.mjs` deploys script + chmod 755 + migrates legacy `.sh` settings matchers to `.js` in place. H4+L2+L3: REMOVED Terminal page + xterm.js/node-pty deps (product decision — half-built preview out; rebuild future sprint); MobileNav swapped Terminal for Analytics; `pnpm audit --prod` still clean; 691 deletions, 19 insertions net. M2: `healOrphanedTeamSessions` now runs on every team-config reconcile, not just boot. L1: client `[ws] Connected` log DEV-gated via `import.meta.env.DEV`; added `client/src/vite-env.d.ts`. Follow-up `390a7ed`: commander-hook port resolution (`COMMANDER_HOOK_URL` env → config.json port → default 11002) + `~/.claude/hooks/package.json { type: module }` deployed so Node loads .js as ESM natively. Server 126→137 (+11), client 147→145 (net; TerminalTabs test removed). Typecheck clean; `pnpm audit --prod` clean. Live verified: synthetic Stop hook → server log reflects correct routing; `/etc/hosts.jsonl` → REJECTED by P.1 H1 allowlist; zero ESM reparse warnings. DEFERRED per team-lead explicit: L4 (transcript_path column drop), L5 (structured logs), M1 (E2E heartbeat test → P.4), M3-M6 (other QA mediums).**

## Feature Wave 2 Highlights

### Architecture
- Rotation-detector DELETED → deterministic `transcript_paths: string[]` column (#204)
- Hook events serialized via promise chain (#209)
- Session create/delete wrapped in DB transactions (#208)
- Status detection: 25-line capture, idle-footer allowlist, numbered-choice block (#222, #236)

### Design System (CSS class spine — DO NOT inline-duplicate)
`.nav-btn` · `.session-tab` · `.cta-btn-primary` · `.filter-chip` · `.waiting-tab-alarm`

### Features shipped
- City view `/city` — pure CSS cyberpunk pixel art (#214)
- Tunnel URL + QR badge in TopCommandBar (#231)
- Manual refresh button in ContextBar (#237)
- Button-style session tabs with state-aware glow (#225)
- SessionCard: effort pill, model badge, time-since, quick-split (#226)
- ProjectCard: linked-sessions cluster, last-scanned, compact indicators (#227)
- Analytics: count-up animation, trend deltas (#212)
- Mobile: safe-area fix (#232), split collapse to strip (#233)
- Stopped fold on SessionsPage (#220)
- Session name disambiguator (#220)

### Fixes
- Analytics null crash (#210)
- useChat stale poll (#193)
- pendingLocal duplicate render (#224)
- Waiting false-positive on idle footer (#236)
- Working→waiting transition lag (#222)

### Audits
- Edge-case audit: 14 scenarios, 0 FAIL (`AUDIT_2026-04-15.md`)
- Token-efficiency audit: 12 surfaces (`AUDIT_TOKENS_2026-04-15.md`)
- UI rundown audit: 2 Major fixed, 8 Nit (`UI_AUDIT_2026-04-15.md`)
- Tunnel security audit: 9 fixes (coder-10, `TUNNEL_AUDIT_2026-04-15.md`)

## Known Issues

- **tsx watch does NOT hot-reload server changes reliably** — MUST manually restart
- **Hooks only fire for sessions started AFTER hook configuration**
- **Agent-status heuristic is regex-based** — evolves with each Claude Code UI change. Consider `.claude/status.json` if exposed upstream.

## Resolved in Phase P

- ~~**node-pty broken**~~ — Terminal page + node-pty/@xterm/* deps REMOVED in Phase P.3 H4. A proper xterm.js rebuild is a future sprint.
- ~~**Host-header spoof bypasses PIN auth**~~ — Phase P.1 C1: default loopback bind + IP-based `isLocalRequest` + boot-time refuse-non-loopback-without-pin.
- ~~**/api/hook-event unauthenticated**~~ — Phase P.1 H1: loopback preHandler + transcript_path allowlist + LIKE → json_each + watcher LRU cap.
- ~~**WebSocket no Origin check**~~ — Phase P.1 H2: `verifyClient` + tightened CSP `connect-src`.
- ~~**Zero focus rings for keyboard users**~~ — Phase P.2 C1: global `:focus-visible`.
- ~~**Modals not announced as dialogs**~~ — Phase P.2 C2: `useModalA11y` + role=dialog across 4 modals.
- ~~**Tables don't degrade on mobile**~~ — Phase P.2 C3: SessionCostTable card stack.
- ~~**Touch targets < 44px**~~ — Phase P.2 C4: 5 buttons + `.session-tab` bumped.
- ~~**commander-hook.sh shell-python3 shim silently dropped events**~~ — Phase P.3 H3: rewritten in pure Node, install-script migrates old `.sh` matchers.

## Resolved decisions (Wave 2 additions)

- **Transcript ownership** — deterministic hook-bound `transcript_paths` list. No heuristics. Hooks append; chat concatenates in array order.
- **Waiting detection** — strong signals first (numbered-choice, [y/N], "Do you want to"), idle-footer short-circuit, no bare `?` pattern. Capture 25 lines.
- **Model default** — Opus 4.7, no `[1m]` suffix (4.7 gets 1M automatically). Commander-spawned sessions hardcode `xhigh` effort instead of reading settings.json.
- **Mobile split** — below 768px, SplitChatLayout forces minimized strip. Teammate tap navigates to full single-pane route.
- **Design system** — all interactive elements use one of 4 CSS class families. Drift is now confined to chat-internal components.

## Next up (pending backlog)

1. **Batch `/output` endpoint** — deferred Phase B sub-item. Skip until multi-teammate views are measurably hot post-#219.
2. **jstudio-init-project helper** — scaffold STATE.md / PM_HANDOFF.md with one prompt
3. **Memory/skill inventory view** — browse `~/.claude/skills/` + memory files as panel
4. **Agent-status via `.claude/status.json`** — replace regex heuristics if upstream exposes it
5. **Stack detection follow-ups (#230)** — only scans 1-level workspace children today; deeper monorepos would need glob recursion. Mapping table starts at ~30 entries — grow as new stacks appear in live projects.

## Critical rules for future coders

- **Skill ≠ Agent** — Skill loads into context. Agent spawns a subagent.
- **PM bootstrap = 3 pieces** — SKILL.md Cold Start + bootstrap prompt + session_type='pm' inject
- **Verify the served code** — curl the Vite endpoint and grep, not just git log
- **`git add <specific-files>`** when coder-10 is active — NEVER `git add -A`
- **`WHERE status != 'stopped'`** is a trap for pane-backed rows; use `OR tmux_session LIKE '%'`
- **Always restart server** after edits: `lsof -ti:11002 | xargs kill -9; pnpm dev` (substitute the active port from `~/.jstudio-commander/config.json` if overridden)
