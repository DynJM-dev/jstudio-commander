# Issue 15.3 — Diagnostic (Phase 1.2 investigation rotation)

**Author:** coder-16 (fresh-context rotation, 2026-04-19)
**Status:** Pre-code investigation COMPLETE (Q1–Q3 answered, §4.2 D1 + §6.1/6.2 amended). Awaiting PM + Jose ratification. **NO CODE** this rotation.
**Related:** Phase 1 `fee7f35`, Phase 1.1 `9abe598` — both failed Jose's live smoke.

**Changelog:**
- v1 (initial draft) — class root cause + contract-level fix shape + 5 open questions.
- v2 — Q1 empirically refuted at JSONL-summary level; Q2 confirmed useChat ChatMessage[] is verbatim; Q3 Codeman reference pattern read and confirms §6 convergence; scope locked to §6.1–6.3 per Jose.
- v4 (§10 addendum) — post §6.1–6.3 smoke trace. Divergence point named: ContextBar's local `isWorking` derivation at `ContextBar.tsx:323` is the single wire the §6.2 OR-gate never reached. §6.1.1 fix is a contract-level prop threading from ChatPage; §6.4 re-tune is two minimal deltas. See §10.
- v3 (pre-code step 1 outcome) — D1 residual RULED OUT via live `sleep 10` capture on coder-16's own JSONL. `tool_use` timestamp `2026-04-19T23:47:14.767Z`, matching `tool_result` at `23:47:24.857Z` (Δ = 10.090s, matches sleep duration). File mtime advanced during the 10s window (tool_use flushed immediately at dispatch, BEFORE bash executes), so `hasPendingToolUseInTranscript` reading the tail would see the unmatched tool_use at any moment during execution. Even if a mid-tool Stop webhook fired (no evidence), the gate at hook-event.routes.ts:344 would correctly defer the idle flip. Proceeding to §6.1–6.3 code.

> **Standard of acceptance.** "Does Commander feel like VSCode Claude / Claude Code terminal / Codeman?" — NOT "does the test suite pass?" Unit-green is a necessary but insufficient signal; every prior acceptance was granted on unit-green and failed on live. This diagnostic is framed against the 10 user-observable acceptance criteria Jose pinned in the dispatch.

---

## 1. Observable fault

**Repro A — bash sleep.** In an attached Claude Code session, user runs `sleep 10 && echo done`.
- **Expected (acceptance criteria 1–4):** ContextBar + chat-bubble LiveActivityRow both show a continuous live indicator with a specific label ("Running Bash" or "Running command…"), elapsed timer climbs 1s/s, state never regresses to Idle.
- **Observed:** ContextBar label disappears / flashes "Idle" / shows a generic "Working…". LiveActivityRow unmounts mid-run. Elapsed timer resets or vanishes. On long-running tools this reads "session is waiting for me" when it is actually doing the user's work.

**Repro B — ls post-approval.** After approving a permission prompt, the follow-up Bash executes but the UI continues to render the waiting state for multiple seconds before catching up, or never surfaces the "Running command" label at all before the turn ends.

Evidence captured in this rotation: see §2 for the ground-truth JSONL event inventory that both repros rest on.

---

## 2. Claude Code event inventory during tool execution (ground truth)

Source: `~/.claude/projects/-Users-josemiguelbonilla-Desktop-Projects-jstudio-commander/ac67d1d9-181b-45c9-869a-31ae586db2d9.jsonl` (live session, 442 lines). Sampled window: first Bash tool_use (`toolu_017jqTTb9w9QURAuLqvsSNZG`).

**Event types present across the full transcript:**

| type | count | payload carrier |
|---|---|---|
| `assistant` | 170 | `.message.content[]` — text / thinking / tool_use |
| `user` | 137 | `.message.content[]` — tool_result (+ free-text user messages) |
| `system` | 20 | `.subtype` ∈ {`compact_boundary`, `stop_hook_summary`, `turn_duration`} |
| `attachment`, `file-history-snapshot`, `last-prompt`, `permission-mode`, `queue-operation` | — | metadata / snapshots |

**Timeline around `toolu_017jqTTb9w9QURAuLqvsSNZG` (Bash `ls -la …`):**

| timestamp | type | content |
|---|---|---|
| 08:02:13.476Z | `assistant` | tool_use (Bash) |
| 08:02:16.365Z | `user` | tool_result |

**Inventory between tool_use and tool_result: ZERO intermediate records.** No heartbeat. No progress event. No streaming chunk. The JSONL file is **silent for the entire duration of tool execution**.

**This is THE constraint.** Any live-indicator UX the team wants must be derived either from:
  (a) the **absence** of a matching `tool_result` after a known `tool_use` (i.e. infer "tool is running" from state that was recorded BEFORE the silence began), or
  (b) a signal source that is NOT the JSONL (pane capture, `statusline-hook` ticks, Commander's own timers).

A design that waits for mid-execution JSONL records to drive the UI will be silent for the length of every tool call — which is precisely what acceptance criterion #1 ("no silent periods") forbids.

**Stop hook behavior wrt JSONL silence.** Claude Code fires `Stop` every time the LLM yields — including the yield between `assistant.tool_use` dispatch and `user.tool_result` arrival. Phase N.0 P1's top-of-handler `status='idle'` flip is correctly gated by `hasPendingToolUseInTranscript` (hook-event.routes.ts:344), so in the steady state the Stop-hook path should NOT flash idle during tool exec. A remaining concern is the race between Claude Code's JSONL append and its Stop-hook emit — if the hook fires before the tool_use line is flushed to disk, the gate returns false and the row flips to idle (§5 D1).

---

## 3. Pipelines that feed Commander's UI state

There are three independent pipelines. The bug class lives in their composition, not in any single stage.

### 3.1 Pane-regex pipeline — `status-poller.service.ts` (5s interval)

```
tmux capture-pane
  → classifyStatusFromPane()      (pane text regex — `❯` idle, verb footer, numbered-choice, etc.)
  → applyActivityHints()          (15.1-D allowlist; upgrades idle→working)
  → [override] hasPendingToolUseInTranscript(transcriptPath)  when hinted ∈ {idle, waiting}
  → computeSessionState()         (typed SessionState per Phase 1)
  → eventBus.emitSessionStatus(id, 'working'|'idle'|…, { state: SessionState, … })
```

**Key constants (status-poller.service.ts:11–90):**
- `POLL_INTERVAL = 5_000` — hard ceiling on how quickly this pipeline can react to a new tool_use.
- `HOOK_YIELD_MS = 60_000` — if any hook fired within 60s and the pending-tool override did NOT fire, skip all reclassification.
- `FORCE_IDLE_COOLDOWN_MS = 60_000` — if row was stale-force-idled recently, skip EVERY reclassification including the pending-tool override. **This is a trap** (§5 D3).
- `STALE_ACTIVITY_MS = 90_000` — stuck-working force-idle trigger.
- `IDLE_GRACE_MS = 8_000` — working→idle only after 8s grace.

### 3.2 Hook pipeline — `hook-event.routes.ts` (push, real-time)

SessionStart / Stop / PostToolUse / SessionEnd → `processHook`:
- SessionStart → `status='working'`, bump heartbeat (:385)
- Stop → gated idle flip on `hasPendingToolUseInTranscript` (:344); emits `eventBus.emitSessionStatus(…, 'idle', {evidence:'stop-hook'})` when gate false.
- Every hook → `bumpLastActivity` + `bumpLastHookAt` (drives `last_hook_at` used by §3.1's yield gate).

### 3.3 Session-tick pipeline — `session-tick.service.ts` (push, statusline-driven)

The installed statusline hook posts `/api/session-tick` every time Claude Code repaints its statusline (seconds-ish cadence while Claude is drawing). Commander upserts `session_ticks`, emits `session:tick`. Client `useSessionTick` feeds ContextBar's `resolveContextPercent` (tick wins, S.1), LiveActivityRow extras, HeartbeatDot freshness.

**The gotcha here** (self-evident but load-bearing for the diagnosis): Claude Code does NOT repaint the statusline during tool execution. During `sleep 10` the statusline stream falls silent for the full 10 seconds. Any UI that treats `session_ticks.updated_at` as proof-of-life will read stale during every tool call.

---

## 4. Code-level drop-points (class root cause)

### 4.1 The controlling drop: `pendingToolName` is never plumbed.

- `hasPendingToolUseInTranscript(path): boolean` (jsonl-parser.service.ts:669) returns **only a boolean**. It does not return the tool name.
- `status-poller.service.ts:402–404` passes `pendingToolUse: boolean` to `computeSessionState` — and nothing else.
- `session-state.service.ts:167–173` constructs `Working:ToolExec` with `toolName: undefined` whenever the override fires.
- `client/src/utils/contextBarAction.ts:124–126` maps `Working:ToolExec` without `toolName` to the **generic** string `'Running tool…'`.

**Consequence.** Acceptance criterion #2 ("Running Bash not Working") is structurally impossible to satisfy via the typed-state pipeline as wired. Every pending-tool override produces the generic label; the richer "Reading STATE.md…" / "Editing x.ts" / "Running Bash" labels that `client/src/components/chat/ContextBar.tsx:48–68` can already derive from `ChatMessage[]` are **blocked** because `resolveActionLabel` returns `'Running tool…'` and does NOT fall through to `jsonlLabel` for the ToolExec case (see `contextBarAction.ts:123–135`: only `Working:Generic` falls through; `Working:ToolExec` returns unconditionally).

This is the textbook **policy inversion** pattern from memory `feedback_policy_inversion_default_render.md`. Phase 1 promoted server-typed state to authoritative; the server's typed-state producer is **impoverished** relative to the legacy client-side derivation. Result: rich labels masked by a generic one.

### 4.2 Temporal drops: UI can read silence or idle between dispatch and first broadcast

| # | Drop | Window | Mitigation today | Gap |
|---|---|---|---|---|
| D1 | Stop-hook / JSONL append race — Stop fires before `tool_use` line flushed → gate at hook-event.routes.ts:344 returns false → row flips to `'idle'` | ≤ 1 poll tick (5s) | Poller override corrects on next tick; Phase N.0 P1 gate empirically holds (see **v2 update**) | **v2 update — REFUTED at JSONL-summary level.** Empirical check over the live transcript `ac67d1d9-*.jsonl` (442 lines, 106 `tool_use` / `tool_result` pairs, 10 `stop_hook_summary` records): ZERO `stop_hook_summary` records land between a `tool_use` timestamp and its matching `tool_result` timestamp. Claude Code only records end-of-turn Stops in the JSONL summary stream. Combined with the `hasPendingToolUseInTranscript` gate at hook-event.routes.ts:344, the race risk on the common path is empirically minimal. Residual risk (not refuted): if Claude Code fires mid-tool Stop *webhooks* to `/api/hook-event` that are NOT recorded as `stop_hook_summary`, the gate still reads the transcript file — which at that instant must already contain the `tool_use` line for the gate to work. A direct live-capture of `stat -f %Sm <jsonl>` vs Commander's `console.log('[hook:Stop]')` timestamps is pre-code step 1 of the fix rotation to rule this residual out. **Until then, §6.2's client-side OR-gate is motivated primarily by D2 latency, not D1 race.** |
| D2 | `POLL_INTERVAL = 5_000` — poller ticks every 5s; first tick after `tool_use` arrival has up to 5s latency | ≤ 5s | none | Elapsed timer, label, bubble indicator all lag |
| D3 | `FORCE_IDLE_COOLDOWN_MS = 60_000` — if this row force-idled in the last 60s, the override at status-poller.service.ts:260 `continue`s BEFORE the pending-tool check at :302 | ≤ 60s | none | A legitimately-working session that happened to stale-force-idle recently will show `Idle` for up to 60s into its next tool call |
| D4 | `HOOK_YIELD_MS = 60_000` skips the classifier when a hook fired recently AND override didn't fire. The override only fires when `hinted.status ∈ {idle, waiting}`. If `hinted.status === 'working'` (e.g. allowlist upgraded from a stale verb) the override is skipped, the classifier is skipped, and the pane-regex branch never gets a chance to change the label | ≤ 60s | none | Label fossils mid-turn |
| D5 | LiveActivityRow gated on `isWorking && !heartbeatStale` (ChatThread.tsx:701). `heartbeatStale` uses a 30s staleness threshold on `session_ticks.updated_at`. During any tool execution the statusline stream goes silent (§3.3). So on any tool that runs > 30s the chat-bubble indicator unmounts even though the session IS working | 30s+ | — | Chat bubble goes silent on long operations |

### 4.3 Symptoms → criteria mapping (not exhaustive — representative)

| Acceptance criterion | Violated by |
|---|---|
| 1 — continuous live indicator | D1 + D2 + D5 |
| 2 — specific labels ("Running Bash" not "Working") | §4.1 (controlling) |
| 3 — elapsed climbs 1s/s | D1 + D2 (LiveElapsed resets when `isWorking` flips to false and back) |
| 4 — idle means waiting-for-user | D1 (transient idle during tool exec) |
| 5 — state never flashes backward | D1, D3, D4 |
| 6 — sub-agents + compaction have distinct labels | Partially — Compacting is wired; sub-agents inherit `Idle:MonitoringSubagents` which maps to `null` label (contextBarAction.ts:140). The `Working:Thinking` subtype exists but is populated only from the `THINKING_VERBS` allowlist — Codeman-style per-tool verbs don't reach it. |
| 7 — Plan widget gated on TodoWrite/TaskCreate events | Out of scope for this diagnostic; handled by `getActivePlan` + F-phase age-gating. |
| 8 — system/synthetic messages render distinctly | Phase K shipped SystemEventChip — not part of this class |
| 9 — stop button targets correct session in split view | S.1 landed pane-id routing — not part of this class |
| 10 — no stale input buffer reappearing | Separate surface |

---

## 5. Class root cause (single explanation)

**Phase 1 inverted the policy without moving the information.**

Before Phase 1: ContextBar's `getActionInfo(messages)` walked the last assistant block on the client and produced a rich, tool-name-aware label ("Reading STATE.md…", "Running command…") from the ChatMessage stream that `useChat` already maintained. Legacy, slightly inconsistent across surfaces, but **information-rich**.

After Phase 1: the authoritative source became server-computed `SessionState`. The server's producer (`computeSessionState` + `hasPendingToolUseInTranscript`) operates on **boolean presence** of a pending tool_use and does NOT carry the tool name, file path, or any of the richer signal the client had. The client's rich `getActionInfo` still exists but is now **gated off** by `resolveActionLabel` returning a generic string for `Working:ToolExec`.

Compounding this, every timing constant in the pane pipeline is tuned to **suppress churn** (8s grace, 60s yield gate, 60s force-idle cooldown, 90s stale-force-idle). Those gates were added one at a time to fight specific oscillation bugs; stacked, they produce multi-second-to-multi-minute windows where the server's view of session state cannot change even when JSONL tells a different story.

The **single coherent class root cause** is: _Commander treats "compute session state" as a server-owned, poll-driven, impoverished pipeline gated against churn — but the user-experience acceptance criteria demand a client-owned, event-driven, information-rich pipeline gated only against visible wrongness._ Every symptom Jose reports reduces to a direct consequence of this inversion: the server is too slow, too coarse, and too conservative to be authoritative, but Phase 1 promoted it anyway. Phase 1.1 widened the override to fire from `waiting`, which is correct but still leaves four other drop-points (D1–D5) and the controlling name-plumb drop (§4.1).

---

## 6. Proposed fix shape (contract-level — no code, no file:line patches)

**Principle.** Invert the invert. The **client's rich derivation** is the authoritative label source for `Working` (and nested subtypes like ToolExec / Composing / Thinking). The **server's typed state** is authoritative for coarse lifecycle transitions (Stopped, Error, WaitingForInput, Compacting, Idle-vs-Working) where the client has no better signal than the server.

This is a reversal of Phase 1's Working-path priority **without** giving up the structural wins of the typed state enum (type safety, Waiting subtype differentiation, Compacting signal, future sub-agent subtype).

### 6.1 Label-source priority, new

For a session whose coarse status is `working`:

```
1. Client jsonlLabel from getActionInfo(messages)     ← authoritative for Working
2. Typed-state subtype hintLabel (Thinking verb)
3. terminalHint (legacy, Compacting literal)
4. Generic "Working..." fallback
```

For Waiting, Stopped, Error, Compacting, Idle: typed state wins (unchanged).

Implementation contract: `resolveActionLabel` inverts the `Working` branch so `jsonlLabel` is consulted before `Working:ToolExec`'s `'Running tool…'` generic. The `Working:ToolExec` case remains in the enum for correctness of reporting and DevTools but is no longer the UI label source.

### 6.2 `isWorking` derivation, new

Today `isWorking` gates LiveActivityRow + ContextBar animation. Derive it from **two OR conditions**:
  - Server says `session:status` coarse status is `'working'`.
  - Client observes an **unmatched tool_use** in the latest `ChatMessage[]` (scan the tail for `tool_use` not followed by a `user.tool_result` with matching id).

The client-side detection is symmetric to the server's `hasPendingToolUseInTranscript` but runs on the ChatMessage stream the client already has, updating immediately on `useChat`'s poll — no waiting for the 5s poller + WS round trip. The server's `isWorking` still wins for post-turn-end recovery (client's tool_use scan won't detect `Composing` without explicit plumbing, which is fine — Composing isn't a tool).

### 6.3 Heartbeat staleness for LiveActivityRow, new

LiveActivityRow's `heartbeatStale` predicate should exclude the tool-execution window. Today it treats "no statusline tick in 30s" as "probably dead". During a single long tool call (`pnpm test`, a long grep, network request) the statusline goes silent **intentionally**. The predicate should be:

```
heartbeatStale := (no statusline tick in 30s) AND (no unmatched tool_use in ChatMessage tail)
```

So the bubble indicator stays live for the full duration of any tool call, regardless of statusline silence.

### 6.4 Poller timing re-tune (optional, secondary)

Not strictly required by the 10 criteria once §6.1–6.3 land — the client-side path makes server latency invisible on the label. But for defense-in-depth on criterion #5 (no backward flashes):
  - Drop `POLL_INTERVAL` to 1–2s **OR** tighten the override to run on every hook event, not just poller ticks.
  - Narrow `FORCE_IDLE_COOLDOWN_MS` to not suppress the pending-tool override (the override is the one signal we trust more than the cooldown).

These are optional and should land in a separate rotation after §6.1–6.3 smoke green.

### 6.5 Regression surfaces to watch

- **Phase N.0 P1 Stop-hook gate** — still correct; the gate on `hasPendingToolUseInTranscript` keeps the 'idle' flip suppressed during tool exec. Do not touch.
- **Phase J.1 IDLE_VERBS allowlist** — still correct; the `working` verb classification is not on the path we're changing.
- **Phase L B1 past-tense gate** — still correct; only affects pane-derived status, not the new client-side label path.
- **Phase M statusline forwarder / Phase S.1 tick-first ContextBar** — the `resolveContextPercent` tick-first invariant is unaffected. Context %, not label.
- **Compacting label** — unchanged. Typed-state `Compacting` still wins the label (as it should).
- **Sub-agent monitoring** — out of scope for the minimum fix; a follow-up rotation can add `Idle:MonitoringSubagents` → "Monitoring N subagents" label if desired (criterion #6 partial).
- **ChatMessage[] source.** If `useChat` delivers messages with any truncation (Phase K's consecutive-same-teammate collapse, etc.), the client-side tool_use scan must tolerate it. Verify `useChat` output vs. raw JSONL parity before relying on a tail scan.

### 6.6 Codeman reference — READ (v2)

Codeman source located at `/Users/josemiguelbonilla/.codeman/app/` per Jose's pointer. The relevant derivation lives in `src/transcript-watcher.ts`.

**Codeman's tool-exec detection, summarized (file:line refs for audit trail):**
- `state.currentTool: string | null` (line 56) — stateful single scalar carrying the running tool's name.
- `state.toolExecuting: boolean` — gate for "is a tool running right now".
- On parsing each new assistant content block (line 366–370): if `block.type === 'tool_use'` and `block.name` present → `toolExecuting = true; currentTool = block.name`, then emits `'transcript:tool_start'` with the name.
- On encountering a matching `tool_result` block (line 371–379): `toolExecuting = false; currentTool = null`, emits `'transcript:tool_end'` with the name + error flag.
- The watcher runs on the JSONL file via stream-tail; no 5s poller, no pane-regex, no typed-state enum.

**Convergence with §6 — CONFIRMED.** Codeman's architecture is literally the shape §6.1 + §6.2 propose: client-owned, rich-named, transcript-tail-driven tool-exec detection. No server round-trip; the component that watches the JSONL also produces the label. Commander's client can mirror this pattern by running `getActionInfo`-style scans against `useChat`'s `ChatMessage[]` (which the server parser preserves verbatim per §8 Q2 answer) and — per Q3 — binding the result to both the label rendering and the `isWorking` gate.

**Delta from Codeman** worth noting (none block the §6 fix):
- Codeman tracks `currentTool` as a scalar, mutated in order of JSONL block arrival. Commander's client uses a single `ChatMessage[]` snapshot per poll, so the equivalent is "scan the tail, find the latest unmatched `tool_use.id`, return its name". Same end state, different access pattern. No semantic divergence.
- Codeman only holds one tool at a time; Commander supports parallel tool_uses (Claude Code emits them in a single assistant block). `getActionInfo` already picks the *last* tool_use in the last assistant block, which is a reasonable approximation — the user sees the most-recently-dispatched tool's name, matching Codeman's last-write-wins semantics.
- Codeman emits `transcript:tool_end` only when the `tool_result` is seen; Commander's client can do the same on its ChatMessage tail. No work for the server beyond what it already does.

**Architectural flaw check (Jose's pre-code gate):** None revealed. §6 does not need revision.

### 6.7 VSCode Claude extension — optional secondary reference

Public repo available if Jose wants a third data point to validate §6 after Codeman convergence. Not required for the minimum fix.

---

## 7. Acceptance approach (live-smoke plan, pre-green)

No PHASE_REPORT will claim green without executing this sequence. Unit-green is _necessary_ but _insufficient_.

1. **Live smoke matrix.** In an attached session:
   - `sleep 10 && echo done` → ContextBar label must read "Running command…" (or "Running Bash…") **continuously** for all 10 seconds. Elapsed timer climbs 1, 2, 3, …, 10. LiveActivityRow visible throughout.
   - `ls -la ~/` right after approval → label flips from "Waiting for approval" directly to "Running command…" with no Idle gap.
   - `Read /tmp/file.ts` → label reads "Reading file.ts…".
   - `Edit /tmp/file.ts` → label reads "Editing file.ts…".
   - A spawned sub-agent → Plan widget shows only on real TodoWrite, not on text-list shape.
   - `/compact` → label reads "Compacting context…" for the duration; no Idle flash.
2. **Non-regression.** Attach a session that was already triggering Phase 1/1.1's existing fixes; confirm no new false-positive labels, no stuck-working, no oscillation alarms.
3. **Self-dogfood** (per memory `feedback_self_dogfood_applies_to_status_fixes.md`). The teammate running inside Commander IS a live session. If THIS coder-16's own ContextBar doesn't reflect accurate labels during its own work, green is not granted. (This is the cheapest single regression guard the project has.)
4. **Unit + integration.** ContextBar label matrix, `isWorking` derivation with unmatched-tool-use tail scan, `heartbeatStale` with tool-use exemption. But these are checkpoint signals, not acceptance.

PHASE_REPORT declares green ONLY when all four passes.

---

## 8. Pre-code investigation outcomes

Original open questions from v1 are closed as of v2:

1. **Q1 — JSONL append vs Stop hook ordering.** EMPIRICALLY REFUTED at JSONL-summary level (0/106 overlaps in live transcript sample). Residual live-capture of Stop webhook timing vs JSONL `stat` mtime deferred to pre-code step 1 of the fix rotation as a belt-and-braces check. See §4.2 D1 v2 update.
2. **Q2 — ChatMessage[] fidelity.** CONFIRMED. Server parser (`jsonl-parser.service.ts:126–135, 165–171`) preserves `tool_use.{id,name,input}` and `tool_result.{tool_use_id→toolUseId, content, is_error}` verbatim. useChat delivers these without transformation. The §6.2 client-side OR-gate can correlate pairs by id reliably. The Phase K consecutive-same-teammate idle-collapse operates on wrapper messages only — tool blocks pass through untouched.
3. **Q3 — Codeman source.** READ at `/Users/josemiguelbonilla/.codeman/app/src/transcript-watcher.ts`. Codeman maintains `currentTool: string | null` + `toolExecuting: boolean` as state mutated on each JSONL `tool_use` / `tool_result` arrival and emits `transcript:tool_start` / `transcript:tool_end` events with tool names. This is precisely the architecture §6.1 + §6.2 propose. §6 CONFIRMED converging on a known-good pattern. No flaws revealed. See §6.6.
4. **Q4 — VSCode Claude extension.** Not fetched — Codeman convergence is sufficient validation per §6.6. Available as secondary reference if Jose wants a third data point.
5. **Q5 — Scope.** LOCKED to §6.1–§6.3 (minimum fix) per Jose. §6.4 poller re-tune deferred to follow-up rotation ONLY IF live smoke post-§6.1–6.3 reveals remaining server-latency visible symptoms. §6.6 (sub-agent label, criterion 6) also deferred. Do not extend.

**Code authorization gate (per team-lead dispatch):** PM + Jose must ratify this v2 doc. If ratified, fix rotation proceeds with pre-code step 1 (Stop-webhook timing live-capture) → then §6.1–6.3 implementation → then acceptance gate per §7.

---

## 9. Summary

- Ground truth: JSONL is SILENT during tool execution (§2). Any continuous live-indicator must come from state captured BEFORE the silence or from a non-JSONL source.
- Class root cause: Phase 1 promoted server-typed state to authoritative for labels, but the producer is impoverished vs. the pre-existing client-side rich derivation. The generic `'Running tool…'` string returned for `Working:ToolExec` without a `toolName` blocks the richer `getActionInfo` label the client can already derive. Compounded by 5 distinct temporal gates (D1–D5 §4.2) that collectively open multi-second-to-multi-minute windows of wrong state.
- Fix shape: Invert the Working-path label priority so the client's rich `getActionInfo(messages)` wins for `working` coarse status, keep the typed state authoritative for Stopped/Error/Waiting/Compacting/Idle, add a client-side unmatched-tool-use OR-gate for `isWorking`, and exempt tool-execution windows from `heartbeatStale`. Server-side poller tuning is defense-in-depth and optional for the first ship.
- Acceptance: live smoke against the 10 criteria + self-dogfood of THIS teammate's ContextBar + non-regression over Phase 1 fixes. Unit-green is insufficient.

**Awaiting PM + Jose review before any code is written.**

---

## 10. §6.1 Wire-Through Investigation (v4 addendum — post §6.1–6.3 smoke)

**Rotation:** §6.1.1 + §6.4 — INVESTIGATION ONLY, NO CODE. Author: coder-16, 2026-04-20.

### 10.0 Smoke result re-cap

§6.1–6.3 shipped as `ec3528a` / `94e5c09` / `0aefcf1`. Jose live-smoke verdict: **partial pass**.
- **PASS:** chat-bubble `LiveActivityRow` now shows continuous per-tool verbs (Thinking / Reading / Editing / tool names / elapsed). Parity with Codeman achieved.
- **FAIL:** `ContextBar` status-bar label stayed on `"Idle — Waiting for instructions"` during case 1 sleep 10; showed `"Waiting for approval"` during the modal in case 2 then flipped straight to Idle during bash execution (never `"Running command"`); on case 4 Edit the activity appeared briefly, vanished during approval-pending, and never showed a continuous label during approved execution.
- Jose quote: *"The system is just slow to show what Claude is really doing inside the chat window and sometime doesn't show it at all. The ContextBar status always stays 'Idle — Waiting for instructions'."*

This is **§20.LL-L10 instance 3** (unit + integration tests green, integration surface not wired). The trace below names the exact divergence point with source evidence so the instance-4 risk is eliminated before code is written.

### 10.1 ContextBar status-bar label render path (BACKWARD trace from the broken DOM output)

Target DOM: `"Idle — Waiting for instructions"` in the status bar above the chat input.

1. **Render site** — `ContextBar.tsx:535` — `<span>{status.label}</span>` inside the status-dot row.
2. **Signal upstream** — `status` is `statusInfo` (with an optional `(queued)` suffix at `ContextBar.tsx:377-379`). `statusInfo` is the return value of `getStatusInfo(effectiveStatus, effectiveAction, hasPrompt, activeTeammateCount, workingTeammateCount)` called at `ContextBar.tsx:372`.
3. **`getStatusInfo` branches** (`ContextBar.tsx:103-154`):
   - `sessionStatus === 'working'` → returns `{ label: actionLabel ?? 'Working...', ... }` (line 110-116).
   - `sessionStatus === 'waiting'` → returns `Waiting for approval` / `Monitoring N teammates` / `Waiting for input`.
   - Default (idle / stopped / undefined) → returns `{ label: 'Idle — Waiting for instructions', ... }` (line 150-153).
4. **`effectiveStatus` upstream** — `ContextBar.tsx:370`: `const effectiveStatus = userJustSent && sessionStatus !== 'working' ? 'working' : sessionStatus;` — pinned to the `sessionStatus` PROP unless `userJustSent` overrides.
5. **`actionLabel` upstream** — `ContextBar.tsx:337-342`: `resolveActionLabel({ isWorking, jsonlLabel, terminalHint, sessionState })`.
6. **`isWorking` inside ContextBar** — `ContextBar.tsx:323`: `const isWorking = sessionStatus === 'working' || userJustSent;`. **This is locally derived. It does not receive any §6.2 / §6.3 signal.**
7. **`jsonlLabel` derivation** — `ContextBar.tsx:324`: `const rawJsonlAction = isWorking ? getActionInfo(messages) : null;`. **`getActionInfo` is EARLY-GATED on the local `isWorking`. When local `isWorking=false`, `rawJsonlAction=null` → `jsonlLabel=null`.**
8. **Prop source** — `ChatPage.tsx:558`: `<ContextBar ... sessionStatus={session?.status} ... userJustSent={userJustSent} ... />`. Raw server status from `session.status` (`useChat` hook) is passed verbatim.

**End of trace.** The ContextBar's status-bar label depends on the local `isWorking` which is `sessionStatus === 'working' || userJustSent` — it NEVER consults `hasUnmatchedToolUse`, it NEVER consults the heartbeat exemption, it is NOT the `isSessionWorking` computed in ChatPage.

### 10.2 LiveActivityRow label render path (BACKWARD trace from the WORKING DOM output)

Target DOM: `"✻ Reading STATE.md · 4s · 5,042 tokens · xhigh"` style row.

1. **Render site** — `LiveActivityRow.tsx:71-92` — renders via `AnimatePresence` gated on `visible && parts.length > 0`.
2. **`visible` prop upstream** — `ChatThread.tsx:701`: `<LiveActivityRow visible={isWorking && !heartbeatStale} ... />`.
3. **`isWorking` prop on ChatThread** — `ChatThread.tsx:397`: `isWorking = false,` default; passed by ChatPage.
4. **`isWorking` at ChatPage** — `ChatPage.tsx:329-332` (after §6.2): `const isSessionWorking = userJustSent || (session?.status === 'working' && !heartbeatStale) || unmatchedToolUse;` — includes the §6.2 OR-gate.
5. **`heartbeatStale` prop on ChatThread** — `ChatPage.tsx:499` (after §6.3): `heartbeatStale={heartbeatStale && !unmatchedToolUse}` — includes the §6.3 exemption.
6. **Label parts** — `LiveActivityRow.tsx:14-39` `buildLiveActivityParts(activity, tick)` — reads `session.activity` (pane-parsed verb/spinner/elapsed/tokens/effort) and `tick.contextWindow.usedPercentage`. Does NOT use `resolveActionLabel` at all.

**End of trace.** LiveActivityRow rides an entirely different label pipeline (pane-parsed `SessionActivity`), and its visibility gate consumes ChatPage's §6.2+§6.3 composite signal.

### 10.3 Divergence point (named, with evidence)

| Surface | `isWorking` signal source | §6.2 OR-gate? | §6.3 heartbeat exemption? | Label source |
|---|---|---|---|---|
| `LiveActivityRow` (chat bubble) | **ChatPage.tsx:330** `isSessionWorking` | **YES** | **YES** (via heartbeatStale override at ChatPage.tsx:499) | `SessionActivity` (pane-parsed) + `SessionTick` |
| `ContextBar` (status bar) | **ContextBar.tsx:323** local `sessionStatus === 'working' \|\| userJustSent` | **NO** | **NO** (never consumed) | `resolveActionLabel` over `{isWorking, jsonlLabel, terminalHint, sessionState}` |

**Divergence point: `ChatPage.tsx:549-571` (the `<ContextBar …/>` call site).** ChatPage passes `sessionStatus={session?.status}` — the raw server status — to ContextBar instead of its already-computed `isSessionWorking` (which OR-gates `hasUnmatchedToolUse` and is the same signal that made `LiveActivityRow` work). ContextBar then redundantly (and impoverishedly) re-derives its own `isWorking` from that raw status.

Why §6.1 didn't reach: §6.1 inverted the Working-branch of `resolveActionLabel` so `jsonlLabel` wins — but when ContextBar's local `isWorking` is false, `getActionInfo` is never even called (ContextBar.tsx:324 short-circuits to null), so `jsonlLabel` is always null in the failing cases, and `resolveActionLabel` never reaches its Working branch at all — it either takes the Idle typed path (→ null) or the fall-through legacy path with no `jsonlLabel` to promote. **§6.1 was correct as written; its input signal was never allowed to flow.**

### 10.4 `resolveActionLabel` reach map

Grep of the entire client tree:
- `contextBarAction.ts:128` — definition.
- `components/chat/ContextBar.tsx:23 + :337` — **the ONLY prod caller**.
- `utils/__tests__/contextBarAction.test.ts` — tests (non-prod).

Exactly one production render surface feeds from `resolveActionLabel`. Fixing the wire at that single call site is sufficient; no other consumer exists to coordinate with.

### 10.5 ContextBar render evidence

**Not captured via a live ephemeral trace.** The contract-level divergence named in §10.3 is backed by direct source evidence:
- `ContextBar.tsx:323` — literal text of the local `isWorking` derivation showing no consumption of §6.2/§6.3 signals.
- `ContextBar.tsx:324` — `rawJsonlAction` early-gate.
- `ChatPage.tsx:550-571` — the full `<ContextBar …/>` prop list showing `sessionStatus={session?.status}` is the only status-like signal passed, and `isSessionWorking` / `unmatchedToolUse` / `heartbeatStale` are NOT threaded to it.

A live console trace would only confirm what the source already states: ContextBar cannot see the OR-gate. Ephemeral console.log instrumentation is left as OPTIONAL smoke-time validation for the post-§6.1.1 rotation — flagged here so PM can require it pre-ship if desired. Per guardrail 2, Jose's browser smoke is the authoritative acceptance gate regardless.

### 10.6 Proposed §6.1.1 wire-through fix (contract level — NO line patches)

**Layer:** `ChatPage.tsx` → `ContextBar.tsx` props boundary. Touch two files only. ContextBar.tsx changes are mechanical (consume new props instead of deriving locally); the logic lives at the ChatPage level where the OR-gate already lives.

**Approach (one layer, narrow contract):**

1. **ChatPage.tsx** — pass the already-computed composite signal to ContextBar. Two new props (or one — decision in PM review):
   - `isWorkingOverride: boolean` — the value of `isSessionWorking` (already computed at `ChatPage.tsx:330`). When true, ContextBar treats the session as working regardless of raw `sessionStatus`.
   - *(optional)* `unmatchedToolUse: boolean` — exposed in case ContextBar wants to branch more finely. Not required by the current fix; flag for future sub-agent / plan work.
2. **ContextBar.tsx** — collapse the local `isWorking` derivation to consume the override:
   - `const isWorking = isWorkingOverride ?? (sessionStatus === 'working' || userJustSent);` (null-safe default preserves backward compat if a caller omits the prop).
   - The `effectiveStatus` pin at line 370 becomes `isWorking ? 'working' : sessionStatus` so `getStatusInfo` takes the Working branch when the composite signal is true.
   - `resolveActionLabel` call is unchanged — `isWorking` feeds it correctly; `jsonlAction` computed at line 324 will now be populated for the target cases.
3. **Tests (user-observable framing — §20.LL-L10 hardening):**
   - *New* — "given ChatPage passes `isWorkingOverride=true` while `sessionStatus='idle'`, ContextBar status-bar reads the rich jsonl label (not `'Idle — Waiting for instructions'`)." Pin the user-observable DOM shape, not the `isWorking` local variable.
   - *New* — "given a ChatMessage with unmatched Bash tool_use and raw `sessionStatus='idle'`, ContextBar status-bar reads `'Running command…'`." Integration-style, feeds the entire prop stack.
   - *Non-regression* — existing Compacting precedence / Stopped / Error typed paths unchanged.

**Rationale for single-prop approach:** ContextBar's local `isWorking` derivation is redundant — ChatPage already computes the authoritative composite. Deleting the redundant derivation is the correct shape; adding independent signals (e.g., threading `unmatchedToolUse` AND `heartbeatStale` separately) would fork the truth source and is the instance-4 risk.

**Blast radius:** ChatPage.tsx + ContextBar.tsx + new test cases in `contextBarAction.test.ts` or a new `ContextBar.test.tsx`. Typed signatures keep the prop threading safe. No other consumers touched.

### 10.7 Proposed §6.4 poller re-tune (minimum, two deltas)

Current constants (`server/src/services/status-poller.service.ts`):
- `POLL_INTERVAL = 5_000` (line 11) — poll cadence.
- `FORCE_IDLE_COOLDOWN_MS = 60_000` (line 90) — suppression window after a force-idle flip.
- (`HOOK_YIELD_MS = 60_000`, `STALE_ACTIVITY_MS = 90_000`, `IDLE_GRACE_MS = 8_000` — NOT changed.)

**Delta 1 — `POLL_INTERVAL: 5_000 → 1_500ms`.**

Justification: Jose observed a user-visible 5s minimum between prompt send and activity indicator in case 1 / case 2. Root cause: the server cannot re-classify between polls. Dropping to 1.5s matches the client's working-mode poll cadence at `useChat.ts:182` and cuts worst-case status-stale window from 5s to 1.5s (a 70% reduction in the symptom window). 1.5s is deliberately the floor: going lower trades against CPU cost on a per-tick pane read of all live sessions.

**Delta 2 — exempt `FORCE_IDLE_COOLDOWN_MS` when `hasPendingToolUseInTranscript` returns true.**

Current behavior (`status-poller.service.ts:259-262`): after a force-idle flip, skip ALL reclassification for 60s.

Observed symptom: after a stale force-idle (e.g. Claude briefly blank-pane at turn end), the user sends a new prompt that starts a Bash. The pending-tool-override at line 302-308 would normally upgrade idle→working, but the cooldown `continue` at line 261 short-circuits the poll BEFORE that override runs, so status stays 'idle' for up to 60 more seconds even while the tool executes. This is the 1-minute-latency Jose named for case 2.

Proposed change (contract level):
- Before the cooldown `continue` at line 261, read the transcript once: `const pendingDuringCooldown = transcriptPath ? hasPendingToolUseInTranscript(transcriptPath) : false;`
- If `pendingDuringCooldown === true`, let the poll fall through to the pending-tool override at line 302-308 (do NOT `continue`). The cooldown exists to suppress PANE-REGEX reclassification oscillation — the structured tool-use signal is orthogonal and strictly higher-trust (per §24.2 and Issue 15.1-H commentary already in the file).
- No change to the cooldown stamping path.

**Out of §6.4 scope (explicitly deferred, per guardrail 3):**
- `HOOK_YIELD_MS`, `STALE_ACTIVITY_MS`, `IDLE_GRACE_MS` — untouched.
- The pane-regex classifier itself — untouched.
- Any re-architecting of the gate ordering — untouched. The two deltas above are additive.

### 10.8 Acceptance approach

Live browser smoke (Jose, authoritative) — same 6-case × 10-criterion matrix from §7. This rotation ADDS explicit ContextBar status-bar label capture for cases 1 / 2 / 4 (the three that broke):

| Case | Expected ContextBar status-bar label | Criterion coverage |
|---|---|---|
| 1. `sleep 10` | `"Running command…"` for the full 10s window, elapsed counter climbs | C1 / C2 / C3 / C5 |
| 2. post-approval `ls` | During modal: `"Waiting for approval"`. After approval: `"Running command…"` until tool_result lands | C1 / C2 / C4 / C9 |
| 4. `Edit file.ts` | Pre-approval: `"Editing file.ts…"`. Post-approval: same label until flush | C1 / C2 / C5 |

Non-regression re-verified:
- 15.1-A compacting → `"Compacting context..."` wins (typed path untouched).
- 16.1.1 idle → `"Idle — Waiting for instructions"` when truly idle (no unmatched tool_use, no userJustSent).
- Issue 8 P0 composing false positives — `shouldSuppressComposingLabel` untouched.

**Self-dogfood:** this coder's own Commander session is the cheapest regression guard per `feedback_self_dogfood_applies_to_status_fixes`. Post-ship, the ContextBar for this very session must show `"Running Bash…"` / `"Reading …"` during operations instead of flashing to Idle.

**Self-verification disclaimer (guardrail 2):** ContextBar status-bar render cannot be self-verified by a browser-less teammate. PHASE_REPORT must explicitly state: *"ContextBar status-bar render NOT self-verified, awaiting Jose browser smoke."* No "tests pass, ship it" framing.

### 10.9 Ratification checklist

- [ ] PM reviews §10.3 divergence name + §10.6 fix shape + §10.7 two poller deltas.
- [ ] PM escalates to Jose for final authorization.
- [ ] Jose approves code rotation scope: §6.1.1 wire-through + §6.4 two-delta re-tune + tests-as-user-observable.
- [ ] Code rotation proceeds with SEPARATE commits per §6.1.1 and per §6.4 delta (3 commits minimum, small, reversible).
- [ ] PHASE_REPORT after code is DRAFT-only until Jose live-smoke passes.

**Awaiting PM + Jose review before any code is written.**
