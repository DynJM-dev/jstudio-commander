{
  "name": "commander",
  "version": "0.1.0-n2",
  "description": "Command-Center multi-session orchestrator. Streams Claude Code hook events to the local Command-Center sidecar via a small bash forwarder (hooks/forward.sh reads ~/.commander/config.json for port + bearer on every invocation). Requires Command-Center running locally.",
  "author": { "name": "JStudio" },
  "license": "MIT"
}
